# src/data/ — content, the single source of truth

Parent: [`../CLAUDE.md`](../CLAUDE.md) → [`/CLAUDE.md`](../../CLAUDE.md).
Any change in this folder updates this file and the root repo map in the
same change.

**This is the only place resume content or toast copy may live.** Per
spec §7: "no component may hardcode copy." If you are building a
component and reach for a string literal that is resume content
(a header, a body paragraph, a tag, a choice label), stop and add it here
instead.

## Files

| File | Status | Notes |
|---|---|---|
| `episodes.js` | ✅ | Exports `episodes` (5 episode objects), `PLAIN_SECTIONS`, `getEpisodeById(id)` and `getNextEpisode(id)`. **Reorganized this batch from chronology to category**, on direct user feedback ("it should NOT be going through my life"): the old `origins`/`guild` split told the story in narrative order (an "origin story" episode mixing school notes-writing, a first hackathon, and university rank; a "roles" episode mixing a paid TA job with unpaid campus leadership), which made "what did they do professionally vs. academically vs. for fun" only answerable by reading every panel. Each episode is now a single category, in this order: `education` (2 panels — university record, pre-university record), `academia` (3 panels — TA, RA, the in-progress paper; a merge of the old `lab` episode plus the TA panel pulled out of `guild`), `guild` (4 panels — campus/community leadership and moderation: ZNotes teaching pulled in from the old `origins`, FOES, NaSCon/TEDx/Isaar, ProSports), `sidequests` (5 panels — every hackathon/competition, now including PakCrypt pulled in from `origins`), `build` (3 panels + the mechanic-4 `branch`, unchanged). Total panel count moved from 16 to 17 as a direct result — no longer a strict spec §6 budget match, since the reorg was the point; word-count bands were re-verified on the two rewritten/moved panels regardless (all still inside 30–60 words body / 2–5 words header). Ids were renamed/reassigned where the category changed (`origins` → `education`; `lab` → `academia`, absorbing `guild`'s old TA panel) — safe because every consumer resolves episodes dynamically via `getEpisodeById`/`useParams`, confirmed by grep, so no other file hardcodes an episode id. `choicePrompt` and `choices` were rewritten per episode to match the new panel order (e.g. `sidequests`' choices now target `panel-0`/`panel-3` since PakCrypt is first and TaxNet moved to index 3). **`PLAIN_SECTIONS` lost its `'research'` entry** — the old `lab` episode used it to render RA/paper panels under a "Research" heading on `/plain`, but now that TA sits in the same episode as RA, giving the whole episode `plainRole: 'research'` would have duplicated the TA role (already covered by `profile.js`'s `experience[]`) in two voices. `academia`'s `plainRole` is `'covered'` instead, and the one piece of unique content that would otherwise have been lost — the in-progress image-processing paper — was folded into the Research Assistant entry's `detail` in `profile.js` so `/plain` doesn't drop it. Verified live: all 5 episodes render their new panels and choices correctly, and `/plain` shows no duplication.

**Phase 10 (content swap) is DONE for this file.** Every title, subtitle, panel body, tag, timestamp, link and choice is real content drawn from the user's master resume — no placeholder prose. `choicePrompt` on every episode is the in-universe line spoken above the choice list, fed to `Typewriter.jsx` via `EpisodeView.jsx`; the `prompt` inside `build.branch` is a *different*, still-unrendered Phase 8 field — don't conflate them. Schema additions made as Opus-level calls per the rule at the bottom of this file: `plainRole` on each episode driving what `/plain` does with it, read via `PLAIN_SECTIONS`; `branch.resolution` on `build`, the convergent outcome copy spec §5 mechanic 4 asks for (still unrendered, Phase 8 work). **Renamed and re-cut again this batch — episode titles are now PLAIN CATEGORY LABELS, and the flavor titles are gone entirely, not demoted.** Direct user instruction after seeing the chapter-select screen: "the corny ahh names for the episodes... don’t add any HERO type names. Simple ahh names... just simple things speaking what do they have in them?" `THE RECORD BOOK` → `EDUCATION`, `OFFICE HOURS` → `TEACHING & RESEARCH`, `ORGANIZED CHAOS` → `LEADERSHIP & COMMUNITY`, `48 HOURS, NO SLEEP` → `COMPETITIONS`, `THINGS THAT SHIPPED` → `PROJECTS`. `subtitle` is now a **contents line** — literally what is inside, no voice (`FAST-NUCES, A Levels, O Levels`), replacing the old mood line (`Grades, ranks, and how they got there`). The reasoning is recorded in the file’s own doc comment and is worth repeating: the game framing lives in the chrome ("SEASON 1", "EPISODE 2", the window art on `../screens/ChapterSelect.jsx`) and in the panel copy, so the label does not have to carry it — and while it was carrying it, nothing on the chapter-select screen told a recruiter that episode 2 is teaching and research, the most employable thing in the whole resume. **Ids were renamed to match** (`academia` → `teaching`, `guild` → `leadership`, `sidequests` → `competitions`, `build` → `projects`; `education` unchanged), which also makes the shareable URLs legible (`/chapters/projects`, not `/chapters/build`). Same safety argument as the last rename, re-confirmed by grep: no file outside this one hardcodes an episode id, everything resolves via `getEpisodeById`/`useParams`. The one real cost is that an existing visitor’s `localStorage` progress keys go stale and their visited marks reset — accepted deliberately, since the payload is five booleans on a portfolio site. **ZNotes (`NOTES FOR STRANGERS`) moved from `leadership` to `teaching`**, as panel index 1, directly after `THE TUTORIAL ROOM`: it is peer teaching, and filed under a leadership heading nobody looking for teaching experience would ever find it. Episode 2 is now four panels (two teaching, two research), episode 3 is three; total is still 17. The two choice sets the move invalidated were repointed — `teaching`’s "What are you researching?" now targets `panel-2` (was `panel-1`, which is now ZNotes), and `leadership`’s first choice was rewritten since the panel it pointed at left the episode. Verified live that all four of episode 2’s panels render in the intended order with the choice resolving to `KNOWLEDGE GRAPHS`. **New field: `scene`** (`lecture` / `study` / `hall` / `nightshift` / `workbench`), naming which interior `../components/EpisodeWindow.jsx` draws for that episode. It lives here rather than in a lookup table in the screen because the drawing is a rendering of what the episode contains — the same reason `plainRole` lives here. Note for whoever writes the episode interiors next: `choicePrompt` and the `choices` labels are still written in the old voice ("So — how did the grades happen?") and are the last corny surface left in this file; the user has flagged that pass as coming after the chapter-select work. **This batch — a full content pass against a new master resume (`~/Downloads/master-resume.md`), whose point was two honesty corrections.** (1) *The research panels were factually wrong for the current work.* `KNOWLEDGE GRAPHS` described generic knowledge-graph construction and link prediction, and `A PAPER IN PROGRESS` claimed a co-authored image-processing paper. The master resume says the RA post is **CHIP, an HEC-funded project on whether disease incidence in Pakistan tracks weather and natural-disaster patterns**, where sourcing and cleaning barely-documented national data is the hard part rather than the modeling; and that the image-processing work is a **separate, just-started collaboration on why knee surgeries fail**, with the role explicitly "not yet defined." Both panels were rewritten and renamed (`DISEASE AND WEATHER`, `WHY KNEE SURGERIES FAIL`), and the second now says out loud that it is too early to claim a result or a settled role. (2) *EduGap and TaxNet no longer read as personally engineered.* The master resume is explicit on both — "keep the resume honest… do NOT claim you personally engineered the harness/RAG/pipeline" — the contribution was architecture and planning input plus the pitch that won the round. Both bodies now name that split in their closing sentence, and their tags carry `Architecture`/`Pitch` instead of implementation stacks. **This is the one change here a later pass must not quietly "improve" back into stronger claims.** Additions: a fourth `leadership` panel (`OFF CAMPUS` — Sundas Foundation thalassemia volunteering, the Mehra Abadi water campaign, Code for Pakistan, the Help 4 Help webinar), since community service had no representation on the game surface at all; and a fourth `projects` panel (`CITYMIND, ONE LIVE GRAPH`), promoted out of `profile.js`'s `alsoBuilt[]` because the master resume now confirms a specific, substantial role there (the CSP layout solver, the dynamic A* router, the risk-to-cost feedback loop that makes the shared graph "live", the Tkinter dashboard, and integrating all five modules) where it had been a vague one-liner. Two `link`s that the unresolved-facts note at the bottom of this file used to list as unfindable are now on record and wired up: CityMind's repo (a teammate's account) and EduGap's. Episode 1's rank panel picked up the current standing (73 credit hours, fifth semester, two golds and a silver rather than just the golds); Parwana's names the register's 5,251 agencies. Panel total is now **19** (was 17). Bands re-verified programmatically across every panel after the pass — all bodies 30–60 words, all headers 2–5, and every `choices[].target` still resolves to an existing panel index. **Subtitles were deliberately held at or under 60 characters**, the length of the longest one `../screens/ChapterSelect.jsx`'s hero-height reservations were measured against live: that screen reserves a fixed `min-h` for the contents line precisely so the page does not resize as you hover across episodes, and a longer subtitle would wrap past the reservation and reintroduce that exact defect. Episode 4's subtitle is unchanged and is still the longest, so the measured worst case is untouched — the first draft of episodes 2 and 5 ran to 64 and 67 characters and was trimmed back for this reason, not for style. **This batch — the voice-ledger project was removed from the site entirely**, direct user request ("I don't want the voice ledger app reference here AT ALL"). Its `projects` episode panel was deleted, dropping that episode from 4 panels to 3 (now `MARIO, IN ASSEMBLY` / `CITYMIND, ONE LIVE GRAPH` / `A CITY IN C++`) and the whole-file panel total from 19 to 18. Follow-on edits in the same pass: the `projects` subtitle no longer opens with "A voice-first ledger"; the two `projects` choices were repointed and relabelled for the new 3-panel order (`panel-0` was the ledger, is now Mario); `build.branch.resolution` was rewritten to drop "a ledger app for shopkeepers"; and `profile.js`'s `alsoBuilt[]` landing-page entry was deleted. The private-repo line in this file's unresolved-facts list and one illustrative name in `docs/PortfolioSpec.md`'s Mechanic 6 mockup were also cleared. All bodies still 30–60 words, headers 2–5, every `choices[].target` still resolves. **This batch — the `/plain` Dossier rebuild (see `../screens/CLAUDE.md`) fixed a real bug in `PLAIN_SECTIONS`/`plainRole`, not a copy change.** The `competitions` episode carried `plainRole: 'projects'`, so its five hackathon panels rendered mixed into the three real `projects` panels under one flat "Projects" heading on `/plain` — a recruiter scanning for shipped work had to sort hackathon sprints out of that list by eye. `competitions` now has its own `plainRole: 'competitions'`, and `PLAIN_SECTIONS` is now two entries instead of one: `[{ role: 'projects', heading: 'Projects' }, { role: 'competitions', heading: 'Competitions & hackathons' }]` — `PlainResume.jsx` reads both `role` and `heading` from here rather than hardcoding either string a second time. Verified live: the Dossier's Projects section shows exactly 3 entries and Competitions & hackathons exactly 5. No panel copy changed. **This batch — Promptopia corrected from "no placement" to runners-up**, on direct user report that he and Raja Muhammad Ali came second. Panel header `PROMPTOPIA, NO MEDAL` → `PROMPTOPIA, RUNNERS-UP`, body rewritten to name the teammate and the second-place finish (still 30–60 words), and the `competitions` subtitle went from `Two second places, a national top ten, and one with no medal` → `Three second places and a national top ten` (well under the 60-char ceiling). Panel/choice counts and targets unchanged. |
| `profile.js` | ✅ | Exports `profile`: name, headline, summary, `highlights[]`, contact (email/location/GitHub/LinkedIn), `education[]`, `skills[]`, `awards[]`, `experience[]`, `alsoBuilt[]`, `volunteering[]`, `certifications[]`. Rendered by `../screens/PlainResume.jsx` (all of it) and by `../components/MainMenu.jsx`, which reads **only** `name` and `headline` for the identity block above the menu items — do not widen that second consumer beyond those two fields, everything else here is `/plain`'s job. It exists because `episodes.js` is a curated narrative surface and `/plain` is the recruiter escape hatch required by spec §2 rule 3 — before this file, `/plain` rendered episode panels plus a name hardcoded in JSX, with no contact details, no education and no skills, so a recruiter taking the escape hatch had no way to act on it. **This batch:** the Research Assistant `experience[]` entry's `detail` now also mentions the unpublished image-processing paper — content that used to live only in `episodes.js`'s `lab` episode, which lost its dedicated `plainRole: 'research'` when TA/RA/paper were merged into the `academia` episode (see `episodes.js`'s row above). Without this addition that fact would have silently disappeared from the recruiter escape hatch. Deliberately plain, un-themed resume prose: its only consumer is the plain document. **This batch — rebuilt against the new master resume.** The Research Assistant entry was materially wrong: it described generic knowledge-graph and link-prediction work and claimed an unpublished co-authored image-processing paper. It now describes **CHIP**, the HEC-funded disease-versus-weather project, and the image-processing work is a **separate `experience[]` entry** scoped honestly as early-stage with the contribution still being defined — rather than folded into the RA role, which is where it landed when `episodes.js`'s old `lab` episode lost its `plainRole` (see that row above; that fold is now superseded). Other corrections: `summary` rewritten around the actual current position; the BS entry notes the fifth semester and lists Fall 2026 in-progress coursework; A Levels gained its activities; the TA entry dates each course rather than listing four in one breath; ProSports names both roles and their dates instead of blurring them into one span. `alsoBuilt[]` lost CityMind (promoted to a real panel — see `episodes.js`) and gained the Governance Crisis report (Promptopia was deliberately NOT added here — it already has a panel, and `/plain` renders competition panels under Projects, so it would have appeared twice); the CPI network entry was rewritten to what the master resume actually says (betweenness, closeness and eigenvector centrality) rather than the vaguer "non-linear spatial patterns" line it carried. `volunteering[]` gained the Help 4 Help university-applications webinar; `skills[]` gained feature extraction, link prediction, SQLite, and an `Other` group for the non-engineering skills the master resume lists. Still deliberately excluded: any marks figure for the C++ Smart City — see the unresolved-facts note at the bottom of this file, which the new master resume still flags as unsettled. **This batch — the `alsoBuilt[]` voice-ledger landing-page entry was deleted**, per the user's request to remove that project from the site entirely (see `episodes.js` row above). **This batch — a new `highlights[]` array added** (the `/plain` Dossier rebuild, see `../screens/CLAUDE.md`): four `{ value, label }` pairs — `1 / 313` cohort rank, `3.96` CGPA · 73 credits, `4` courses TA'd, `2×` hackathon runner-up — feeding the new Dossier's "10-second band" stat row above `summary`. Every number here already exists elsewhere in this file (rank/CGPA in `education[]`, the TA course count derivable from `experience[]`, hackathon placements in `awards[]`) restated as short display-face values rather than a new source of truth, kept here rather than hardcoded in `PlainResume.jsx` per this repo's standing "no component may hardcode copy" rule — a stat number is copy too. **Corrected in Opus review:** the first tile's `value` is **`#1`** with label `Of 313 in cohort`, not `1 / 313` / `Cohort rank`. At 375px each tile gives its value 130px of content width, and `1 / 313` in the 40px display face needs more than that, so it wrapped to two lines and rendered that tile **107px** tall against **53px** for the other three — a visibly ragged row. Moving the denominator into the small label fixes it with no fact lost; measured after: all four values 53px, one unique height. Shortening the data was chosen over shrinking the whole band, which would have cost the '10-second' punch on every width. **This batch — `awards[]` gained a Promptopia runners-up line** (generative-AI competition, Google Developer Groups, FAST-NUCES) after the user corrected that result from "no placement" to second. It sits in `awards[]` rather than `alsoBuilt[]`: the earlier "Promptopia NOT added here, it already has a panel" note was about avoiding a duplicate *project* entry, but `awards[]` is a placements list (it already carries non-hackathon results — PakCrypt finalist, AIESEC Best Delegate) and the panel does not render into the Dossier's awards section, so there is no double-up. `highlights[]` `2×` is unchanged — Promptopia is a competition, not a hackathon. **This batch — a `photo` field and a content-density pass.** New top-level `profile.photo` (`'portrait.jpg'`, a bare filename served from `public/`) feeds the `/plain` Dossier's new identity-block headshot — `PlainResume.jsx` resolves it against `import.meta.env.BASE_URL` and hides the frame if the file is missing (same graceful-degradation pattern as the episode cover art; set `photo: null` to remove it). `MainMenu.jsx` still reads only `name`/`headline`, unchanged. Separately, on the user's instruction to make thin `/plain` entries "appear full" without overdoing it, six one-line `detail` strings were expanded to 1–2 plausible sentences describing the real scope of each role — NaSCon CS Event Head, TEDx Media & Marketing, FCS Buddy Program, Code for Pakistan, the Help 4 Help webinar, Mehra Abadi water campaign — plus the Cricnalize `alsoBuilt[]` blurb. No new roles, dates, numbers, or awards were invented; the deliberately-vague entries stay vague — the image-processing collaboration ("role not yet defined") and the EduGap/TaxNet contribution-split sentences were left exactly as the role-honesty rule below requires. |
| `toastVariants.js` | ✅ | Exports `toastVariants` (copy pool keyed by trigger: `episodeFirstOpen`, `panelExpand`, `choiceMade`, `pdfDownload`, `lateNightWindow`) and `pickToastVariant(trigger, seed)`, which cycles deterministically through the pool for that trigger. Consumed by `../components/ToastProvider.jsx`. **`lateNightWindow` is new:** it is the payoff copy for the title-screen easter egg in `../components/TitleBackdrop.jsx` (the one lit window on an otherwise dark building — click it and someone is still at a desk up there). It is deliberately the only pool written about a person rather than narrating the visitor's own browsing, because the scene is talking back rather than tracking a choice; keep it dry rather than boastful if it's ever extended. |

## Rules for anyone editing content here

- Keep placeholder body copy at roughly the same word count as the real
  content will be (spec §6: "sized to match realistic final content so
  layout does not shift when swapped") — don't shorten placeholders for
  convenience.
- `cover` paths point at `/covers/epN.webp`, which do not exist yet — see
  [`../assets/CLAUDE.md`](../assets/CLAUDE.md). Components consuming
  `episodes.js` should already have a fallback silhouette per spec §4.2
  ("the grid never looks broken before real art exists") — that's a
  component-level concern, not a data-level one.
- The `branch` object on `build` only — this is a one-joke mechanic per
  spec §5 mechanic 4, do not add a second branch to another episode.
- Phase 10 (content swap) is done for `episodes.js`. It did require two
  schema additions (`plainRole`, `branch.resolution`) and one new file
  (`profile.js`); those were made as Opus-level calls, not ad hoc, and
  are documented in the table above. The rule still stands for anyone
  else: a new field per panel/episode is a schema change — flag it as a
  decision for Opus (root `CLAUDE.md` §2) rather than adding it quietly,
  since every component reading this file assumes the current shape.
- **Unresolved facts deliberately kept OUT of the content**, flagged in
  the master resume itself and not to be invented by a later pass:
  the Smart City / "A CITY IN C++" marks (the LaTeX resume claims 134/134,
  the repo's task doc totals 102 — no number appears in the panel until
  that is settled). The "A CITY IN C++" panel's `link` is `null` because
  that repo is private. **Two entries on this list were resolved by the
  new master resume and are no longer open:** EduGap's repo URL (now on
  record, wired to its panel) and CityMind's (on a teammate's account, as
  suspected, and likewise wired up).
- **Role honesty is content, not tone.** EduGap and TaxNet were team
  builds where the contribution was design input and the winning pitch,
  not the engineering; the master resume says so in as many words and the
  panels now say so too. Do not rewrite those closing sentences into
  something punchier that reclaims authorship.
