# src/screens/ — route-level screens

Parent: [`../CLAUDE.md`](../CLAUDE.md) → [`/CLAUDE.md`](../../CLAUDE.md).
Any change in this folder updates this file and the root repo map/phase
table in the same change.

**Status: SIX screens built, routing fully wired, Phase 6 motion
pass landed.** (Corrected: this line used to say "all four screens" and
that `App.jsx` mounts routes for "all four" — that has been wrong since
the game surface landed. The four document/reading screens below are
joined by `Floor.jsx` (`/floor`) and `Play.jsx` (`/play/:id`), the two
screens of the game surface — root `CLAUDE.md` §0.1 and phase 11. All
six are nested under the same `AnimatedOutlet` layout route, so nothing
about the transition, the scroll reset or ESC handling is special-cased
for them.) `App.jsx` mounts `<BrowserRouter>` with routes for all six. `TitleScreen.jsx` and `EpisodeView.jsx` now use Framer Motion's
`useReducedMotion()` instead of an inline `window.matchMedia` check (see
the note below — this was patched in one sweep across both files, not
file-by-file, per the note that used to be here). `EpisodeView.jsx`
wraps its episode-title-card dismissal in `AnimatePresence` (spec 3.5
"Chapter open") and wraps each content panel in
[`../components/RevealPanel.jsx`](../components/CLAUDE.md) for the
scroll-reveal + parallax. `ChapterSelect.jsx` wraps its grid cards in the
same `RevealPanel.jsx`. `TitleScreen.jsx` deliberately does **not** use
`AnimatePresence` for its beat swaps — see the note in that file for why
(spec 4.1's own stage directions call for a hard cut, not a dissolve, and
a dissolve here would flash the page's `--paper` background between
beats since nothing is mounted behind the departing beat).

## Files (spec §7 file structure)

| File | Route | Status | Notes |
|---|---|---|---|
| `TitleScreen.jsx` | `/` | ✅ | Sequences `StartPrompt` → `PresentsCard` → `TitleReveal` → `MainMenu` (see [`../components/CLAUDE.md`](../components/CLAUDE.md) — all four now exist). Owns the beat-progression state and a window-level `keydown` listener (Tab/Shift excluded) so every beat is skippable by click, tap, *or* key, per spec §4.1. `StartPrompt` (beat 0, **new this batch**, not in spec §4.1's original list) is gated by the same session-scoped `hasPlayedIntro` flag as the rest — a genuine fresh visit opens on it, a same-session SPA return to `/` skips straight to `MainMenu` as before. Reduced motion (OS-level `prefers-reduced-motion`, via Framer Motion's `useReducedMotion()`, OR the Settings "motion" toggle) collapses beats 1–2 instantly, but **not** beat 0 — see `StartPrompt.jsx`'s entry for why it deliberately doesn't auto-skip. Beat swaps are a plain conditional render, not `AnimatePresence` — see the doc comment at the top of this file for why a cross-fade here would contradict spec 4.1's own "cut to" stage direction. **Fixed this batch:** the 3-beat intro was replaying in full every time `/` was reached from inside the app (e.g. `ChapterSelect.jsx`'s "← Title" link, `PlainResume.jsx`'s "Back to the interactive version" link) — jarring on a SPA nav where the visitor just saw it seconds ago. A module-level `hasPlayedIntro` flag (deliberately not `useState`/context, since it must outlive this component's unmount on route change but still reset on an actual page reload) now makes the intro a once-per-browser-session thing: it flips true the first time the `MENU` beat is reached, and the component's initial `beat` state reads it, so any later mount of `/` in the same session opens straight on `MainMenu`. A genuine fresh load/refresh still gets the full `PresentsCard` → `TitleReveal` sequence. Fires `sounds.intro` (see `../lib/CLAUDE.md`) — a short synthesized signature motif, the user's "Netflix ta-dum" request — reusing the exact same `hasPlayedIntro` flag (checked before it flips) so it plays once per session and never replays on a same-session SPA return to `/`. Routes through `useSound()`, so it respects the Settings sound toggle and browser autoplay policy like every other cue in the app. **Retimed this batch:** it now starts the instant beat 2 (`TitleReveal`) is reached, not beat 3 — "play it the moment the second loading screen comes" — and is actively cut off (not just left to finish) the moment `beat` moves on to `MENU`, via a `stopIntro` closure returned from `playSound('intro')` and called from the triggering effect's cleanup function. That cleanup fires whether `TitleReveal`'s own hold times out naturally or the visitor skips it early, so the sting reliably ends exactly when the menu appears regardless of how that beat ended. Also documented here: a live "the sound doesn't play on reload" report turned out to be correct browser behavior, not a bug — beats 1–2 auto-advance with zero interaction required, so a visitor who never clicks/taps/presses a key during them never produces a user gesture on that pageload, and no cue (not just `intro`) can play without one. See the doc comment at the top of this file and `../lib/CLAUDE.md` for the full explanation. **Then a real bug turned up in that same area** ("I still can't hear shit until I CLICK"): the beat-2 effect used to call `playSound('intro')` and flip `hasPlayedIntro` unconditionally, even when reached via `TitleReveal`'s own timer with no gesture yet — the attempt correctly failed silently, but the one-shot flag was already spent, so a later click could never trigger it again. The effect now checks the new `hasHadUserGesture()` export from `../lib/sound.js` first; if a gesture already happened it plays immediately as before, otherwise it attaches a one-time `pointerdown`/`keydown` listener (only while beat 2 is showing, torn down on the same cleanup that stops the tune) and plays on the first real one instead. See `../lib/CLAUDE.md` for the full mechanism. **Root cause finally addressed this batch, not just chased:** a live report ("I can't hear any sound... UNTIL I click on the website which is poor") made clear that catching an *incidental* click was never going to feel reliable, because there usually wasn't one that early. `StartPrompt` (beat 0) now asks for that click explicitly, up front, before anything else in the sequence — so by the time beat 2's effect runs, `hasHadUserGesture()` should already read `true` every time, and the `pointerdown`/`keydown`-wait branch becomes a safety net rather than the primary path. Verified live end-to-end: `navigator.userActivation.hasBeenActive` reads `false` while `StartPrompt` shows, and a single keypress on it both flips that to `true` and — confirmed via a patched `createOscillator` — results in all 5 of `sounds.intro`'s voices actually firing once beat 2 is reached. **Also calls `useMusic().start()`** (new `../context/MusicContext.jsx`) in an effect keyed on `beat === BEATS.MENU` — this is what tells `../components/BackgroundMusic.jsx` (now mounted once in `App.jsx`, no longer by `MainMenu.jsx`, see that file's row) it's safe to start playing, replacing "the component just mounted" as the never-play-during-beats-0-2 signal. `startMusic()` is a one-way flip and idempotent, so it's harmless that this effect also re-fires on every later same-session return to `/` that opens straight on `MENU`. |
| `ChapterSelect.jsx` | `/chapters` | ✅ | **Rebuilt this batch** from a plain responsive grid into a hero-panel + filmstrip layout, directly matching a live Telltale episode-select screenshot the user supplied (their in-game episode menu: one large "now showing" panel with synopsis + a Start button, and a row of episode tiles underneath). The hero (`Panel.jsx`, wrapped in `RevealPanel.jsx`) shows the currently-selected episode's number/title/visited-status/subtitle/`choicePrompt` and a `--signal`-filled "▸ START EPISODE N" `Link` that is the only thing that actually navigates to `/chapters/:id`. The filmstrip below it is a horizontally-scrolling row of `Panel`-as-`button` tiles (one per episode, each its own `RevealPanel` for staggered reveal); clicking a tile does **not** navigate — it sets local `selectedId` state, which re-renders the hero above. Defaults `selectedId` to `useProgress().lastEpisodeId` when set (resume-where-you-left-off, matching the reference's pre-selected tile), else episode 1. Visited episodes still get the small `--signal` corner triangle (hero art + filmstrip tiles), and nothing is ever locked — every tile is a real focusable `<button>`, every hero Start link is a real `<a>`, so the screen is still keyboard/no-timer/nothing-locked compliant per spec §2 (a keyboard user reaches an episode via Tab to the Start button, which updates its `href` as selection changes, rather than activating a tile directly — one extra step versus the old direct-link grid, mirroring how the reference UI itself works on a controller). Cover images (`/covers/epN.webp`) still don't exist, so hero and tiles alike render the same flat `--shadow`→`--dusk` gradient silhouette with the episode number watermarked. Mobile: hero collapses to a single column (art above/below text via `order-first`) and the Start button goes full-width; the filmstrip stays a horizontal scroller at every breakpoint (verified no page-level horizontal overflow — only the filmstrip's own `overflow-x-auto` scrolls). Still includes the "VIEW FULL RESUME (PDF)" link to `/plain`. `Panel.jsx` is not ref-forwarding, so there's deliberately no arrow-key roving-focus across filmstrip tiles (unlike `MainMenu.jsx`/`DialogueChoice.jsx`) — native Tab order between tiles already satisfies keyboard access, and forwarding refs through the shared Panel primitive for one screen wasn't worth the shared-component risk. Verified live: filmstrip selection swaps the hero without navigating, the Start link's `href` tracks the selection, keyboard Tab reaches every control, `npm run lint` and `npm run build` both pass. **Rebuilt again this batch — the palette, the art, the interaction model and the copy priority all changed; treat everything above this sentence as superseded where it conflicts.** The trigger was direct user feedback that the screen was "WAYY too basic" and, more specifically, that its theme was "extremely different from the MENU... it appears as we are changing the whole THEME quite quickly." That was the real defect: a recruiter arrived from a dark, parallaxing night skyline and landed on a cream document with a red button, so the route dolly read as the site swapping themes mid-click rather than as movement through one place. **The screen now sits in the same city.** `../components/TitleBackdrop.jsx` renders behind everything in its new `distant` variant (far + mid layers only, no pointer parallax, heavier scrim — see that file’s row), and each episode is drawn as a room in that city by the new `../components/EpisodeWindow.jsx`. `Panel.jsx` gained a `tone="night"` surface for the same reason (an ink border on an ink ground is invisible). **Visited state is the window being lit, not a `--signal` corner triangle** — the old screen spent eight uses of `--signal` (Start slab, six corner marks, a link glyph) against spec §3.1’s budget of three, which is why the red had stopped reading as emphasis; it now spends exactly two, verified live by walking every element under `<main>` and matching computed `background-color`/`color` against `rgb(196,50,30)`. **Tiles navigate now.** Hover or keyboard focus previews an episode in the hero (`preview` state), clicking opens it — the previous build made a tile click change the hero only, so the primary path cost two clicks. That two-step is right in the reference because it is a controller UI on a television; with a mouse it is friction the 90-second rule cannot afford. The hero’s Start button stays for touch/keyboard, and `preview` falls back to `committed` (`useProgress().lastEpisodeId`) on mouse-out so the hero is never stranded on the last tile the cursor crossed. **Nothing truncates**: three of five titles used to clip mid-word ("48 HOURS, NO SLE…"); tiles are 272px at `lg` and labels wrap instead of ellipsing — verified live that `scrollWidth <= clientWidth` on all five labels at both 1280px and 375px, and that all five tiles report identical heights. The per-tile density that used to fight for room (contents line, tags, span, panel count) moved into the hero, which computes the year span from the panels’ own `timestamp` fields (`spanFor`) rather than storing dates twice. **The `/plain` escape hatch moved into the header**, opposite "← Title", alongside a real `Season 1 · N of 5 viewed` progress line — it is a spec §2 non-negotiable and it was styled like a footer credit at the bottom of the page. Deliberately avoided, and listed here so nobody re-adds them as "polish": rounded corners, blurred shadows, a symmetric centred hero, a gradient-masked filmstrip, an icon set. The hero is weighted left and bleeds past the right margin (`lg:-mr-8 xl:-mr-16`), and the filmstrip carries a negative right margin so the row overruns the frame — measured live at 1280px, `scrollWidth` 1424 vs `clientWidth` 1225, so the last tile is clipped by the viewport edge and the strip reads as continuing rather than as five cards on a grid. Verified live at 1280px and 375px: no page-level horizontal overflow at either size, hover preview swaps the hero title/tags/meta and the Start link’s `href` together, mouse-out restores the committed episode, and both `/chapters/teaching` and `/chapters/leadership` resolve under the renamed episode ids. One verification caveat: a programmatic `.focus()` did not fire the preview in this environment’s non-composited Browser pane, but a dispatched `focusin` on the same element did, confirming the `onFocus` handler is bound and correct — real keyboard focus delivers `focusin`. `npm run lint` clean (no new warnings), `npm run build` clean at 140.69 KB gzipped JS. **Three live-reported defects fixed this batch, all in this screen.** (1) **The page resized as you hovered across tiles** ("the menu is of different size per chapters and thus, the website changes size per chapter hovering over it"). Every variable-height element in the hero now reserves its maximum height instead of sizing to its own content: the title reserves two lines, the contents line two, the tag row three chip-rows on a 375px column and two from `sm` up, and the meta line two lines on mobile (the trailing "· Viewed" only exists for visited episodes and is exactly what tipped it over on a narrow column). The hero grid also has a `lg:min-h-[360px]` floor. Reserving space is the fix rather than clamping, because truncation is the defect this screen was just rebuilt to remove. The reservations were measured per episode at each width rather than guessed — episode 4’s tags are the longest and set the floor. Verified by cycling the hover through all five episodes at 375px, 640px, 768px and 1280px and recording `document.documentElement.scrollHeight` at each step: one unique value per width (1169 / 1194 / 1162 / 948), where before it moved across a 25px range on mobile and 16px on desktop. (2) **The filmstrip’s native scrollbar is hidden** ("the scroll wheel below the chapters looks bad") via a new `.no-scrollbar` utility in `../index.css` — it read as a stray browser control bolted under the art. It is replaced by better affordances rather than by none: ◂ ▸ stepper buttons sit in the "EPISODES" heading row with an `◂ ▸ to browse` hint, and the row still scrolls by wheel, trackpad and touch. (3) **Left/Right arrow keys now move between episodes**, wrapping at both ends, and scroll the newly selected tile to the centre of the strip. This forced a state simplification that was overdue: selection is now ONE value driven by hover, focus, the steppers and the keyboard alike. The previous build split it into a committed id plus a transient hover preview that reset on mouse-out, which cannot coexist with arrow keys — a keyboard move would be silently discarded the next time the cursor left a tile. `scrollIntoView` is `behavior: 'auto'` under reduced motion and `'smooth'` otherwise, with `block: 'nearest'` so centring a tile horizontally never scrolls the page vertically (verified: `window.scrollY` unchanged). Verified live: five ArrowRight presses walk EDUCATION → … → PROJECTS → wrap, ArrowLeft steps back, both stepper buttons move the selection, `aria-current="true"` follows it, the strip reports zero scrollbar height, and `scrollIntoView` moves the row to its exact maximum offset (199.2 of 199) and back. The smooth-scroll variant could not be observed animating here — this environment’s Browser pane is not composited, so rAF-driven scrolling is frozen, the same known limitation documented against Phase 6 — but the `'auto'` path proves the mechanism and the geometry. **This batch — the window `keydown` listener gained an `Enter` branch** (direct user report: hovering an episode and pressing Enter did nothing). Enter now opens the currently-selected episode via `useNavigate()` when DOM focus is on `<body>` (the mouse-hover case, where `onMouseEnter` set the selection but nothing native would fire); when a tile `<a>` or a `<button>` genuinely holds focus, the branch returns early and native activation runs. The Left/Right handling is unchanged, now sharing the same modifier/`INPUT`/`TEXTAREA` guards. Verified live: 3× ArrowRight selects COMPETITIONS, Enter → `/chapters/competitions`. **Still open and NOT addressed this batch: `EpisodeWindow.jsx`’s art**, which the user reports as "not that well done." A redesign is on hold pending a reference screenshot of the Batman: A Telltale Series episode menu, which the user has asked this screen to take heavy inspiration from — the previous rebuild of this screen only landed because an actual screenshot was supplied rather than a description, and guessing at a reference is what produced the art being complained about. **Rebuilt AGAIN this batch, against an actual screenshot of the Batman: A Telltale Series episode menu that the user supplied as the reference to take "MAJOR and HEAVY inspiration" from. Everything above about this screen’s layout is superseded.** What was taken from the reference, in order of how much it changes the screen: the selected episode’s key art is **full-bleed** — the background of the entire screen, with no panel, border or shadow around it — and the text sits directly on it, top-left, over a left-to-right scrim that exists only to hold contrast; the hero text order is the reference’s exactly (small "Episode N", title large in the accent color, small all-caps status line, description, then the button); the Start button is a **hollow outlined rectangle**, not a filled slab; the filmstrip sits at the bottom with labels **below** each thumbnail, the selected one bright with a light border and the rest dimmed; unvisited episodes carry a small corner ribbon the way the reference tags unplayed ones with "NEW!"; a season line with circular ◂ ▸ steppers sits above the strip, and a row of secondary actions ("◂ Previous menu", "▤ Plain resume") below it — which is a better home for both than either had before. **What was deliberately NOT taken: the reference’s cyan grade and its light sans-serif.** Those would have reintroduced exactly the theme jump this screen was rebuilt to remove two batches ago. Palette stays `--ink`/`--dusk` dark and `--bone`/`--paper` light, type stays Alfa Slab One, accent stays `--signal`. **`Panel.jsx` and `RevealPanel.jsx` are no longer used on this screen** — a deliberate call, not an oversight, and the one worth pushing back on if you disagree: the comic-panel primitive is the language of the CONTENT layer and belongs on `/chapters/:id` where panels are literally comic panels; hanging paper-panel chrome on full-bleed cinematic art fought the reference on every axis. `RevealPanel` went with it because this layout no longer scrolls at desktop sizes, so a scroll-triggered reveal had nothing to trigger on and its parallax transform was only a source of overflow. Moving the `--signal` spend from a filled Start slab to the episode title dropped the screen to **exactly one** accent use, verified live. **A real bug was caught while measuring this, unrelated to the redesign brief:** the screen used `min-h-full`, and `App.jsx` wraps every screen in `PageTransition.jsx`, which is `display: contents` at rest — an element that generates no box, so a percentage `min-height` has nothing to resolve against and collapses to `auto`. This screen is the first one short enough for that to show: `<main>` measured 737px inside an 812px viewport, leaving a **75px band of the cream `--paper` body background** exposed below the artwork. Fixed with `min-h-screen`, which skips the containing-block chain entirely; other screens have the same latent issue but enough content to hide it. Verified live after the fix at 375×812, 1280×800 and 1280×600: `<main>` fills the viewport exactly with a zero-pixel gap below it, no horizontal overflow at any size, and hovering across all five tiles produces a single unique `(scrollHeight, Start-button top)` pair per width — i.e. the layout-shift defect stays fixed under the new layout. At viewport heights below ~727px the page scrolls rather than cramming, which is the intended fallback. Arrow keys, both steppers, `aria-current`, and the hidden scrollbar were all re-verified after the rebuild. Blur filters on the whole screen dropped to **one** (hero only; tiles render at `detail="low"`, see `../components/CLAUDE.md`’s `EpisodeArt.jsx` row) against ~18 for a naive full-detail approach, which matters because `PageTransition.jsx` has to zoom this screen. `npm run lint` clean, `npm run build` clean at 141.6 KB gzipped. **This batch — one link added to the secondary action row; nothing else on this screen changed.** That row (under the season stepper) was `◂ Previous menu` / `▤ Plain resume`; it is now `◂ Previous menu` / `▸ Play the floor` (→ `/floor`) / `▤ Plain resume`, in that order. It is not optional decoration: `/chapters` is the ungated Episode Explorer, and the stairwell in the overworld plus every locked door on the floor point visitors *here* — which is the entire reason locked doors are compatible with spec §2 rule 1 (root `CLAUDE.md` §0.1). Without a way back to the floor from this screen, anyone who arrived from a locked door lands somewhere with no door out except the browser's Back button. It is a plain `ACTION`-styled text link like its two neighbours, so this screen's `--signal` budget is untouched and still spent exactly once (the hero title). |
| `EpisodeView.jsx` | `/chapters/:id` | ✅ | **Rebuilt this batch. Two statements in the previous version of this row are now WRONG and are corrected here rather than quietly dropped: panels no longer cycle `full / 2⁄3-left / 2⁄3-right` widths by index, and the episode's `choices` no longer render through `DialogueChoice.jsx` at all.** This screen was the clearest instance of the live complaint that drove the whole game-surface batch — *“the text is all over the place… lack of fluency”* — and three separate things caused it. **(1) `WIDTH_CLASSES`.** Cycling every panel through full / two-thirds-left / two-thirds-right moved both the width *and* the left edge of the text on every panel down the page, so the column visibly zig-zagged and a reader's eye had no anchor line to return to. There is now ONE column: every panel shares the same left edge and the same measure (`max-w-3xl` + `px-6`), and emphasis is carried by type size and vertical rhythm instead of by position. **If you are tempted to re-introduce staggered widths “for visual interest,” that is the defect, not the interest.** **(2) The light `--paper` palette.** Everything a visitor passes through to reach this screen — the main menu, `/chapters`, `/floor`, `/play/:id` — is the dark night city, so arriving here read as the site swapping themes mid-click. That is the *same* defect that forced `ChapterSelect.jsx`'s rebuild (root `CLAUDE.md` phase 4); this screen was the last place it survived. The treatment is deliberately the one `ChapterSelect.jsx` already established rather than a third new one: `--ink` ground, `--bone` text at graded opacities, `--shadow`/`--dusk` surfaces through `Panel`'s existing `tone="night"`, hairline bone rules, Alfa Slab headings. **(3) The `DialogueChoice` block. REMOVED, not relocated** — and the reasoning is worth keeping because it is the argument against re-adding it: it was a *fake* choice (every option ran `scrollIntoView` to a panel that was already on the page and one flick of the wheel away, dressing up navigation the reader did not need as a decision they had to make); it sat between the visitor and the content, which is exactly the 90-second-rule failure spec §2 rule 1 exists to prevent, and moving it below the panels would only have left a “jump to” menu underneath the very things it jumps to; and each option renders a `bg-signal` marker bar, so a three-option block spent three accents on its own and would have blown spec 3.1's budget on a control that navigates nowhere. `DialogueChoice.jsx` itself is untouched and still available for a **real** branching moment (mechanic 4, phase 8) — this screen just stopped using it for a fake one. **No content was lost:** each episode's `choicePrompt`/`choices` data is still in `episodes.js`. **WHAT IS DELIBERATELY KEPT: `Panel.jsx` and `RevealPanel.jsx`.** `ChapterSelect.jsx` dropped both when it went full-bleed, and its own row says why that was right *there* and wrong *here*: the comic-panel primitive is the language of the CONTENT layer, and this is the content layer — these are literally the comic panels the primitive exists for. This screen also genuinely scrolls, so `RevealPanel`'s scroll-triggered reveal has something to trigger on, unlike on the non-scrolling chapter select. **NEW: the one link into the game surface.** A `▸ Play this episode` outline button (→ `/play/:id`) closes the header, rendered **only when `getGame(episode.id)` returns something**, so the screen degrades cleanly if a game is ever pulled from `games.js` rather than shipping a link to a “No such episode” card. This is the only place on the reading route that mentions the games at all — the reading route has to stay complete for someone who never plays. **Still here, unchanged:** the full-screen title card (episode number, title, subtitle) holding 1.4s, dismissible by click or any key, auto-skipped under reduced motion, wrapped in `AnimatePresence` for the ~450ms cross-fade into content (spec 3.5 “Chapter open”) — the content underneath is always already mounted, so no timer ever gates it (spec §2 rule 2); `useProgress().visitEpisode(id)` on mount with the `episodeFirstOpen` toast fired on first visit only (checked against `isVisited` *before* marking visited); and the end-of-episode card with `NEXT EPISODE` (omitted on the last episode), back to chapters and plain resume — now left-aligned on the same column edge as everything above it, since the old narrow centred box broke the single anchor line the rebuild is built around. **Masthead:** the episode's own `EpisodeArt` sits behind the header ONLY, at 18% opacity and `detail="low"` — full detail would put a Gaussian blur plus a displacement filter on a screen `PageTransition.jsx` has to zoom, and none of that fidelity survives at that opacity anyway. Two scrims, not one: the vertical one settles the masthead into the page, and the horizontal one exists because every `EpisodeArt` scene is lit from a window on one side, so without it the brightest part of the picture landed exactly under the right-hand “Plain resume” link and ate its contrast. **`min-h-screen`, not `min-h-full`** — `PageTransition.jsx` is `display: contents` at rest, so a percentage min-height has nothing to resolve against and collapses to `auto`. That bug has now been caught twice in this repo; do not “tidy” it back. **Measured live at 1280×800: exactly 3 uses of `--signal` — spec §3.1's hard limit, i.e. zero headroom — and no horizontal overflow.** (The file's own doc comment budgets *two* at rest, the episode title and the Play button; the live count came back three, so treat three as the number and do not add a fourth accent here without removing one first.) The `build` episode's `branch` object (mechanic 4) and the choice-stats screen (mechanic 6) are still **not** rendered here — phase 7/8 work, untouched by this batch. |
| `PlainResume.jsx` | `/plain` | ✅ | **Rebuilt this batch into "The Dossier."** The recruiter escape hatch (spec §2 rule 3, §7 accessibility floor) was a 197-line unstyled single-column dump — every entry the same visual weight, no index, no jump-to, no search — and the live complaint was specific: "extremely hard to navigate and find the stuff we are actually looking for... we have to scroll in all that mess." Everything below supersedes the previous row. **Surface:** this screen now carries the site's design language (`--ink`/`--paper`/the display face) on the light half of the palette, the paper counterpart to `/chapters`' night city. The previous "no design tokens at all" line above was an implementation reading, not spec text — spec §7 requires only semantic headings, no ARIA gymnastics, and being linked from every screen, none of which forbid theming. The actual PDF a recruiter saves is still genuinely plain black-on-white — see `../index.css`'s new `@media print` block, which forces that regardless of the on-screen theme. `--signal` is spent exactly twice on this screen (verified live via `getComputedStyle`): the active section-rail row's marker and the Download/print button. No `Panel.jsx`/`RevealPanel.jsx` (same call as `ChapterSelect.jsx` — the comic-panel primitive is the content/game layer's language) and no framer-motion (the only motion is a plain `scrollIntoView`, gated `'smooth'`/`'auto'` by the usual combined `useReducedMotion() \|\| !useSettings().motion` check). **Layout:** desktop is a `lg:sticky lg:top-0` left rail (identity block, a 9-row section index, Download/print + a back link to `/`) beside a right column; below `lg` it collapses to one column with the identity block first, a Download+back row second, and the section index becoming a `sticky top-0` horizontal chip bar third — all driven by responsive `order-*` classes on the *same* three elements, not duplicated per breakpoint. That non-duplication is load-bearing for the signal budget: `getComputedStyle` sees whatever is in the DOM regardless of which breakpoint's CSS currently hides it, so two separate active-marker elements (one per breakpoint) would blow the two-use budget to three or more at any single viewport width tested. A "10-second band" of four stat tiles (new `profile.highlights[]`, see `../data/CLAUDE.md`) sits above `profile.summary`, followed by a filter/expand-all/collapse-all control bar (not sticky on mobile even though the chip bar above it is — stacking two sticky headers fought for the same real estate, and the brief explicitly allows opting out rather than fighting it). **Sections, in recruiter-priority order:** 01 Experience (9), 02 Projects (3), 03 Competitions & hackathons (5), 04 Skills (6, non-collapsible definition grid), 05 Education (3), 06 Honors & awards (7, non-collapsible list), 07 Volunteering (6), 08 Other work (4), 09 Certifications (2, non-collapsible list) — 30 collapsible entries total across the six `<details>`-based sections. Five structurally different source shapes (`profile.js`'s `experience[]`/`education[]`/`volunteering[]`/`alsoBuilt[]`, `episodes.js` panels) are normalized once, at module load, into one shared entry shape (`{ id, title, subline, tags?, body, coursework?, link?, search }`) and rendered through the new `../components/DossierEntry.jsx` — see that file's row. Every entry id is derived deterministically from its own content via a local `slug()` helper, never an array index (the filter changes what's rendered, so an index-keyed id would point at the wrong row after a filter change). **Filter:** a case-insensitive substring match against every entry's precomputed `search` string (title/org/timespan/body/coursework/tags/skills items/award & cert text). Non-matching entries don't render; a section with zero matches renders nothing (heading included) and its rail row shows `0`, dimmed further; matching entries auto-expand; a "No entries match" state with a Clear-filter button covers the nothing-matches case. Clearing the filter restores the visitor's own manually-opened set untouched — `openIds` is only ever written to by explicit toggles (ignored while the filter or print forces every entry open) and by Expand all/Collapse all, never by the filter itself. **Scrollspy:** an `IntersectionObserver` (`rootMargin: '-20% 0px -70% 0px'`) over the currently-rendered `<section>` elements, re-created whenever the filtered section list changes (a filtered-out section has no node to observe). **Print:** `handleDownload` keeps `showToast('pdfDownload')` + `window.print()`, now preceded by `setPrinting(true)` so every `<details>` is already expanded before the browser takes over; `beforeprint`/`afterprint` window listeners give a bare Ctrl+P the identical treatment and revert `printing` afterward without ever touching the user's own open set. **Two data-layer bugs fixed in this batch, not styling** — see `../data/CLAUDE.md`: `episodes.js`'s `competitions` episode was tagged `plainRole: 'projects'`, mixing five hackathon panels into the "Projects" heading; it now has its own `plainRole: 'competitions'` section, and the top nav's link to `/chapters` was removed per direct instruction ("no EPISODE BS to figure out the Resume"), leaving exactly one back link. Still linked from `TitleScreen`/`MainMenu` (PLAIN RESUME item), `ChapterSelect` ("▤ Plain resume"), and `EpisodeView` (header link) — none of those call sites changed. Verified live: exactly 2 signal-colored elements at 375/768/1280px, no horizontal overflow at any of those three widths, `<main>` fills the viewport via `min-h-screen` (not `min-h-full` — same `PageTransition.jsx`-is-`display:contents`-at-rest bug `ChapterSelect.jsx` already documents), filter/expand-all/collapse-all/empty-state all confirmed via dispatched `input` events and `details[open]` counts, rail-click `scrollIntoView` confirmed reaching the target section's top under reduced motion (the smooth variant could not be observed completing — this environment's Browser pane is non-composited, `document.timeline.currentTime` pinned at 0, the same documented limitation elsewhere in this repo, and it was confirmed here to also stall `IntersectionObserver` callbacks entirely — even the mandatory initial callback never fired against a bare diagnostic observer, so the scrollspy's *mechanism* was verified by code inspection and by the shared `setActiveSectionId` path already proven correct via rail clicks, not by a live scroll-triggered callback). `npm run lint` clean (no new warnings), `npm run build` clean at 147.08 KB gzipped JS / 8.93 KB gzipped CSS. **Three defects found and fixed in Opus review after that verification pass, all confirmed live by measurement.** (1) **The mobile section index was not sticky at all** — the single most important navigation affordance on the most constrained viewport, and the whole point of the rebuild. `position: sticky` is clamped to the element's parent box, and the `<nav>` sat inside the `<aside>`, which on mobile is a short content-sized flex column measuring **336px** on a **5,479px** page. Measured: after scrolling to y=1500 the nav's `getBoundingClientRect().top` was **-1177** and `navStillVisible` was `false` — the index scrolled away permanently after ~340px and could not be reached again without scrolling back to the top. The agent's own a-j verification missed it because rail clicks were only exercised at desktop width, where the nav is `lg:static` inside an aside that sticks as one block. Fixed by giving the `<aside>` `contents lg:flex` — `display: contents` means it generates no box at all on mobile, so its children become direct flex children of `.dossier-grid` and the nav's sticky containing block becomes the full-height page container instead of a 336px stub. Measured after: `top: 0` and pinned at scroll y=2500 (375px) and y=2000 (768px). (2) **That fix exposed a second defect it would otherwise have created:** with the bar genuinely sticky, `scrollIntoView({block: 'start'})` parks a section heading at viewport top *underneath* the now-pinned 53px bar. Fixed with `scroll-mt-16 lg:scroll-mt-6` on every `<section>`; measured 11px of clearance below the bar on mobile, and all three desktop rail jumps (Volunteering/Education/Competitions) land the section at exactly **24px** (`lg:scroll-mt-6`). (3) `display: contents` also promotes the aside's `order-1`/`order-2`/`order-3` children into the same flex container as the content column, whose default `order: 0` would have painted the entire resume *above* the identity block; the content column therefore carries `order-4 lg:order-none`. Measured mobile paint order after the fix: identity (40px) -> Download/back (247px) -> nav (355px) -> content (448px). Re-verified after all three: signal count still exactly **2** at 1280px, no horizontal overflow at 375/768/1280px, `<main>` still fills the viewport, Expand all 30 / Collapse all 0, `beforeprint` 30 / `afterprint` 0, filter `graph` -> 9 of 45 with the visitor's manually-opened entry preserved across the filter cycle. One correction to the agent's report: the desktop rail jump initially measured 208px rather than 0 with motion ON — that is the frozen smooth-scroll in this non-composited pane, not a defect; with motion off the same three jumps are exact. **This batch — two live-reported fixes.** (1) **Headshot added to the identity block.** New `profile.photo` field (a bare filename in `public/`, resolved against `import.meta.env.BASE_URL` so it survives the GitHub Pages subpath); rendered as a 160px (144px at `lg:`) square with a 2px `--ink` border. **This batch — full color and larger:** the `grayscale` class and the smaller 96/80px sizing were both removed on direct feedback ("in Black and White and wayy too small"); `public/portrait.jpg` is now a real color headshot rather than the generated "AA" placeholder. `onError` flips a local `photoBroken` flag that removes the `<img>` entirely — same graceful-degradation contract as the not-yet-drawn episode art, so a missing file shows nothing rather than a broken-image glyph. Does not touch the `--signal` budget (still exactly 2, re-verified live). A generated placeholder `public/portrait.jpg` ("AA" on `--shadow`) ships so the frame isn't empty; the user replaces it with a real photo at the same path. (2) **Mobile section-index chip bar no longer reads as clipped.** It is a 9-chip horizontal scroller ~1,540px wide on a 375px screen, and it carried `no-scrollbar` — so a long chip ("03 COMPETITIONS & HACKATHONS") ran off the right edge with no affordance and looked broken. `no-scrollbar` removed here (kept on `ChapterSelect`'s filmstrip, which has steppers + arrow keys as alternatives — this bar has none), leaving a thin native scrollbar on mobile; `lg:overflow-visible` still drops the scroll container on desktop so nothing shows there. **Follow-up, same batch:** the *desktop* vertical rail had the same defect for a different reason — the `<a>` chips were `whitespace-nowrap`, so "Competitions & hackathons" in the heavy Alfa Slab display face overran the fixed 16rem rail and pushed its count off the edge. Added `lg:whitespace-normal` + `lg:items-start` on the chip and `min-w-0`/`lg:leading-[1.15]` on the label so that one row wraps to two lines inside the rail; the count span got `shrink-0` so it can never be squeezed out. Verified live at 1440px: no chip's right edge passes the rail, every count sits ~12px inside it, only row 3 grows (46px vs 34px), no page-level horizontal overflow. Verified live at 375px: `scrollbarPx` 2, no page-level horizontal overflow, `--signal` count still 2. `npm run lint` clean (pre-existing warnings in other files only), `npm run build` clean at 147.63 KB gzipped JS / 9.06 KB gzipped CSS. |
| `Floor.jsx` | `/floor` | ✅ | **New this batch — the walkable overworld behind NEW GAME** (root `CLAUDE.md` §0.1, phase 11). Mostly a host: `../game/scenes/overworld.js` draws and runs the world, and everything in this file exists to make that world reachable by people who are not looking at it. **THREE THINGS THIS OWNS THAT A CANVAS CANNOT.** **(1) The prompt mirror.** Whatever the walker is standing in front of is rendered as real DOM text in a bar under the canvas *and* pushed into `GameCanvas`'s `aria-live` region, so a screen-reader user and a sighted player are told the same thing at the same time. A canvas is completely opaque to a reader, and “the same material is also on `/chapters`” is not an answer when the door in front of you is the thing you need to know about. **(2) The control pad.** `◂` / `▸` / `Enter` are real `<button>`s that drive the game by **dispatching the same key events the engine already listens for** (`press()` fires a `keydown` on `window` and the matching `keyup` 130ms later), not by reaching into the scene. That is the whole point: there is ONE input path rather than two, whatever the keyboard can do these buttons do identically, and **a scene never needs to know a touch pad exists**. They are pointer-held rather than click-once, so holding a direction walks continuously instead of stepping per tap, and the interval is cleared on pointer-up, pointer-leave, pointer-cancel and unmount. **(3) The exits.** The Episode Explorer and the plain resume are one click away in the header at all times, *in addition to* the stairwell that exists inside the world — belt and braces on the rule that makes locked doors legal at all. **Spawn position lives in `sessionStorage` (`tt_floor_spawn`), deliberately NOT in `ProgressContext`**, because it is a per-visit convenience and not progress: coming out of a room should put you back at that door, but a fresh session should start you at the desk where the story does. It is read once on mount and immediately removed, so a later fresh arrival at `/floor` starts at the desk again; every access is `try`/`catch`ed, since private mode throws and spawning at the desk is an acceptable fallback. Door state (`unlocked`/`cleared`) is a pure function of `useProgress().cleared` and is memoised on that alone — rebuilding the door list on every render would hand `GameCanvas` a new scene factory and **restart the scene mid-walk**. Scene events map to: `enter` → record the spawn, navigate to `/play/:id`; `stairs` → `/chapters`; `locked` → a toast and **no navigation**. **Mobile fixes this batch:** the three header links were each wrapping to two lines at 375px, so the header ate several rows of a phone screen before the corridor even started — all three are now `whitespace-nowrap` with smaller type and tighter tracking below `sm`; and the scene slot went `min-h-[58vh]` → `min-h-[46vh]`, so the prompt mirror and the control pad share the screen with the corridor instead of sitting below the fold. Verified live at 375×812 with no horizontal overflow. **This batch — two changes.** **(1) The `locked` event no longer fires the wrong toast.** It called `showToast('episodeFirstOpen')`, whose pool says "THE RECRUITER WILL REMEMBER THAT." — congratulating a visitor for failing to get into a barred room. It now fires the new `doorLocked` pool (see `../data/CLAUDE.md`), which names the bar and points at the stairs. **(2) THE DESK IS INTERACTABLE, and it opens `/plain`.** It had been scenery since the screen was built — the lamp from the title-screen window, drawn and inert. It is now a real spot in `../game/scenes/overworld.js` at world x 246 (which is exactly `drawStudy`'s own `lampX`, so the interactable sits on the room's existing focal point rather than near it), it takes the same focus/prompt/`entering` path as the stairwell, and its `desk` event navigates here to the plain resume. The default spawn moved 190 → 120 in the same change: 190 was 56 units from the desk, well inside `REACH` (115), so a fresh arrival at `/floor` opened straight onto the desk prompt before the visitor had walked a step, which reads as broken. 120 is 126 units away — just outside reach, so the desk greets you almost immediately but only once you have moved toward it. The prompt mirror describes it for screen readers ("The desk — the plain resume. Every fact, on one page."). **Worth noting why the desk got this and not, say, a sixth door:** the plain resume is the one surface a recruiter actually needs, and it now has a physical place in the world rather than only a header link — which is the same argument that put the stairwell inside the corridor instead of in a caption over it. |
| `Play.jsx` | `/play/:id` | ✅ | **New this batch — the shared chrome around all five minigames.** The scene modules render a game and emit events; everything *around* the game lives here, once, so the five stay consistent and **a new game is a scene file plus a `games.js` entry and nothing more**. Do not push chrome down into a scene. **THREE STATES.** `brief` — what the game is, how it is played and what winning means, shown **before the canvas mounts**, so nobody is dropped into a mechanic they have not been told about (the goal line is deliberately explicit where it needs to be: `FORTY-EIGHT HOURS` says up front that you cannot place first). `playing` — the canvas plus a **record column** that fills with beats as they are revealed, so nothing scrolls past unread, alongside a large transient flash of the beat just earned; that reveal is the payload — if a visitor plays and learns nothing the whole concept has failed — so it is deliberately hard to miss *and* written permanently into the record. `results` — the outro, the count uncovered out of `beats.length`, and the ways onward. **The scene is built by a `useCallback` factory keyed on a `runKey` counter**, not on the raw props: `GameCanvas` rebuilds the engine whenever the `scene` factory changes identity, so a stable reference is the only thing stopping an unrelated parent re-render from restarting a run mid-play. `startRun()` bumps `runKey` and resets the run state, and that is the *only* path that creates a fresh scene. **“Read it straight” (→ `/chapters/:id`) is present in ALL THREE states**, not tucked into a menu — on the brief card, in the record column, and as the first action on the results card. The game is *a* way to read the episode and never *the* way (root `CLAUDE.md` §6 rules 1 and 3); if you ever find yourself hiding that link to tidy a layout, you are re-gating the resume. **The results card offers the next door outright after two failed runs** (`!won && attempts >= 2` → “Open the next door anyway,” which calls `clearEpisode(id)` and returns to the floor). That is the skill-check release valve: offered only once someone has actually struggled, so it never undercuts a first win, but it has to exist, because a locked door plus a game you cannot beat would put resume content behind a reflex test. On a `done` event it stores the outcome and stat, switches to `results`, and **calls `clearEpisode(id)` only when the outcome is `'win'`** — `ProgressContext`'s `cleared` set is the only thing the overworld's gating reads, and it is separate from `visited` on purpose (see `../context/CLAUDE.md`). The canvas is left mounted but `paused` under the results card, so the frozen frame stays behind it rather than a black rectangle. Both this screen and `Floor.jsx` carry the `game-surface` class on their `<main>`, which is what `../index.css`'s `html:has(.game-surface)` rule keys off to stop a `--paper` scrollbar-gutter band showing down the right edge. **This batch — rebuilt around the live report that the resume had gone missing behind the games** ("it's like we are MORE indulged into the game that the Resume is BARELY JUST NOT THERE... make it such that the resume isn't just something on the side but rather something we are continuously working upon"). **That was this file's fault, not the scenes'.** The record column used to start empty — literally "Nothing yet. Play to fill it." — so for the first thirty seconds of every game there was no resume on screen at all, and the brief card described the mechanic without ever saying what it had to do with the person. Three fixes, all here, none in a scene: **(1) `game.why`** (new required field in `../data/games.js`) states the metaphor in plain words on the brief card and again on the results card. The brief card now also leads with `episode.number`/`episode.title`/`episode.subtitle` rather than only the game's own name, and lists **every beat label** under "What you will uncover" — labels and not their text, deliberately: enough to see the shape of the episode and decide whether to play, not so much that playing is pointless. **(2) The record column is PRE-SEEDED with all of `beats[]` from before the first frame**, unreached lines dim under their own label, each filling in with its text the moment the game pays it out. It is a table of contents being completed rather than an empty box that might eventually contain something — the single highest-leverage change in this batch. **(3) The results card prints the WHOLE record, including the lines a short run never reached**, marked `· not reached` but printed in full, under a line saying so. That is not a consolation prize, it is the rule restated: the game may be lost, the resume may not be locked (root `CLAUDE.md` §6 rules 1 and 2). The card also gained a direct `/plain` link, so every one of the three states now offers both escape hatches. **A real layout bug was fixed in the same pass, reported live as "in level 3 the screen goes into scrollable at the end."** The playing layout was `min-h-screen` with the record column's `<ol>` at `flex-1 overflow-y-auto` — but a flex child will not scroll until it is allowed to be smaller than its content, so with no `min-h-0` the list simply grew, the aside grew with it, and once eight beats had landed the whole PAGE grew past the viewport and scrolled the canvas out from under the player mid-game. Episode 3 surfaced it because it reveals on a timer and always banks all eight. Fixed with `lg:h-screen lg:min-h-0 lg:overflow-hidden` on the row and `min-h-0` on the list, so the record scrolls inside its own column and the game stays exactly one viewport tall. Mobile deliberately keeps `min-h-screen` and stacks. `Play.jsx` also now passes `modules: game.modules` into `createScene`. |

## Rules for whoever builds these

- Every screen reads from `../data/episodes.js` — no hardcoded copy.
  (The site owner's name/tagline and UI chrome labels like menu items are
  treated as shell identity, not resume content, matching the existing
  precedent in `PresentsCard.jsx`.)
- Every screen must be keyboard-navigable start to finish; this is a
  requirement, not a nice-to-have (spec §2 rule 1). Verified manually via
  keyboard-only pass through all four document screens. The two game
  screens meet it differently and deliberately: `Floor.jsx`'s control pad
  and `Play.jsx`'s three states are ordinary `<button>`/`<a>` elements,
  and the canvas itself is mirrored into DOM text plus an `aria-live`
  region rather than being made "accessible" in place.
- **Resolved (Phase 6):** `useReducedMotion()` (Framer Motion's hook) has
  replaced the old inline `window.matchMedia('(prefers-reduced-motion: reduce)')`
  check in both `TitleScreen.jsx` and `EpisodeView.jsx` (the only two
  files that had it), in one sweep, combined with `useSettings().motion`
  the same way as before — either one being "reduced" disables motion.
  `ChapterSelect.jsx` and `PlainResume.jsx` never had inline motion
  checks of their own; `ChapterSelect.jsx`'s new `RevealPanel.jsx` usage
  does its own combined check internally.
- Update this table's status column and the root `CLAUDE.md` Phase 4/5/11
  rows the moment any of these six files changes meaningfully.
- `Floor.jsx` and `Play.jsx` host canvas scenes but must never hold a
  mechanic or a line of copy: mechanics live in `../game/scenes/`, copy in
  `../data/games.js`. Run `npm run verify:scenes` after any change either
  screen makes to a scene.
- **Punctuation pass (this batch):** the few hardcoded reader-facing
  strings that live in these files rather than in `../data/` —
  `Floor.jsx`'s `promptLabel`, `Play.jsx`'s two footer links,
  `PlainResume.jsx`'s filter `placeholder`, and the `spanFor`/`formatYears`
  year-range joiners in `ChapterSelect.jsx` and `EpisodeView.jsx` (now
  `${first} to ${last}`, not `${first} — ${last}`) — had their em dashes
  removed on user instruction. No logic changed; the `/present/i` span
  detector still matches the new lowercase `to present` timestamps.
