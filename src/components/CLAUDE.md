# src/components/ — reusable UI primitives

Parent: [`../CLAUDE.md`](../CLAUDE.md) → [`/CLAUDE.md`](../../CLAUDE.md).
Any change in this folder updates this file and the root repo map/phase
table in the same change.

Everything here is presentational/interaction plumbing. **No component in
this folder may hardcode resume copy** — content comes from
[`../data/episodes.js`](../data/CLAUDE.md) or `toastVariants.js`, passed
in as props.

## Built (✅)

| File | Spec ref | What it does |
|---|---|---|
| `Panel.jsx` | §3.3 | The comic-panel primitive everything else is built from: 3px ink border with a seeded irregular `clip-path` (2–6px jitter per corner, deterministic per `seed` prop so re-renders don't reshuffle the shape), hard 6px offset shadow growing to 8px on hover, halftone texture overlay, 120ms ease-out lift on hover. Restyle shared rules here, never per-instance. **This batch — a `tone` prop, `'paper'` (default, unchanged) or `'night'`.** Added as a shared rule rather than a per-instance override precisely because of the standing "restyle here, never per-instance" note above: `ChapterSelect.jsx` now sits over the night city, where an ink border on an ink-ish ground is invisible and `bg-paper` is simply the wrong surface. The `night` tone inverts the border to `--bone` at 30% and lifts the fill off `--ink` toward `--dusk` far enough that the hard ink offset shadow still reads as a shadow rather than disappearing into the background, and it swaps the halftone overlay for a new `.texture-halftone-light` (`../styles/textures.css`) since dark dots on a dark surface are pure opacity cost. Both tones’ colors come through `style`, not Tailwind arbitrary values — `color-mix()` expressions do not survive class-name round-tripping cleanly, and this component already sets `style` for its clip-path, so there is no new mechanism here. Everything else (seeded clip-path, 3px border width, 6px→8px hover shadow, lift) is identical across tones and untouched. **Follow-up, same batch: the `night` tone currently has no callers.** `ChapterSelect.jsx` was rebuilt again against the Batman reference into full-bleed cinematic art with no panel chrome at all, so it no longer uses `Panel` on either the hero or the tiles (see that screen’s row in `../screens/CLAUDE.md` for why that was a deliberate call). The tone is kept rather than reverted because it is a correct, tested surface for any future screen that sits on a dark ground, and removing it would only have to be rebuilt from scratch the next time one exists. If a year goes by with no second caller, delete it — an untested branch of a shared primitive is worse than no branch. |
| `DialogueChoice.jsx` | §5 mechanic 2 | **Also updated this batch:** roving focus (`focusIndex`, arrow keys) plays the `navigate` sound cue and activating an option plays `select`, both via `../lib/sound.js`'s `useSound()` — silently no-ops when the Settings sound toggle is off. Earlier update: the `prompt` now renders through `Typewriter.jsx` (the quote marks stay outside it — typing those out one character at a time looks like a rendering glitch rather than speech). It is also now actually *passed* one: `EpisodeView.jsx` feeds it the new `choicePrompt` field from `episodes.js`. Before, no episode supplied a prompt, so this branch never rendered and the typewriter setting had nothing to act on. Renders a prompt + stacked list of choice buttons (Telltale's four-option layout, pre-flattened for mobile). Full arrow-key navigation + Enter/Escape. `onSelect` must scroll/reveal existing content, never navigate away — choices are shortcuts, not gates. Options with `silent: true` render dimmer with a `[silence]` tag. Hover/focus state (§3.5 "Choice hover" row): text shifts right 4px (unchanged) and the `--signal` left bar now literally slides in — `scale-x-0`→`scale-x-100` with `origin-left`, 120ms — instead of the old color-only fade, still pure CSS (no Framer Motion needed for a hover pseudo-state). |
| `MemoryToast.jsx` | §5 mechanic 1 | Visual-only: renders one toast message. Not used standalone — rendered by `ToastProvider`. **Rebuilt a second time this batch on a live user screenshot.** The prior version (flat translucent `bg-ink/85` bar, two thin `bg-bone/45` rules, centered letter-spaced caps) was already a user-directed departure from spec §5's Panel-chrome description, built from a verbal description of the Telltale HUD caption — but when the user then supplied an actual screenshot of the real in-game notification, they called it out as still wrong. That photo is now the reference: a **solid opaque `--ink` banner**, a **warning-triangle icon** (inline SVG, `--bone`, `currentColor`), **left-aligned bold sentence-case `--bone` text** (not centered caps), and — the distinctive part — a **hand-torn right edge** instead of a clean rectangle (left/top/bottom stay flush). The torn edge is a static `clip-path: polygon(...)` constant (`TORN_EDGE_CLIP`) plus a `drop-shadow` filter so the shadow follows the jagged silhouette instead of fighting it with a rectangular box-shadow — fixed, not seeded/randomized like `Panel.jsx`'s per-instance jitter, since there's only ever one toast on screen at a time. Position and motion direction live in `ToastProvider.jsx`, one level up. **Enlarged this batch** on live feedback that the toast read as smaller than the corner it sat in: `max-w-sm`/`sm:max-w-md` → `max-w-md`/`sm:max-w-lg`, more padding (`py-3 pl-4 pr-12` → `py-4 pl-5 pr-14`), bigger icon (22px → 28px) and text (`text-sm`/`md:text-base` → `text-base`/`md:text-lg`). **Rebuilt a third time this batch on three more live-reported glitches, all in the same `TORN_EDGE_CLIP` polygon/padding pairing:** (1) "text renders outside the box" — the old polygon's notches cut up to 22% of the box's own width inward, deeper than the `pr-14`-only padding accounted for, so the ink background had a bite missing exactly where text sat at some vertical positions (text itself was never outside the box, it just had no opaque fill behind it there). Fixed by capping notch depth at 6% on each side (both `px-10`, symmetric now) so every notch has a real margin regardless of how many lines the message wraps to. (2) the torn edge existed only on the right — mirrored onto the left, so both vertical sides read as torn "claw" teeth, verified as exact mirror-image depth patterns. (3) the top edge was still a dead-flush straight line between the two top corners, reading as a computer-drawn rectangle capping two hand-torn sides rather than one consistently rough banner. The top now gets the same flush/bite/flush language as the sides, but in fixed pixels (6–9px) rather than percent-of-box — the sides use percent because height (their reference axis) already varies a lot with message length, so a proportional bite keeps looking right at any height, but percent-of-width on the top would make the tear shallower on a one-line toast than a two-line one purely from wrapping, which isn't about the tear at all. A `background: linear-gradient(--shadow → --ink over 16px)` (both from `tokens.css`, no new hex) now rides under the jagged top clip too, since a flat fill under a jagged cutline still reads as a clean color block with a fancy edge — a paper tear also varies tonally right at the line, which the gradient approximates cheaply. Verified live: notch/text margins measured via `getBoundingClientRect()` at both the narrow (~284px, one-line) and wide (`sm:max-w-lg`, 512px, two-line) box sizes, comfortably clear of the 6%/9px caps in both. `npm run lint` and `npm run build` both still clean, bundle size unchanged. **Rebuilt a fourth time this batch** on live feedback ("soo poorly designed") that was correct on inspection: that `TORN_EDGE_CLIP` cut 6–9% of the box's own width/height on every edge, repeated roughly every 8% of the perimeter — a tight, evenly-spaced sawtooth that reads as a flame/thorn outline at this component's actual size, not a torn-paper edge, and it visually contradicted `Panel.jsx`'s own restraint (2–6px jitter, four points total) that every other irregular shape in the app follows. `TORN_EDGE_CLIP` now cuts **one shallow 4–6px bite per edge** (fixed px depth, matching Panel.jsx's scale; only the bite's *position* along the edge is percent-based, so it stays proportionate as the box resizes) instead of a repeating pattern. Layout also tightened: the bare outline triangle floating on flat `--ink` read as a generic browser-alert glyph, so the icon now sits on a small `h-10 w-10` `--shadow` badge, and the card gained a `boxShadow: inset 0 0 0 1px rgba(239,231,216,0.12)` hairline so the jagged silhouette stays legible against a dark backdrop rather than only against `--paper`. Verified via the running dev server (`document.querySelector('[role="status"]')`'s computed `clipPath` and `getBoundingClientRect()` after programmatically firing the `lateNightWindow` easter egg) that the new geometry renders as intended; a side-by-side before/after mockup was also built and shared with the user since the Browser-pane preview in this session would not composite screenshots. **Rebuilt a fifth time this batch — the whole premise changed, not just the numbers.** Direct feedback: a hard silhouette (any clip-path, restrained or not) was never what was wanted. The actual ask is a *faded* border — the ink color itself dissolving into whatever's behind it, evoking a decayed/barren environment bleeding at its edges, not a shape cut out of one. `clip-path` can only produce a crisp edge, so it's gone entirely. Replaced with an SVG `<mask>`: a `radialGradient` (`gradientUnits="objectBoundingBox"`, so its ellipse automatically matches this box's own aspect ratio) run through the same `feTurbulence`+`feDisplacementMap` pairing `TitleReveal.jsx` already uses for its distressed lettering, at a similar baseFrequency register, so the fade rim comes out ragged/eroded rather than a clean vignette. Applied only to the ink background layer (a separate absolutely-positioned div) so text/icon stay fully opaque and legible; `filter: drop-shadow` on the outer wrapper now reads that same fading alpha, so the shadow softens at the edges instead of tracing a hard rectangle. **One real bug hit and fixed during this rebuild:** the gradient's `objectBoundingBox` frame must be relative to the box's *true* dimensions, not the overscanned region the filter needs for displacement headroom — the first pass sized the filtered `<rect>` to the same `-30%/-70%/160%/240%` overscan as the `<filter>`'s own region, which made the gradient's 0–100% span (and thus its solid-to-transparent falloff) fall almost entirely outside the visible toast, so nothing visibly faded at all. Fixed by keeping the filtered rect at the real `0,0,100%,100%` and giving only the `<filter>` element's own region the overscan — the two are separate coordinate frames, easy to conflate. Verified this time by extracting the live mask's actual `<defs>` out of the DOM (via `XMLSerializer`, taking care to select the toast's own mask specifically — `document.querySelector('svg defs')` first grabbed a filter belonging to `TitleBackdrop.jsx`'s window-glow instead, since several unrelated `<svg>`s share the page), re-rendering it into a self-contained SVG data URI, rasterizing to a canvas, and sampling pixel alpha along the midline and both edges: solid 255 across the whole center, fading to the 160s at the left/right edges and jumping non-monotonically (0→17→55→77→84→67→103…) near the top edge — confirming both the fade and the intended raggedness are actually rendering, not just present in markup. |
| `ToastProvider.jsx` | §5 mechanic 1, §3.5 "Toast appear" | Global toast queue + context (`useToast().showToast(trigger)`). **Now also plays the `toast` sound cue** (`../lib/sound.js`'s `useSound()`) inside `showToast`, one call covering every trigger in the app — `episodeFirstOpen`, `panelExpand`, `choiceMade`, `pdfDownload`, and the title-screen easter egg — rather than a `playSound` call at each site that fires one. Enforces: never show two at once, queue cap 3 (oldest dropped), session hard cap 8 (silently stops after — **except `lateNightWindow`, exempted this batch: live feedback reported the title-screen easter egg "running out of clicks" once the shared session counter hit 8 from ordinary toasts, but unlike those one-shot narrative beats the easter egg is a discoverable secret meant to be re-clicked indefinitely, so `showToast` skips the cap check for that trigger alone while every other trigger still respects it; the exempt calls still increment the shared counter, used only for variant-cycling and React keys**), 3.2s hold before fade. A rapid burst of triggers (e.g. repeatedly clicking the easter-egg window) enqueues correctly but only becomes visible one at a time on the existing pacing — that's this queue behaving as designed for every toast type, not something worth special-casing for one trigger. `aria-live="polite"`. Picks copy via `pickToastVariant` from `../data/toastVariants.js`. **Phase 6:** the toast is wrapped in `AnimatePresence` + `motion.div` — fades/slides in, holds, then fades/slides out, per spec 3.5. Reads `useReducedMotion()` + `useSettings().motion`; under reduced motion it renders the same tree with no `initial`/`y` animation, so it just appears/disappears with the queue timing, never mid-slide. **Repositioned this batch:** moved from `fixed bottom-4 left-4` to `fixed left-0 top-0` (flush both edges) to match the reference screenshot's top-left anchor, and the slide direction changed from `x: -24` (in from the side) to `y: -16` (drops in from above) to match. **Repositioned again this batch:** flush against the corner read as cramped, per live feedback, so the wrapper moved off the edge — `left-0 top-0` → `left-4 top-4` (`left-8 top-6` at `sm:`) — and its `max-w-sm sm:max-w-md` grew to `max-w-md sm:max-w-lg` to match `MemoryToast.jsx`'s own enlargement (see that row). Slide direction/motion unchanged. |
| `StartPrompt.jsx` | new — not in spec §4.1 | Beat 0, ahead of `PresentsCard`: a full-bleed `--ink` "click to start" gate, same full-bleed-`<button>` pattern as `PresentsCard.jsx`/`TitleReveal.jsx`. Exists to fix a hard platform constraint a live user report kept hitting ("I can't hear any sound... UNTIL I click on the website which is poor"): no browser plays audio before a real user gesture, full stop, and every earlier attempt (retiming `sounds.intro`, deferring to an incidental click) only ever chased that constraint rather than removing it. This screen asks for the click up front instead, in-universe, old-school-arcade styled — see its own doc comment for the full rationale, including why this is a deliberate, narrow, user-requested exception to spec §2 rule 1 ("no forced interaction"): one unavoidable click before anything renders, justified because it serves a hard technical requirement rather than gating content, and it doesn't touch `/plain` (a separate route, reachable directly) at all. **Rebuilt this batch** on direct live feedback that the screen was "waaay too small" — the old version was two skinny centered lines floating alone in an empty `--ink` void with no hook. It now leads with an identity block above the CTA: an "An Interactive Resume" eyebrow tag (shell chrome, same precedent as `PresentsCard.jsx`'s "presents" framing — hardcoded here, not resume copy), the name and headline read from `../data/profile.js` (the same source `MainMenu.jsx`'s own identity block draws from, so the two can't drift), and a `--signal` divider rule, so a recruiter sees whose site this is before being asked to click anything. **A real bug surfaced and was fixed during verification, not just a size bump:** this codebase's `tokens.css` overrides Tailwind's `text-xs`...`text-3xl` scale with its own values (`text-3xl` = 64px, not Tailwind's default 30px — see `../styles/CLAUDE.md`), and the CTA's first draft used `text-3xl sm:text-4xl`, which put a 64px, non-wrapping line (the flex column's `items-center` never constrains child width) onto a 375px mobile viewport — verified via `getBoundingClientRect()` to render 187px past the right edge with nothing to scroll to see it. Fixed by measuring actual rendered text widths at each breakpoint (off-screen canvas-free DOM probe, not guessed) and landing on `text-lg sm:text-xl md:text-2xl xl:text-3xl` (20/28/40/64px) with a `max-w-[260px]` mobile cap so the line wraps safely if a future copy change makes it too wide, rather than depending on a pixel-exact fit. Verified live at 375px and 1280px viewports: every line's `getBoundingClientRect()` sits fully inside its viewport at both sizes, no horizontal overflow. The blink (still `prompt-blink` from `../styles/motion.css`, a hard on/off `steps(1)` cut) is now scoped to just the CTA line, not the whole block. **Deliberately does NOT auto-skip under reduced motion** unlike `PresentsCard.jsx`/`TitleReveal.jsx` — reduced motion turns off the blink, not the gate itself, since skipping it would silently reintroduce the exact bug it exists to fix for the visitors who have that setting on. Gesture-unlock chain (verified in an earlier batch, unaffected by this one): `navigator.userActivation.hasBeenActive` reads `false` while this screen shows, and a keypress on it flips that to `true`, which is what lets `sounds.intro` actually play once beat 2 is reached. |
| `PresentsCard.jsx` | §4.1 Beat 1 | Full-black "ABDULLAH AAMIR PRESENTS" card. Holds 1.2s, fades 400ms, skippable by click/tap/key at any time via `onDone`. If `reducedMotion` prop is true, calls `onDone` immediately with no visible hold. |
| `TitleReveal.jsx` | §4.1 Beat 2 | Full-bleed backdrop with the name resolving on top using the comic-cover lettering treatment (§3.4: heavy uppercase display face, `--bone`/off-white only — never `--signal` — dark gradient under the text for contrast, no card/box). No painted art exists yet (`../assets/title-art/` is still empty), so the backdrop is `TitleBackdrop.jsx` (see below); a `TITLE_ART_URL` constant at the top of the file is the one line to change once real art lands. Holds ~1.8s then calls `onDone`. `reducedMotion` prop skips straight to done. **Fixed this batch:** the distressed/torn-edge letterform filter and ink-splatter accent from §3.4, previously deferred, are now implemented — an SVG `feTurbulence`+`feDisplacementMap` filter applied to the name span via `filter: url(#title-distressed-edge) drop-shadow(...)`, plus a small ink-splatter SVG (a few dots + a drip) bleeding from one corner of the name only, per spec's "restrained to a corner or an edge, not smeared across the whole word." **Skew bug fixed:** the filter first shipped as `baseFrequency="0.01 0.09"` with `scale="5"`. The `0.01` horizontal term is a very low frequency — one long smooth wave across the entire word rather than fine noise — so the displacement bent the whole name and read as the title being *skewed/slanted*, not distressed (user-reported from a live screenshot). Now `baseFrequency="0.055"` (high and roughly isotropic) with `numOctaves="3"` and `scale="1.8"`: it chews the outline only and leaves the baseline visually straight. If anyone retunes this, keep the frequency high — low frequencies warp glyphs, they don't roughen them. |
| `MainMenu.jsx` | §4.1 Beat 3 | **This batch — three direct live-feedback fixes.** (1) The EXTRAS item is now labeled **PLAIN RESUME** — "EXTRAS" as a word didn't tell a recruiter what it was, which is why the menu carried an explanatory second line underneath it ("Extras — plain-text resume & PDF, no game required"); once the label itself says what it is, that line is redundant and has been removed, leaving only the keyboard hint (`↑ ↓ to navigate · Enter to select`). The item's `key` (`'extras'`) and its `/plain` destination are unchanged — only the visible label and the leftover line moved. (2) The three OPTIONS toggle buttons (Motion/Sound/Typewriter) switched from `font-body` (Inter) to `font-display` (Alfa Slab One) — reported live as making the screen "look out of touch," which tracks: they were the one plain-grotesque, small-caps UI row sitting directly under five bold slab-serif menu items in the same nav. They read as menu items in the same interaction model (arrow-key-adjacent, same screen, same font as `NEW GAME`/`EPISODES`/etc.), not as resume body prose, so `tokens.css`'s "Alfa Slab One is titles-only, never body copy" restriction doesn't cover them — see the inline comment above the options `<div>`. `tracking-wide` dropped to `tracking-normal` on these three buttons since a heavy slab face gets worse, not better, with extra letter-spacing at `text-sm`. (3) **Not this file, but caused by it going out with a related fix:** the `lateNightWindow` easter-egg toast (see `TitleBackdrop.jsx`'s row below) is now exempt from `ToastProvider.jsx`'s 8-toasts-per-session cap — see that file's row for why. Plain vertical text list over the *same* backdrop as `TitleReveal.jsx` — now literally the same component, `TitleBackdrop.jsx` (see below), not just visually matching Tailwind classes copy-pasted between the two files (no screen cut — matches spec's "this continuity is the actual Walking Dead structure" note). Items: NEW GAME, CONTINUE (omitted entirely, not disabled, unless `useProgress().hasSave`), EPISODES, EXTRAS, OPTIONS. Up/down arrow + Enter keyboard nav via focus-based roving selection (same pattern as `DialogueChoice.jsx`). Selection state = right-shift + a single `▸` in `--signal` + brighter text, no box/fill. **Added this batch:** a left-edge scrim (`inset-y-0 left-0 w-2/3`, `--ink` 80%→transparent) alongside the existing bottom gradient — now that `TitleBackdrop.jsx`'s skyline rises to ~70% of the frame, the menu items sit over rooftops and lit windows instead of empty sky, and needed their own contrast floor. Kept to the left third so it darkens the text column without flattening the scene the menu is meant to sit inside. **Also added this batch:** an identity block above the items (name + headline, read from `../data/profile.js` rather than retyped, so those strings keep one source of truth — see `../data/CLAUDE.md`) and an orientation line below them (`↑ ↓ to navigate · Enter to select` / `Extras — plain-text resume & PDF, no game required`). The menu was previously five bare verbs over a picture, which told a recruiter landing there neither whose resume this is nor where the real information lives; naming the escape hatch explicitly is worth more than the small loss of in-universe purity, given spec's overriding "information in under 90 seconds without playing along" constraint. **Critical fix (earlier this batch):** the `<nav>` is `h-full`, so it covered the ENTIRE viewport and sat above `TitleBackdrop.jsx` in paint order, swallowing every click aimed at the scene — which is why the backdrop's easter-egg window could never be hit in a real browser. (The embers kept working and masked the problem, because those run off a window-level listener that doesn't care what's on top; an earlier verification pass also missed it by dispatching events straight onto the element, bypassing hit-testing entirely. Verify clickability with `document.elementFromPoint`, not synthetic dispatch.) The nav is now `pointer-events-none` with `pointer-events-auto` restored on the `<ul>` and the options group, so empty space lets clicks through. **Sound wired in:** roving focus (arrow keys) plays `navigate`, activating a menu item plays `select`, both via `../lib/sound.js`'s `useSound()`. **Mouse hover now plays `navigate` too** — the original build had hover call `setSelected` directly, bypassing `focusIndex`/the sound cue on purpose (keyboard-only tick); the user asked for hover to make a sound as well when it changes the selection, so hover now routes through a small `hoverIndex(index)` helper that plays `navigate` only when hovering an item that isn't already selected (guards against a keyboard move landing under a stationary cursor double-firing the cue). The OPTIONS SOUND toggle is back (see below) — it was removed in an earlier pass when there was nothing for it to gate. EXTRAS routes straight to `/plain` (the dressed-up escape hatch). OPTIONS expands inline `useSettings()` toggles (motion/sound/typewriter) rather than a separate `SettingsTray.jsx` screen — that component is still not started, see below. Version tag (`v1.0`) bottom-right. **Fixed this batch:** the menu used to imperatively call `itemRefs.current[0]?.focus()` on mount to seed keyboard roving-focus on NEW GAME. Browsers' `:focus-visible` heuristic treats a page's very first focus (even script-driven) as keyboard-worthy, so this put the `--signal` 2px focus ring around NEW GAME on every load — mouse users included, and inconsistently across browsers, which is why it was reported live as "sometimes" a big red box rather than every time. That `useEffect` is removed; nothing is focused imperatively on mount now. Keyboard reachability is unaffected — Tab moves native focus to NEW GAME via normal tab order (which *does* legitimately trigger `:focus-visible`), and arrow keys take over from there through the existing `handleKeyDown`. The `▸` selection highlight on NEW GAME is still shown by default (it's driven by the `selected` React state, not actual DOM focus), so the menu still visually indicates a default item — it just no longer forces a visible focus ring onto every visitor. The OPTIONS group has a fourth toggle, `Music: On/Off`, between Sound and Typewriter — same `font-display`/`tracking-normal` styling as the other three, no unconditional `sounds.select()` call on click (matches Motion/Typewriter, not Sound, since there's nothing to demonstrate synchronously). **This batch — a `◂ Previous Track` / `Next Track ▸` control pair added directly below the Music toggle**, direct user request ("Add Option so that we can change music from the OPTIONS... going next music and previous music"): the existing "Now Playing" badge could only skip forward, and only from wherever it happened to be rendered, not from OPTIONS. Both buttons call `useMusic()`'s `previous()`/`next()` (new this batch — see `../context/CLAUDE.md`'s `MusicContext.jsx` row for the underlying `{history, pointer}` logic), the same context `BackgroundMusic.jsx` reads, so a click here changes the exact track the badge shows and hears. The pair only renders when `useMusic().hasMultipleTracks` is true (mirrors the badge's own `disabled={!isPlaylist}` graceful-degradation — no dead controls when the playlist has 0 or 1 tracks), and Previous Track individually disables via `hasPrevious` when there's no earlier track in history to go back to, rather than wrapping around or silently no-oping with no visible feedback. **No longer mounts `BackgroundMusic.jsx`** — it used to, as a sibling of `TitleBackdrop.jsx`, but that meant the radio stopped on every navigation away from this screen, including into `/plain`. Per direct user follow-up (keep the radio playing across menu-ish navigation, only stop specifically on entering `/chapters*`), `BackgroundMusic.jsx` moved to being mounted once in `App.jsx` instead — see that file's row above and `../CLAUDE.md`'s App.jsx note. This screen no longer has any special relationship to it beyond the Music toggle above, which still lives here since it's a Settings control, not a mount point. **This batch — direct live-reported bug fixed: arrow-key navigation didn't reach into OPTIONS.** Up/Down previously only ever cycled `selected` through the 5 top-level `items` (NEW GAME/CONTINUE/EPISODES/PLAIN RESUME/OPTIONS) regardless of whether the OPTIONS panel was expanded, so the Motion/Sound/Music/Track/Typewriter toggles had no index in that roving-focus scheme at all and arrow keys simply kept bouncing around the same 5 items instead of "expanding into" the open panel. Fixed by introducing `optionControls` (the toggle rows, defined the same way as `items`) and `navItems = optionsOpen ? [...items, ...optionControls] : items` — `selected`/`focusIndex`/the window keydown handler now all operate on `navItems`, so opening OPTIONS while it's selected (always the last top-level item) and pressing Down walks into Motion → Sound → Music → (Previous/Next Track, when the playlist has more than one entry) → Typewriter, then wraps back around to NEW GAME — one continuous loop rather than two disconnected ones. The options `<div>` is now rendered by mapping over `optionControls` (each row gets a combined index, the same `translate-x-2` + `▸` selection styling as the top items, and `onFocus`/`onMouseEnter` wired to that index) instead of five hand-written buttons with no index at all — this also means Previous/Next Track are now two independently-selectable stacked rows rather than one side-by-side pair, a deliberate layout trade-off for giving every control its own arrow-key stop. Enter activation is index-aware about the `select` sound cue: top-level items still get an unconditional `playSound('select')` before their action (unchanged), while an option toggle's own `action` carries whatever sound it needs (e.g. Sound's still plays `sounds.select()` unconditionally; Motion/Music/Typewriter/Track actions play nothing extra) so Enter and a mouse click on the same control sound identical, matching the pre-existing per-control behavior instead of adding a new blanket cue. A new effect snaps `selected` back onto the OPTIONS item itself if the panel closes (click or Enter) while selection was sitting on one of the toggles, so there's never a stale out-of-range index with no visible marker. Verified live via `javascript_tool`-dispatched `keydown` events (the sandboxed Browser pane's compositor is unavailable for screenshots in this environment, same known limitation noted elsewhere in this file): stepping Down from OPTIONS walked Motion → Sound → Music → ◂ Previous Track → Next Track ▸ → Typewriter → wrapped to NEW GAME → CONTINUE; stepping back Up wrapped correctly the other way; closing OPTIONS while a toggle was selected left selection cleanly on OPTIONS with no dangling state. `npm run lint`/`npm run build` both clean. **This batch — the window `keydown` handler gained an `Escape` branch:** when the OPTIONS panel is open, ESC closes it (`setOptionsOpen(false)`) and `preventDefault`s, so ESC backs out of the sub-panel before the new global `EscapeBack.jsx` handler (which no-ops on `/`) is reached. `optionsOpen` added to that effect's dep array. Verified live: OPTIONS open + ESC closes the panel, stays on `/`. **This batch — the menu now points at two different surfaces, and the labels say so** (root `CLAUDE.md` §0.1, phase 11). **NEW GAME navigates to `/floor`** — the walkable overworld with a door per episode — **where it previously went to `/chapters`**; and the **EPISODES item is relabelled EPISODE EXPLORER**, still going to `/chapters`. That is the whole change to this file, but the reason matters more than the diff: the two items are now genuinely different destinations rather than two labels for one screen — **one for playing, one for skimming.** That split is exactly what lets the floor have real locked doors without gating any content: everything behind a locked door on `/floor` is readable immediately and in any order through the Explorer, which is never gated, and `PLAIN RESUME` (unchanged) is a third ungated way in. **If NEW GAME and EPISODE EXPLORER are ever pointed at the same route again, the locks stop being legal under spec §2 rule 1** — collapse the two items and you have to delete the gating with them. CONTINUE is unchanged and still resumes at `/chapters/${lastEpisodeId}`, i.e. on the reading surface, since `lastEpisodeId` is set by reading an episode (`visited`), not by clearing a door (`cleared`) — see `../context/CLAUDE.md` on why those two are separate sets. The roving-focus list, the OPTIONS panel and the ESC behaviour above are untouched; the item count did not change, so nothing about the keyboard walk-through moved. |
| `TitleBackdrop.jsx` | §4.1 beats 2-3 backdrop | Shared full-bleed "environment scene" placeholder mounted by both `TitleReveal.jsx` and `MainMenu.jsx`. Replaces the original flat `--dusk`→`--shadow` gradient, which spec 4.1 explicitly rules out ("a single illustrated environment scene... not a photo, not a gradient"). **Rebuilt a second time this batch** after the user screenshotted the first rebuild: the skyline filled only ~20% of the frame (leaving a huge empty gradient above it), the "lit windows" were flat pale rects with no halo so they read as gray squares rather than light, and the buildings were plain merged blocks. Now composed as **four parallax depth planes** — hills → far city → mid city → near city — each a lighter-to-darker step toward camera with a distance-haze band between far and mid, which is what actually sells depth (a single silhouette strip cannot). Skyline peaks at **~69% of viewport height** (verified arithmetically against the layer geometry, not eyeballed), per the user's requested 70/30 ratio. Buildings are hand-placed with mismatched widths/heights, deliberate overlap/occlusion, and mixed detail (setbacks, rooftop water tanks, antennas with crossbars, peaked roofs) — evenly-spaced same-shape blocks are exactly what read as "generated" before. Windows are a real per-building grid (`windowsFor`, deterministic via a `seeded` helper in the same spirit as `Panel.jsx`'s `seededJitter` — never `Math.random` in render): a minority lit, a band of "visible but dark" panes for facade texture, and each lit pane drawn **twice** — a blurred `feGaussianBlur` halo plus a crisp core — because a pale rect without a halo does not read as a light source. Each layer's viewBox aspect is matched to the aspect its container actually gets at desktop size, so `preserveAspectRatio="none"` costs ~no distortion there while degrading gracefully (taller, narrower skyline) on mobile. Interactive touches: **mouse-parallax** across all four planes, **click-spawned embers**, and **the easter egg** — one near-layer building (flagged `solo: true` in the `LAYERS` table) renders with every window dark except a single larger, steadily-breathing lit pane; clicking it reveals a head-and-shoulders silhouette at a desk with a monitor and fires a `lateNightWindow` memory toast via `useToast()` (copy lives in `../data/toastVariants.js`, never inline here). **Fixed this batch:** the toast used to be gated behind the same `soloFound` flag as the visual reveal, so re-clicking after the first time did nothing — reported as "the easter egg doesn't work." Only the visual reveal is one-time now; the toast fires on every click (it enqueues into `ToastProvider`'s existing queue/pacing like any other toast, so rapid repeat clicks don't all appear instantly — see `ToastProvider.jsx`'s note on this). **Three things were tried and cut here — do not re-add them assuming they were an oversight:** (1) a figure that walked edge-to-edge on a timer (read as an amateur animated GIF pasted over the art); (2) its replacement, a lone figure standing on a near rooftop — better composed, but at the scale the near layer actually renders it was a ~30px smudge nobody could find, so its interactivity may as well not have existed; (3) a "click any lit window 3× → shooting star" easter egg, replaced because one findable, meaningful secret beats a hidden combo nobody discovers. The solo window telegraphs itself (it is the only light on an otherwise black building) without needing hint text, and is the reason the other lit windows are no longer clickable at all. **Pointer tracking and the ember click listener are on `window`, not on this component's own div, and that is load-bearing:** `MainMenu.jsx` stacks a full-height `<nav>` over this backdrop as a *sibling*, so element-level handlers never see events once the menu is up — parallax worked on beat 2 and silently died on beat 3, which is exactly the bug this fixes. The ember listener excludes `button, a, [role="button"]` so picking a menu item isn't also a firework. Keyframes live in `../styles/motion.css`. Motion gating: embers, fog drift, window flicker and the solo pane's breath all check `reducedMotion`; the **easter egg itself still works** under reduced motion (it is content, not decoration) and simply reveals instantly. `interactive` (default false) gates both click-embers *and* the easter egg to beat 3 — during beat 2 every click skips the beat, so a click landing on the window would fire the reveal and its toast onto a screen the visitor has already left. Nothing calls `stopPropagation`, so beat 2's click-to-skip is never blocked. Verified live this batch: parallax responds over the menu region with the near layer travelling ~2.8× the far (correct depth ordering), the solo window reveals + toasts once and ignores repeat clicks, menu-button clicks spawn 0 embers while sky clicks spawn 4, and no trace of the removed figure/star remains in the DOM. Still a placeholder, not real art: when a commissioned/painted scene lands in `../assets/title-art/`, retire this component (do not layer it under the photo) and point `TITLE_ART_URL` in the two callers at the asset. **This batch — a `variant` prop, `'title'` (default, unchanged) or `'distant'`, added so `../screens/ChapterSelect.jsx` can sit in this same city instead of cutting to an unrelated screen.** `distant` differs three ways and each is load-bearing: (1) the NEAR layer is not rendered — that is the composition (on `/chapters` you are looking at the skyline from further back, and the screen’s own content occupies the foreground the near layer would fight), and it also drops roughly two thirds of this component’s ~1,450 `<rect>`s, which matters because `PageTransition.jsx`’s dolly has to zoom whichever screen is incoming and that cost was already reported as lag once. Dropping the near layer also drops the easter-egg window, which is correct — that secret belongs to the title screen, and duplicating it on another route would cheapen it. (2) No pointer parallax at all, so no `window`-level `pointermove` listener is even attached; a backdrop leaning around behind a screen someone is trying to read is distraction, not depth. (3) A heavier scrim, since real content sits over this rather than a title card with its own. Verified live on `/chapters`: 9 `<svg>` elements under `<main>` — hills + far + mid for the backdrop, plus the hero window and five tiles — i.e. the near layer is genuinely absent, not merely hidden. **Follow-up, same batch: the `distant` variant no longer has a caller either.** `ChapterSelect.jsx`’s Batman-reference rebuild made the episode key art full-bleed, so there is no backdrop behind it to render. The continuity that variant existed to provide did not disappear — it moved inside `EpisodeArt.jsx`, which draws the city skyline out through the window in every scene, so `/chapters` is still literally inside the same city, just from indoors now. Same reasoning as `Panel.jsx`’s `night` tone above: the variant is kept because it is correct and tested and the next dark screen will want it, but if nothing claims it, delete it rather than leave an untested branch in a shared component. |
| `EpisodeArt.jsx` | not in spec §7’s list | **New this batch, replacing `EpisodeWindow.jsx`, which was deleted rather than kept alongside it.** Episode key art: one large backlit silhouette against a bright window, drawn per episode and selected by the `scene` field in `../data/episodes.js` (content, not styling — the drawing is a rendering of what the episode contains). **Why the rewrite.** `EpisodeWindow.jsx` drew a small room full of props — desks, tool pegboards, paper stacks, coffee cups, several small figures — and the user reported it live as "the SVG arts do look bad, they aren’t that well done." The diagnosis held up: that much incident needs ~500px to resolve and this art renders down to ~176px in a filmstrip tile, where it turned to mush. The user then supplied a screenshot of the Batman: A Telltale Series episode menu, whose key art is the opposite approach — ONE subject, very large, in full silhouette, backlit by a single big light source, with heavy haze and a strong vignette. Two big shapes and a glow survive being shrunk; a room full of 14px furniture does not. Every scene now follows one template: a large bright window upper-centre-right as the only real light, one figure in front of it at ~50–60% of frame height in solid `--ink`, a dark foreground band along the bottom for depth, then haze, god rays and a vignette pressing in on all four edges. Figures are back-view silhouettes in three poses (`stand`/`sit`/`hunch`) drawn in a local 200×420 box and scaled by the caller, so a scene positions one by height rather than by editing path coordinates. **Continuity moved inside the window.** `/chapters` used to render `TitleBackdrop.jsx`’s skyline behind it so the screen stayed in the same night city as the title screen; this art is full-bleed, so that backdrop is gone from that route and the link had to live somewhere else. Every scene now draws the city out through the glass, lit panes and all — you are inside one of the title screen’s lit windows, looking out, which is a more literal version of the same idea than a backdrop was. **Palette is deliberately NOT the reference’s cool cyan grade** — light is `--bone` into `--paper`, darks are `--ink` and `--dusk`, exactly as on the title screen, because the reference’s color would have been the theme jump this screen was rebuilt to remove. No `--signal` in the file at all. **`detail` is a performance rule, not a fidelity one:** `'full'` (hero) draws god rays, a blurred bloom halo and haze; `'low'` (tiles) draws the identical composition and silhouettes with no `feGaussianBlur` whatsoever, because blur is invisible at 176px and five tiles plus a hero at full detail meant ~18 live blur filters on a screen `PageTransition.jsx` has to zoom — exactly the shape of the lag reported against the route dolly earlier. Verified live: one blur filter on the entire screen, zero inside any tile, 341 SVG nodes across all six scene instances. `lit` (the visited state) is the same drawing with the light turned down rather than a different one, and composes with the filmstrip’s own CSS dimming of unselected tiles. Like `TitleBackdrop.jsx` this is a code-only stand-in — if real painted covers ever land in `../assets/covers/`, retire it rather than layering it under them. **Rebuilt again this batch** on the report that the art "isn't meeting the style requirements." The staging above (one window, one large backlit subject, dark foreground band, haze, vignette) is kept — a live screenshot showed the problem was never the staging, and named five concrete defects instead. **(1) All five scenes rendered identically.** Each scene's distinguishing props — the lecture board, the podium, the crowd, the pegboard — were filled `var(--ink)` and drawn against an `--ink` background, so they were invisible; only episode 2's monitor and episode 4's clock ever read, because those two were the only props filled with the LIGHT gradient. The art was not under-drawn, it was drawn in a color that could not be seen. Fixed with a **tonal ladder** (`T.wall`/`T.far`/`T.mid`/`T.near`): scene parts are assigned to depth groups with genuinely different values and only the plane nearest camera is pure ink. **(2) The figure was a snowman** — ellipse head on a symmetric trapezoid, no neck, no shoulder slope, no arms. Replaced with hand-authored paths carrying a neck, sloping shoulders, hanging arms and deliberate left/right asymmetry, plus a fourth pose (`gesture`, one arm raised) so the lecturer and the speaker are not the same person standing the same way. **(3) No rim light** — a 100%-ink silhouette against a glow reads as a hole punched in the frame. New `Rim` helper draws each silhouette twice: a `--bone` copy offset a few units toward the window, then the ink copy on top; the sliver of bone surviving along the light-facing contour IS the rim. Chosen over a blur-based glow specifically because it is a plain fill, so it costs nothing and survives being scaled to a 176px thumbnail. Its sign is computed per scene from which side of the subject the window is actually on, so it stays correct when a scene lights from the other side. **(4) No texture — the actual "style" miss.** Every other surface in this app is deliberately printed and distressed (`Panel.jsx`'s torn clip-path, `TitleReveal.jsx`'s `feTurbulence` lettering, `../styles/textures.css`'s halftone and grain); this was the one surface in ruler-straight vector, so it read as a diagram. Contours now run through a `feTurbulence`+`feDisplacementMap` roughening filter (same technique as `TitleReveal.jsx`, `scale="11"` — small on purpose, a hand-inked edge and not a melted shape), applied to the room as ONE group so all contours share a single displacement field rather than each wobbling independently; and the light's falloff is a **halftone dot pattern masked by a radial gradient** instead of a smooth ramp, so the glow breaks into dots at its edge the way a printed one does. **(5) Identical camera in all five** — the same window rectangle at the same coordinates every episode. Each scene now owns its `win` (position, size, and mullion `cols`/`rows`), and episode 5 puts the window on the LEFT and lights from that side. **Two further defects were then caught by measurement, not by eye, and fixed in the same pass** (the Browser pane's compositor died partway through this batch — a known limitation documented elsewhere in this file — so verification moved to rasterizing each live `<svg>` into a canvas and reading pixels back): (a) **episodes 1 and 3 still read as the same thumbnail**, scoring 6.8 mean per-cell luminance difference on a 16×9 grid where every other pair scored 15–25 — both were a figure in front of a right-hand window with an audience below. Episode 1's window is now WIDE and HIGH rather than tall, and its foreground is horizontal raked bench slabs rather than episode 3's domed heads, so the two differ in silhouette language and not just in props; the weakest pair is now **15.9** and the range 15.9–32.2. (b) **Tiles were too dark to read at all** — 75% of every tile's pixels fell between luminance 18 and 25 out of 255, which is the same unreadable-at-thumbnail-size failure this component exists to fix. Cause was three multiplied dimmings stacking on one tile: the unvisited `lightOpacity`, the hero-strength vignette, and the filmstrip's own CSS dimming of unselected tiles. The vignette is now lighter and wider on `detail="low"`, and unvisited TILES hold more light (0.78) than unvisited heroes (0.55) — the visited/unvisited distinction still reads because it is a comparison between neighbouring tiles, not an absolute. Tile p95 brightness went 107–150 → 143–182. **The `detail` performance rule is intact and re-verified**: 1 `feGaussianBlur` and 1 `feDisplacementMap` on the entire screen, both on the hero, zero of either inside any tile; 425 SVG nodes across all six scene instances (was 341). `--signal` uses on the screen still exactly 1, no page-level horizontal overflow, `<main>` still fills the viewport exactly (800/800 at 1280×800). `npm run lint` clean, `npm run build` clean at 144.8 KB gzipped. **Not verified visually** — the compositor limitation above meant no screenshot could be taken after the rebuild, so the composition itself should be eyeballed in a real browser before this is called done. |
| `RevealPanel.jsx` | §3.5 motion table + mechanic 5 | **New, Phase 6/9.** Motion wrapper around `Panel.jsx` (or any panel-shaped block) — wraps from the outside only, never touches `Panel.jsx`'s own clip-path/shadow/seed logic. Two jobs: (1) "Panel reveal on scroll" — fade + 12px rise, 300ms, staggered 60ms per panel via an `index` prop, triggered once via `whileInView`; (2) mechanic 5's second ambient detail — a slow scroll-driven parallax on the wrapped content, capped at the spec-mandated 8px via `useTransform`. Both gated on `useReducedMotion() \|\| !useSettings().motion`; under reduced motion it still renders the *same* `motion.div` + ref (not a different plain-`div` branch) with no animation props, so `useScroll`'s target ref stays stable and content is visible immediately, never mid-animation. Used by `EpisodeView.jsx` (content panels) and `ChapterSelect.jsx` (grid cards). |
| `Typewriter.jsx` | §3.5, OPTIONS toggle §4.1 | **New this batch.** 22ms-per-character reveal, skippable by clicking the text or pressing Enter mid-reveal (the Enter listener is on `window` and only mounted while typing — the alternative was making a paragraph focusable purely to catch a keypress). Short-circuits to the complete string when `useSettings().typewriter` is off, when `useSettings().motion` is off, or under `useReducedMotion()`, and does so by *initialising* state to the full length rather than animating fast, so text is never briefly caught mid-reveal. Two details that keep it from gating content: the full string is always in the DOM in an `sr-only` span (a screen reader never waits on the animation), and the untyped remainder renders at `opacity-0` rather than being absent, so the element holds its final size from frame one instead of reflowing everything below it on every tick. **Scope is deliberate:** it is used ONLY on `DialogueChoice.jsx`'s prompt — the single genuinely dialogue-styled string in the app. It is not applied to panel bodies, which are 30–60 words of real resume content each; typing 16 of those out would put a timer between a recruiter and the information, which spec §2 rule 2 forbids outright and the 90-second constraint rules out anyway. Reset-on-prop-change happens during render (React's documented pattern), not in an effect, so a new line never renders one frame of the previous line's progress. |
| `AmbientLayer.jsx` | mechanic 5 | **New, Phase 9.** Mounted once in `App.jsx`, inside `SettingsProvider`. Renders the film-grain overlay (`.texture-grain` + `.texture-grain-animate` from `../styles/textures.css`, fixed full-viewport, `pointer-events-none`, 8fps steps() flicker) — the first of the two mechanic-5 ambient details chosen (the second is `RevealPanel.jsx`'s parallax). Not rendered at all under reduced motion (OS `prefers-reduced-motion` OR the Settings motion toggle), rather than just freezing the animation. Also syncs a `motion-off` class onto `<html>` whenever the Settings motion toggle is off, so plain-CSS transitions elsewhere (Panel hover lift, DialogueChoice hover bar) honor the in-app toggle too, not just the OS media query — see the matching block in `../index.css`. Lives here rather than in `SettingsContext.jsx` because `src/context/*` was off-limits for this batch. |
| `EscapeBack.jsx` | not in spec | **New this batch**, direct user request: "pressing ESC from any place inside the software should take me to the previous menu." Headless (`return null`), mounted once in `App.jsx` inside `<BrowserRouter>` so one window `keydown` listener covers every screen. ESC maps to a **hierarchical** parent via `parentOf(pathname)` — `/chapters/:id` → `/chapters`, `/chapters` → `/`, `/plain` → `/`, `/` → no-op — deliberately not `navigate(-1)`: history-back would walk off the site on a deep-link/refresh and could disagree with the on-screen "◂ Previous menu" links, which are hard destinations. Modified-key chords (Cmd/Ctrl/Alt/Shift+Esc) are ignored so it never shadows a browser/OS shortcut. `MainMenu.jsx` handles ESC on its own to close an open OPTIONS panel first (see its row), so on `/` the panel backs out before this handler's no-op applies. Verified live: episode → `/chapters` → `/`, `/plain` → `/`, and OPTIONS-open + ESC closes the panel without navigating. **This batch — `parentOf()` learned the two game routes** (root `CLAUDE.md` §0.1, phase 11): `/play/:id` → `/floor`, and `/floor` → `/` (the same level as `/chapters` and `/plain`). The first is the point of the change — **a room escapes to the floor, not to the title screen**, so ESC agrees exactly with the on-screen “◂ Leave the room” control in `../screens/Play.jsx` rather than teleporting a player two levels out of the building. The hierarchical-not-`navigate(-1)` reasoning above applies with more force here, not less: a visitor who deep-links or refreshes `/play/education` has no history behind it, and history-back would drop them off the site mid-game. Both patterns are prefix tests on the pathname, matching the existing `/chapters/` branch, so a future game route slots in the same way. Note that `../components/PageTransition.jsx`'s `ROUTE_DEPTHS` encodes the *same* hierarchy independently — keep the two tables agreeing, or ESC will dolly the camera the wrong way. |
| `BackgroundMusic.jsx` | not in spec | The main-menu "radio," direct user request (spec/root `CLAUDE.md` §9 precedent: not a build-phase item, added the same way `PageTransition.jsx`/`StartPrompt.jsx` were). **Moved this batch** from being mounted by `MainMenu.jsx` (Beat 3) — start-on-mount, stop-on-unmount, i.e. it stopped on ANY navigation away from the menu, including into `/plain` — to being mounted once in `App.jsx`, outside the routed screens, so the component itself never unmounts on navigation. Direct user follow-up: the radio should keep playing across menu-ish navigation (`/`, `/plain`, toggling OPTIONS) and only stop specifically on entering `/chapters` or `/chapters/:id` — "scoped to the menu only" turned out to mean narrower than intended once `/plain` was in the mix. Two gates now decide playback, both read inside this component: `useMusic().started` (new `../context/MusicContext.jsx` — a one-way session flag `TitleScreen.jsx` flips true the instant its beat sequence reaches MENU, replacing "is this component mounted" as the never-play-before-the-menu signal) and `useLocation().pathname.startsWith('/chapters')` (pauses without unmounting, so `<audio>`'s `currentTime` is preserved and playback resumes from the same spot rather than restarting — verified live: paused mid-track entering `/chapters`, stayed paused at the same position through `/chapters/:id`, resumed and kept advancing on navigating to `/plain`). `shouldPlay = music && started && !inChapters` replaces the old bare `music` check as the play/pause effect's gate; the "Now Playing" badge needs no separate change since it already keys off the `isPlaying` state that effect sets. Tracks come from `../lib/musicPlaylist.js`, which auto-discovers whatever MP3/OGG files exist in `../assets/audio/` via `import.meta.glob`; **renders nothing at all — no `<audio>`, no badge — when that list is empty**, the same graceful-degradation pattern this app uses for `TITLE_ART_URL`/cover placeholders. **`../assets/audio/` now holds 14 real tracks** (see that folder's `CLAUDE.md`) — two fetched directly from Mixkit's public CDN by this assistant with the user's explicit permission (never ripped/downloaded from the YouTube link the user originally offered — declined for copyright/ToS reasons when this feature was scoped), the other twelve dropped in by the user themselves mid-session, picked up automatically with zero code changes. Behavior: **shuffled, not sequential** — direct user follow-up request ("it doesn't start from a fixed audio but a randomizer... plays a random audio from the list"). The opening track is chosen randomly on mount; every advance — automatic (`ended`) or manual (clicking the badge, or the new Previous/Next Track OPTIONS controls — see `MainMenu.jsx`'s row below) — excludes whichever track is currently playing, so immediate repeats can't happen even by chance. A true single-track playlist instead uses the `<audio loop>` attribute directly (no randomization possible or needed with one file). **This batch — track selection moved out of this component, into `../context/MusicContext.jsx`:** `trackIndex` and the `next()`/`previous()` logic (a `{history, pointer}` shuffle-with-history stack, not a bare random index — see that context's row for why "previous" needs real history to mean anything) now live in `MusicContext` so `MainMenu.jsx`'s OPTIONS panel can drive them directly, without reaching into this component. This file no longer owns *which* track is selected, only actual playback of whichever one the context says is current — the `<audio>` element, play/pause, and the fade-in ramp. The "Now Playing" badge's skip-forward click now calls the context's `next()` instead of local state. Each track fades its volume in over ~600ms on start via a manual `setInterval` ramp (plain `<audio>` element, a different mechanism from `../lib/sound.js`'s synthesized Web Audio cues) so auto-advancing never pops. Gated by the Settings `music` toggle (`../context/CLAUDE.md`, `MainMenu.jsx`'s OPTIONS group), independent of `sound`. Playback still can't violate "never autoplay": `<audio>.play()` needs a real user gesture, which `StartPrompt.jsx` (Beat 0) already guarantees before the menu can render — "plays automatically when you turn the website on" is accurate from the very next moment after that unavoidable first click/tap/key, not sooner, since no browser permits earlier audio regardless of what this code does; a `pointerdown`/`keydown` retry-on-failure listener is a safety net for stricter per-browser heuristics, mirroring `TitleScreen.jsx`'s `sounds.intro` pattern. **Badge relabeled "Now Playing"** (was "Currently Playing" in an earlier pass this same batch, before a direct follow-up request specified the exact wording) — fixed top-right, `--ink` chip, three-bar CSS equalizer glyph (`eq-bar` keyframe, `../styles/motion.css`, reduced-motion gated), and now shows song title in the display face (Alfa Slab One, "a great font" per the user's request) plus an artist/source line underneath sourced from `musicPlaylist.js`'s per-track metadata. Itself a real `<button>` that skips to a new random track on click when the playlist has more than one entry, keyboard-reachable, and only renders once a track has actually started playing (not merely attempted). Verified: `Music: On/Off` toggle in OPTIONS, `npm run lint`/`npm run build` both clean at every step, all 14 tracks bundle correctly as separate lazy-loaded assets (no JS bundle growth — still ~137KB gzipped). Live in-browser verification of playback/badge/skip succeeded earlier in this batch (confirmed real audio playing, correct title/artist swap on skip); the later shuffle-logic and title-case-fix changes were verified via direct Node unit checks (1000-trial no-repeat/coverage check for the shuffle picker, and the fixed `titleCase()` re-run against all 14 real filenames) rather than a second live pass, after the sandboxed Browser pane's compositor became unavailable again (a known, previously-documented limitation of this environment — see Phase 6's route-transition verification note in the root `CLAUDE.md`). **The track-selection move to `MusicContext.jsx` was verified live this batch** via the accessibility tree (the Browser pane's compositor was unavailable for screenshots again, same known limitation, but `read_page`/`javascript_tool` work fine): opened OPTIONS, clicked Next Track and watched the "Now Playing" badge swap from "Herbal Tea" to "Lullaby," clicked Previous Track and watched it swap back to "Herbal Tea," then confirmed via `document.querySelectorAll('button')` that "◂ Previous Track" reports `disabled: true` at the start of history (no further back to go) while every other control stays enabled. `npm run lint` and `npm run build` both clean, bundle size unchanged (~137.8KB gzipped JS). **This batch — the "excludes whichever track is currently playing" behavior described above is superseded.** A live report that a track could return after two or three skips was correct behavior for a memoryless random pick, so `next()` in `../context/MusicContext.jsx` now draws from a shuffled bag with a no-repeat window instead: every track plays once before any plays twice, and nothing returns inside half a playlist. Nothing in this file changed — it only calls `next()` — but its doc comment was corrected so the two files don't describe the same mechanism differently. See that context's row for the algorithm and the verification numbers. **This batch — the route gate is gone; the radio now plays site-wide.** Direct user request: "The New Game Menu and the Episodes menu should have the music from the main menu kept on playing alike it is in the Plain Resume Setting." NEW GAME and EPISODES both navigate to `/chapters`, which was exactly the route this component paused on, so the bed cut every time a visitor entered the game surface — the same discontinuity the previous widening (menu-only → menu + `/plain`) was meant to fix, just one route further in. `useLocation()` and the `inChapters` check are removed and `shouldPlay` is now `music && started`. Because playback no longer depends on the pathname, a navigation doesn't touch the `<audio>` element at all: the track keeps playing through the route change rather than pausing and resuming from the same offset, which is a stronger guarantee than the old pause/resume gave. `/chapters` and `/chapters/:id` play no audio of their own, so there is nothing to duck for, and the OPTIONS `music` toggle is still the way to silence it. The "Now Playing" badge is now visible on those screens too — `ChapterSelect.jsx` keeps its hero text top-LEFT, so the fixed top-right badge has clear space; on `EpisodeView.jsx` it sits above the `z-40` full-screen title card (badge is `z-50`), consistent with it being a persistent HUD everywhere else. Verified live against the running dev server: after reaching the menu, `currentTime` ran 1.81s → 13.86s across a NEW GAME navigation on the SAME track file with `paused: false` throughout (i.e. no pause and no restart), the badge rendered on `/chapters`, and on `/chapters/education` an 11-sample probe at 1s intervals showed `currentTime` advancing 13.57 → 23.57 with `paused: false` and one unchanged track for the whole window. `npm run lint` clean (only the pre-existing warnings in other files), `npm run build` clean at 144.77 KB gzipped, unchanged. **This batch — the "Now Playing" badge auto-collapses.** Live report: with the radio now playing site-wide, the full-width card sat permanently on top of top-right content on every menu and game screen (`Floor.jsx` header links, `EpisodeView.jsx` masthead link, `Play.jsx` record column). Two new bits of local state: `expanded` (default true) and a `collapseTimerRef`. An effect keyed on `[trackIndex, isPlaying]` opens the card and schedules a collapse to `COLLAPSE_MS` (4500ms) — so it re-opens on first play and every track change/skip, but **not** on navigation (entering a menu doesn't touch playback, so the effect doesn't re-run). Collapsed, the button renders only the equaliser glyph (`p-2`, ~31px square); `onMouseEnter`/`onFocus` call `openBadge()` (cancels the timer, expands), `onMouseLeave`/`onBlur` call `scheduleCollapse()`. Click logic is state-aware: collapsed → expand + reschedule; expanded → `next()` as before. `aria-label` reflects the state ("Click to show track details" vs "Click to skip to the next track"). Verified live on the dev server: 199px on track start → 31px after 4.5s → 199px on focus → 31px after blur. `npm run lint`/`npm run build` clean, bundle unchanged. |
| `DossierEntry.jsx` | not in spec | **New this batch**, for the `/plain` Dossier rebuild (`../screens/PlainResume.jsx`, see that file's row and `../screens/CLAUDE.md`) — the one new component file that rebuild's brief allowed, spun out because six of that screen's nine sections (Experience, Projects, Competitions, Education, Volunteering, Other work) render this exact collapsible-row shape and it was already long enough without six near-duplicate `<details>` blocks inline. One native `<details>`/`<summary>` per entry: title (`<h3>`, display face), an optional `subline` (org · timespan or a panel's timestamp), optional tag chips, a ▸/▾ glyph that flips with state, and a body of prose plus optional coursework/link. Deliberately not `Panel.jsx`/`RevealPanel.jsx` — same call already made on `ChapterSelect.jsx`: the comic-panel primitive is the content/game layer's language, a document surface uses rules and type. **Controlled, not uncontrolled** — `open`/`onToggle` props, no internal state — because `PlainResume.jsx`'s Expand all/Collapse all, the filter's auto-expand, and the print handler all need to drive every entry's open state at once from one `Set` of open ids, which an uncontrolled `<details>` (each managing its own DOM attribute) can't support. Consumes one normalized entry shape (`{ id, title, subline, tags?, body, coursework?, link? }`) that `PlainResume.jsx` maps five structurally different source shapes into before reaching this component, so it only has to know one contract. |
| `PageTransition.jsx` | not in spec | Route-change transition, wrapped around every screen by `App.jsx`'s `AnimatedOutlet` (keyed on `location.pathname` via `AnimatePresence mode="wait"`). Not spec-mandated — direct user request, and **rebuilt from scratch this batch** on a follow-up. The first version was a horizontal `--ink` curtain wiping across via `scaleX` while the content only cross-faded; the follow-up asked for something categorically different: "rather than the sliding animation... the side options and everything slowly fades away, a WHOOSH slow sound effect as we move into the center of the screen with the buildings moving away and we enter the specific menu, and then the same effect in reverse if we come back to the main menu." It is now a **dolly** — a camera push along z, nothing sliding sideways. Going deeper (`/`→`/chapters`, `/chapters`→an episode, `/`→`/plain`) the outgoing screen scales 1 → 1.45 and fades, so the menu items drift outward and dissolve while `TitleBackdrop.jsx`'s skyline grows past the frame edges (that IS "the buildings moving away" — the camera is the thing moving, not the buildings), and the incoming screen grows in from 0.86. Coming back is the exact inverse: outgoing recedes to 0.84, the menu returns from 1.32. **Direction is route depth, not history** — `/` is 0, `/chapters`/`/plain` are 1, `/chapters/:id` is 2; shallower than where you were is "out", anything else "in" — deliberately so the browser Back button and an in-page "← Title" link read identically, both being the camera pulling back. Depth lives in a `ROUTE_DEPTHS` table and is computed by the exported `noteRouteChange()`, which `AnimatedOutlet` calls **during render**: on a navigation the outgoing screen is an element `AnimatePresence` is holding onto, so its props are frozen at the previous navigation's values and can't carry the answer, while a module-level value set before either child renders can. It's idempotent per pathname so StrictMode's double render can't flip it. Eases are asymmetric on purpose (accelerating out, decelerating in) — one shared curve is what made the old version read as a single mechanical slide. An opaque `--ink` veil sits **behind** the moving screen (`z-20`, content at `z-30`, both below the toast's `z-50` and the grain's `z-[70]`, matching the paint order those screens already had unwrapped) and crossfades so the beat between the two screens is a dip to black rather than a flash of bare `--paper` — `mode="wait"` fully unmounts the outgoing screen before the incoming one exists, and without the veil that gap shows the body background. **The `display` switch is the load-bearing part, not a detail.** The previous version animated `opacity` ONLY, because a CSS `transform` on any ancestor becomes the containing block for `position: fixed` descendants, and `TitleScreen.jsx`'s beats, `MainMenu.jsx` and `EpisodeView.jsx`'s title card are all `fixed inset-0` and rely on that resolving to the viewport. A zoom *is* a transform, so that constraint had to be solved rather than avoided: while animating, the wrapper is `fixed inset-0 overflow-hidden`, i.e. exactly viewport-sized — it becomes those children's containing block but resolves to precisely the same rectangle they'd have had, and `overflow-hidden` gives the zoom its frame; at rest it is `display: contents`, generating no box at all, so there is no transform, no containing block and no layout participation, which is the state it spends nearly all its time in. `settled` flips on `onAnimationComplete` **with a `setTimeout` backstop**, and the backstop is not belt-and-braces — `AnimatePresence initial={false}` skips the enter animation on the very first mount (no page-load zoom, deliberate) so that callback never fires there, and without it the first screen you land on would stay `fixed`/`overflow-hidden` forever and `/plain` could not scroll. Confirmed live: after a fresh load the wrapper reads `class="contents" style="opacity: 1; transform: none"` — framer does emit `transform: none` at rest, not an identity matrix. Scroll: `AnimatedOutlet` now resets scroll on navigation, which fires while the outgoing screen is still visible, so `noteRouteChange()` also records `window.scrollY` at the one moment it's still correct (after the navigation, before that layout effect) and the exiting instance applies it as a negative `marginTop` on an inner plain div — margin rather than `translateY`, since a second transform on an ancestor would reopen the `fixed` problem above. Under reduced motion (OS or the Settings toggle) it renders `children` with no wrapper at all, so `AnimatePresence` has nothing to animate and the swap is instant; the whoosh (`whooshIn`/`whooshOut`, new in `../lib/sound.js`) is suppressed there too, being the sound of a camera move that isn't happening. **Verification (partial, and the gap is environmental — root-caused this time, not just noted):** the direction table was exercised against the real module in the running page — `/`→`/chapters` in, →`/chapters/education` in, →`/chapters` out, →`/` out, →`/plain` in, →`/` out, repeat `/` unchanged. A real NEW GAME click fired `whooshIn`'s rising 200 → 2000 Hz sweep and drove the exit scale through a correctly accelerating ramp (1.002, 1.006, 1.018, 1.028, 1.05, 1.07, 1.11, 1.14 … 1.45). Opacity could not be observed and the exit never completed, and the cause is now pinned rather than guessed: this Browser pane is never composited, so `document.timeline.currentTime` is stuck at **0** and every Web Animations API animation is frozen — framer-motion accelerates `opacity` via WAAPI while running `scale` on its own rAF frameloop, so with a `requestAnimationFrame` shim installed the scale animates and the opacity does not, and `AnimatePresence` waits forever on the frozen half. The exiting element's WAAPI keyframes were read directly and are correct (`opacity: ["1", "0"]`). A real browser tab has a live timeline and runs both. The reduced-motion path was separately confirmed end-to-end (no wrapper in the DOM at all, instant swap). `npm run lint`/`npm run build` clean; bundle 138.6 KB gzipped, up ~0.8 KB. **Optimized this batch on a live report that "the website lags a bit whilst doing this."** The cause was measured, not guessed: the main menu being zoomed is ~1,450 SVG `<rect>`s (`TitleBackdrop.jsx`'s four parallax planes and their per-building window grids) plus three `feGaussianBlur` window-glow filters, counted live in the running page — and a browser re-rasterizes vector content whenever the scale it is drawn at changes, so an un-promoted zoom re-draws that entire scene every frame. Three changes, and the first two only work as a pair: (1) the moving wrapper now carries a `.route-stage` class (`src/index.css`) = `will-change: transform, opacity` + `contain: paint`, promoting the scene to its own compositor layer so it rasterizes once and the zoom becomes a GPU texture transform; it is attached only while moving, since a permanently promoted full-viewport layer costs GPU memory for nothing. (2) `html.route-moving` pauses decorative CSS animation for the duration of the move — not a separate nice-to-have, because a promoted layer only stays cheap while its contents hold still, and `window-flicker`/`solo-glow` (SVG opacity), `fog-drift` (SVG transform) and the film grain (`background-position`, full viewport at 8fps) all repaint continuously, which would force the layer to re-rasterize and put the per-frame cost straight back. `animation-play-state: paused` rather than cancelling, so they resume exactly where they were; Framer Motion is untouched by it, driving this transition through inline styles and WAAPI rather than CSS animations. The class is refcounted, since an outgoing and an incoming instance can both consider themselves moving across a commit and whichever unmounts first must not strip it from under the other. (3) Peak zoom trimmed 1.45 → 1.3 (and 1.32 → 1.24 returning): rasterizing at 1.45 costs ~2.1x the pixels of 1.0, at 1.3 ~1.7x. The arrival values (0.86/0.84) were deliberately left alone — scaling below 1 is cheap by comparison. Applied in a `useLayoutEffect` so the class is on the element before the first animated frame paints, verified live: sampling `<html>`'s class, the wrapper's computed `will-change`/`contain`, and the play state of both a flickering window group and the grain overlay across a real NEW GAME click showed all four flip together in a single step (`contents`/`auto`/`none`/`running`/`running` → `route-stage fixed…`/`transform, opacity`/`paint`/`paused`/`paused`) with no un-promoted intermediate frame, and a subsequent reload confirmed everything is released again at rest (`<html>` class empty, grain `running`, wrapper back to `contents`). The reverse direction was also verified end-to-end in the same pass — `← Title` fired the falling 2000 → 200 Hz sweep and the wrapper measured exactly the layout viewport (1265×720), confirming `contain: paint` does not disturb the `fixed inset-0` sizing the `display`-switch trick protects. Bundle after the optimization: 138.7 KB gzipped JS / 7.29 KB CSS. **This batch — `ROUTE_DEPTHS` gained the two game routes: `/play/:id` = 2 and `/floor` = 1** (root `CLAUDE.md` §0.1, phase 11). Those numbers deliberately **mirror the reading surface's depths** — `/floor` sits one level in from the menu like `/chapters`, and a room on it sits one level further like `/chapters/:id` — so walking into a door dollies **in** and leaving a room dollies back **out**: the same camera grammar as the rest of the site, with no special case for “game mode.” This is also why the two game screens were nested under the same `AnimatedOutlet` layout route as everything else (see `../CLAUDE.md`'s `App.jsx` row) rather than mounted outside it. **The ordering constraint already documented above still applies and now covers four patterns:** the more specific pattern must be tested first, so `/^\/chapters\/[^/]+/` precedes `/^\/chapters\/?$/` and `/^\/play\/[^/]+/` precedes nothing it could shadow — but if a bare `/play` index route is ever added, it goes **after** the `/play/:id` pattern, not before. Anything unrecognised still falls through to 0 (menu level), so a future route added without touching this table gets title-screen treatment rather than an arbitrary depth. `EscapeBack.jsx`'s `parentOf()` encodes the same hierarchy separately; the two must stay in agreement. |

## Not started (⬜)

| File | Spec ref | What it needs to do |
|---|---|---|
| `ChoiceStats.jsx` | §5 mechanic 6 | End-of-episode percentage-bar screen. Bars fill left-to-right on scroll-into-view, 500ms ease-out, staggered 80ms. Must work with dummy percentages (spec provides sample numbers) if no analytics backend is wired — do not block this component on a backend existing. |
| `ConfidenceMeter.jsx` | §5 mechanic 3 | Thin 4px persistent top-right bar, no numbers/percentage label, reads `useProgress().confidence` (already computed in `ProgressContext.jsx`). At 100% (all 5 episodes visited via `allEpisodesVisited`), surface the secret-ending unlock. Keep it small enough that it never competes with content. |
| `SettingsTray.jsx` | OPTIONS menu, §4.1 | Motion / sound / typewriter toggles, reading and writing `useSettings()`. Currently unnecessary — `MainMenu.jsx` expands all three inline. |

## Notes for whoever builds the remaining four

- Match `Panel.jsx`'s pattern of a small `seed`-driven pure function for any per-instance visual variation — don't hand-roll random values in render (breaks on re-render, fails deterministic tests).
- **Resolved:** `MainMenu.jsx` and `TitleReveal.jsx` stayed fully independent, each with its own `onDone`/`reducedMotion` prop handoff (`MainMenu.jsx` has no `onDone` since it's the terminal beat). `src/screens/TitleScreen.jsx` owns the beat-sequencing state and swaps between the three components.
- **Superseded this batch:** the backdrop used to be duplicated Tailwind classes in both files (judged simpler when it was two lines of gradient). Once the backdrop grew into a real layered scene (`TitleBackdrop.jsx` — SVG silhouettes, vignette, halftone), duplication stopped being the simpler option: two copies could silently drift apart, directly undermining spec 4.1's "this continuity... is the actual Walking Dead structure" requirement that beats 2 and 3 be the *same* scene. `TitleBackdrop.jsx` is now a shared component both files mount; each file still owns its own text/interaction layer on top of it.
