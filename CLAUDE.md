# Telltale-Style Interactive Resume — Master Guide

This is the root guidance file for the whole repo. It is the entry point:
read this first, then follow the link into whichever folder you're about
to touch for the folder-specific detail.

**Full spec of record:** [`docs/PortfolioSpec.md`](docs/PortfolioSpec.md)
(copied from the original brief). If anything in this file or a folder
`CLAUDE.md` ever seems to contradict the spec, the spec wins — fix the
`CLAUDE.md` to match it, don't silently follow the stale doc.

## 0. The one-line concept

A personal resume that behaves like a Telltale (Walking Dead) episode
booting up: presents card → painted title reveal → in-universe main menu
→ chapter select → episode view with comic panels. The game is the skin;
the resume is the substance. **Core constraint that overrides everything
else:** a recruiter must be able to get the actual information in under
90 seconds without playing along. See spec §1–2 for the full non-negotiable
rules (no forced interaction, no gating timers, always a plain-text escape
hatch, load fast, respect reduced motion, mobile is not an afterthought).

### 0.1 The game surface (added after the spec — read this before touching `/chapters` or `/floor`)

The original build had a problem the spec did not anticipate: once
`/plain` became a good document, **the episode screens had no reason to
exist.** They carried the same facts as the Dossier, in boxes, with
"dialogue choices" that only scrolled to a panel you could have scrolled
to yourself, and panel widths that alternated down the page so the text
visibly zig-zagged. Reported symptom: *"the text is all over the place…
lack of fluency,"* and *"we don't wanna make this way too generic."*

The answer was to give an episode **a verb**. There are now two ways in,
and keeping them distinct is what makes the whole thing legal:

| Path | Route | Gated? | For |
|---|---|---|---|
| **NEW GAME** | `/floor` → `/play/:id` | Yes, doors open in order | Playing |
| **EPISODE EXPLORER** | `/chapters` → `/chapters/:id` | **Never** | Skimming |
| **PLAIN RESUME** | `/plain` | **Never** | Recruiting |

`/floor` is a walkable, top-lit corridor drawn on a canvas: the title
screen is a city at night with one window lit, and the floor is *the
inside of that window*. Five doors, one per episode, and behind each a
small game whose mechanic is a metaphor for the work it describes — you
stack semesters into a tower that joins the skyline, triage a hall with
more raised hands than one person can reach, watch four meters drain
because you cannot save all four, fly a night you cannot win, wire five
modules onto one shared graph. Facts surface **as you play**, one short
beat at a time, never as a wall of copy.

**Two rules that must not be traded away:**

1. **The stairwell in the corridor goes to `/chapters`, and `/chapters`
   is never gated.** That is the only reason locked doors are compatible
   with §2 rule 1. `Play.jsx` also offers the next door after two failed
   runs, so no visitor is ever walled off by reflexes.
2. **`FORTY-EIGHT HOURS` cannot be won past second place.** The real
   record is three second places and a national top ten. A game that let
   you take gold would be a nicer game and a dishonest one.

Full architecture, the frozen engine contract, and the per-scene notes:
[`src/game/CLAUDE.md`](src/game/CLAUDE.md).

## 1. Mandatory update rule

**Whenever any code file in this repo changes — new file, edit, delete,
rename — two documents must be updated in the same change:**

1. This root `CLAUDE.md` (at minimum: the Repo Map status table in §4,
   and the Build Phase Status in §5 if the change advances/breaks a phase).
2. The `CLAUDE.md` inside the folder that file lives in (its file list,
   status, and any implementation notes).

Do not defer this to "later" or a separate commit. A `CLAUDE.md` that
describes a folder incorrectly is worse than no `CLAUDE.md` at all,
because it will be trusted. If you are the one making the change, you are
the one who updates both files before considering the task done.

## 2. Model policy — who does what

**Opus decides. Sonnet/Haiku code.** This is a hard rule, not a
suggestion, and it exists because time is the scarce resource here, not
model capability for most of this build.

- **Opus's job:** resolve ambiguity, make architecture/design-token
  judgment calls, decide which model handles the next unit of work,
  review/approve a phase as done, and write or update `CLAUDE.md` files
  itself when the decision-level context is what's being recorded. Opus
  does not hand-write component/screen implementation code except to fix
  a judgment-level defect that a lower model flagged as ambiguous.
- **Sonnet (default coding model):** all standard implementation —
  new components, screens, context wiring, routing, most of the work in
  the Build Phase list below. If you're not sure whether a task needs
  Opus or Sonnet, it's Sonnet.
- **Haiku:** mechanical, low-judgment work only — placeholder copy edits
  in `episodes.js`, repetitive near-identical edits across files, simple
  CSS value tweaks, filling in a `CLAUDE.md` status table after Sonnet
  finishes a phase.
- **Batch by phase, not by file.** Pick one model for an entire phase or
  logical unit of work (e.g. "build all of §5 mechanic 1") and stay on it
  until that unit is done. Do not switch models mid-task to save a few
  tokens on one file — the switching overhead costs more time than it
  saves. Switch only at phase boundaries, per Build Order in spec §8.

## 3. Design tokens (spec §3) — quick reference

Full detail and rationale: spec §3. Source of truth in code:
[`src/styles/tokens.css`](src/styles/CLAUDE.md).

| Token | Hex | Use |
|---|---|---|
| `--ink` | `#141210` | Borders, outlines, text on light surfaces |
| `--paper` | `#D8CFC0` | Base background |
| `--shadow` | `#3B342E` | Panel fills, secondary surfaces |
| `--signal` | `#C4321E` | THE accent — max 3 uses per screen |
| `--dusk` | `#2A3742` | Cool counter-shadow, sparing |
| `--bone` | `#EFE7D8` | Text on dark panels |

Display face: Alfa Slab One / Oswald 700 / Archivo Black (titles only).
Body face: Inter / Source Sans 3 / Public Sans. Never hardcode a hex
value or font stack in a component — reference the token.

## 4. Repo map

Status legend: ✅ implemented and matches spec · 🚧 partially implemented
· ⬜ not started · ⚠️ stale/broken, needs cleanup.

```
/                             ✅ scaffold present
├── .github/
│   └── workflows/
│       └── deploy.yml        ✅ builds with `npm run build` and deploys
│                                `dist/` to GitHub Pages on every push to
│                                `main` (`actions/upload-pages-artifact` +
│                                `actions/deploy-pages`). Not part of the
│                                original 10-phase spec build — added so
│                                the live site has a stable link for
│                                outside testers. See §9 below.
├── docs/
│   ├── PortfolioSpec.md      ✅ spec of record, do not edit casually
│   └── content-draft.md      ⚠️ Phase 10 intake form — now SUPERSEDED. The
│                                user supplied a full master resume instead
│                                and the copy questions are answered; only
│                                its §3 (art) and §4 (stats numbers) are
│                                still open. Delete once those two land.
├── public/                   🚧 see public/CLAUDE.md
├── scripts/
│   └── verify-scenes.mjs     ✅ `npm run verify:scenes`. Drives every
│                                minigame's real update()/draw() against a
│                                stub 2D context under three input modes
│                                (pointer only, keyboard only, hold) and
│                                asserts the scene contract: reveals form a
│                                complete prefix of beats[], `done` fires
│                                exactly once with a valid outcome,
│                                progress stays in 0..1, listeners are
│                                released on dispose, and nothing throws.
│                                It exists because this environment's
│                                Browser pane does not run
│                                requestAnimationFrame (measured: 0 ticks
│                                in 500ms) — static rendering can be
│                                screenshotted, behaviour cannot. Run it
│                                after ANY change under src/game/scenes/.
├── src/                      🚧 see src/CLAUDE.md
│   ├── main.jsx              ✅ standard Vite bootstrap, unlikely to need changes
│   ├── App.jsx                ✅ router root — BrowserRouter wrapping
│   │                            ProgressProvider → SettingsProvider →
│   │                            ToastProvider → AmbientLayer +
│   │                            BackgroundMusic + EscapeBack + Routes for
│   │                            /, /chapters, /chapters/:id, /plain (spec
│   │                            §7). EscapeBack.jsx (new this batch) is a
│   │                            headless global ESC-to-go-back handler. Default Vite template markup and dead
│   │                            asset imports removed, not recreated.
│   │                            **This batch:** the four routes are now
│   │                            nested under one layout `<Route element=
│   │                            {<AnimatedOutlet />}>` instead of each
│   │                            getting a top-level `element` — see the
│   │                            in-file doc comment and
│   │                            `components/PageTransition.jsx` below for
│   │                            why (a route-change "parallax" transition,
│   │                            not spec-mandated — direct user request).
│   │                            **This batch:** `AnimatedOutlet` also calls
│   │                            `noteRouteChange()` during render (to give
│   │                            the now-directional transition its in/out
│   │                            direction) and resets scroll to the top on
│   │                            every route change — see src/CLAUDE.md.
│   ├── index.css              ✅ Tailwind v4 theme mapped to tokens.css,
│   │                            plus a class-scoped reduced-motion mirror.
│   │                            **This batch:** a `@media print` block
│   │                            appended at the end, scoped under
│   │                            `.dossier` (the root class on
│   │                            `PlainResume.jsx`'s `<main>`) so it can
│   │                            never leak onto the three game screens —
│   │                            forces true black-on-white regardless of
│   │                            that screen's on-screen theme, hides
│   │                            `.dossier-chrome` (rail/chip-bar, control
│   │                            bar, back link, download button), forces
│   │                            the two-column layout to a single column,
│   │                            forces `<details>` content visible as a
│   │                            belt-and-braces backstop alongside the
│   │                            JS-driven expand-before-print, and prints
│   │                            external link URLs after the link text.
│   │                            One rule (`.texture-grain { display:
│   │                            none }`) is deliberately NOT scoped to
│   │                            `.dossier` — the film-grain overlay is a
│   │                            `position: fixed` sibling of every screen
│   │                            mounted by `AmbientLayer.jsx`, not a
│   │                            descendant of any one screen's root, so
│   │                            it has to be suppressed globally.
│   ├── components/           🚧 see src/components/CLAUDE.md — 11 of the
│   │                            original spec §7 14 built, plus
│   │                            StartPrompt.jsx (new this batch, not in
│   │                            spec — a beat-0 "click to start" gate).
│   │                            **This batch:** EscapeBack.jsx — a
│   │                            headless global ESC handler mounted once
│   │                            in App.jsx that walks the route hierarchy
│   │                            up one level (episode → /chapters →
│   │                            / ; /plain → /), direct user request.
│   │                            TitleBackdrop.jsx is a four-plane city scene
│   │                            (~69% frame height, glowing window grids,
│   │                            parallax, embers, lone-lit-window easter
│   │                            egg; no human figure) — Phase 3 below.
│   │                            **This batch:** PageTransition.jsx — the
│   │                            route-change transition, not in spec §7's
│   │                            original list. Rebuilt this batch from a
│   │                            sideways curtain wipe into a directional
│   │                            camera dolly (zoom in going deeper, zoom
│   │                            back out returning to the menu) with a
│   │                            matching whoosh cue — direct user request.
│   │                            Also: BackgroundMusic.jsx, the main-menu
│   │                            lofi "radio" (direct user request, not in
│   │                            spec), auto-discovering tracks from
│   │                            assets/audio/ via lib/musicPlaylist.js.
│   │                            Mounted once in App.jsx (was: by
│   │                            MainMenu.jsx, which stopped it on any nav
│   │                            away from the menu). **This batch:** the
│   │                            remaining /chapters* pause gate is removed
│   │                            too, so the radio now plays on every
│   │                            route — direct user request.
│   │                            **This batch:** EpisodeWindow.jsx — episode
│   │                            cover art drawn as one lit window seen from
│   │                            outside, five interiors keyed by a new
│   │                            `scene` field in episodes.js — now
│   │                            EpisodeArt.jsx, rebuilt against a Batman:
│   │                            A Telltale Series screenshot as one big
│   │                            backlit silhouette per episode.
│   │                            **This batch:** EpisodeArt.jsx rebuilt a
│   │                            third time — the staging is kept, but the
│   │                            drawing moved into this app's own printed-
│   │                            ink language (tonal ladder, rim light,
│   │                            roughened contours, halftone falloff) and
│   │                            each episode got its own camera. See
│   │                            src/components/CLAUDE.md for the five
│   │                            defects this fixed and the measurements. Panel.jsx
│   │                            gained a `tone` prop ('paper' default,
│   │                            'night' new) and TitleBackdrop.jsx a
│   │                            `variant` prop ('title' default, 'distant'
│   │                            new) — both so ChapterSelect.jsx can sit
│   │                            inside the same night city as the menu
│   │                            instead of cutting to a light-mode screen.
│   ├── context/               ✅ see src/context/CLAUDE.md — ProgressContext +
│   │                            SettingsContext + MusicContext.jsx, whose
│   │                            track picker became a shuffled bag with a
│   │                            no-repeat window this batch (was a
│   │                            memoryless random pick that let a track
│   │                            return after 2-3 skips).
│   │                            **This batch:** fixed a live-reported bug —
│   │                            ChapterSelect.jsx showing "10 of 5 viewed."
│   │                            ProgressContext.jsx's persisted `visited`
│   │                            array could carry stale episode ids left
│   │                            over from earlier episodes.js schemas
│   │                            (ids were renamed more than once); it now
│   │                            reconciles against the current episode id
│   │                            set on load and on every visit, so the
│   │                            count can never exceed episodes.length
│   │                            again and stale ids self-heal out of
│   │                            localStorage.
│   ├── data/                  ✅ see src/data/CLAUDE.md — content source of
│   │                            truth. episodes.js carries REAL content
│   │                            (Phase 10) organized by CATEGORY, not
│   │                            chronology — education / academia (TA+RA)
│   │                            / leadership+volunteering / hackathons /
│   │                            projects; profile.js carries the
│   │                            contact/education/skills/awards material
│   │                            that only /plain renders.
│   │                            **This batch:** episode titles are now
│   │                            PLAIN CATEGORY LABELS (EDUCATION /
│   │                            TEACHING & RESEARCH / LEADERSHIP &
│   │                            COMMUNITY / COMPETITIONS / PROJECTS), the
│   │                            flavor titles are gone, subtitle is a
│   │                            contents line, ids were renamed to match,
│   │                            and ZNotes moved from leadership into
│   │                            teaching.
│   │                            **This batch:** a full pass against a new
│   │                            master resume — the CHIP/HEC research
│   │                            correction, the EduGap/TaxNet role-honesty
│   │                            correction, two new panels (OFF CAMPUS,
│   │                            CITYMIND) and a rebuilt profile.js. See
│   │                            src/data/CLAUDE.md.
│   │                            **This batch (the `/plain` Dossier
│   │                            rebuild):** two real bugs fixed, not
│   │                            content changes. (1) `episodes.js`'s
│   │                            `competitions` episode carried
│   │                            `plainRole: 'projects'`, so its five
│   │                            hackathon panels rendered mixed into the
│   │                            three real project panels under one flat
│   │                            "Projects" heading on `/plain` — now
│   │                            `plainRole: 'competitions'`, and
│   │                            `PLAIN_SECTIONS` gained a second entry
│   │                            (`{ role: 'competitions', heading:
│   │                            'Competitions & hackathons' }`) so the
│   │                            two split into their own sections. (2)
│   │                            `profile.js` gained a `highlights[]`
│   │                            array (four `{ value, label }` pairs —
│   │                            cohort rank, CGPA, TA count, hackathon
│   │                            placements) feeding the new Dossier's
│   │                            "10-second band" stat row, so that
│   │                            screen never hardcodes a number in JSX.
│   ├── game/                   ✅ NEW — the playable layer. See
│   │                            src/game/CLAUDE.md, which is the file to
│   │                            read before touching any of this.
│   │                            engine.js (fixed 960x540 virtual box,
│   │                            letterboxed; fixed 60Hz accumulator;
│   │                            named input actions — FROZEN, five scenes
│   │                            code against it), draw.js (the shared ink
│   │                            vocabulary: litWindow/withRim/figure/
│   │                            roughRect/halftone/grain — mirrors
│   │                            EpisodeArt.jsx and TitleBackdrop.jsx so
│   │                            five separately-built games look like one
│   │                            game), palette.js (reads tokens.css at
│   │                            runtime so a canvas obeys the same tokens
│   │                            as the DOM — no scene may contain a colour
│   │                            literal), GameCanvas.jsx (the only place
│   │                            the engine touches React; scene passed as
│   │                            a FACTORY so each mount is clean), and
│   │                            scenes/ — overworld.js (the floor) plus
│   │                            the five minigames, with stack.js as the
│   │                            reference implementation.
│   ├── lib/                    ✅ see src/lib/CLAUDE.md.
│   │                             sound.js: Web Audio-synthesized UI cues
│   │                             (navigate/select/toast, plus new
│   │                             whooshIn/whooshOut for the route dolly),
│   │                             no shipped files.
│   │                             musicPlaylist.js (new this batch):
│   │                             auto-discovers lofi tracks in
│   │                             assets/audio/ for BackgroundMusic.jsx
│   ├── screens/               ✅ see src/screens/CLAUDE.md — all 4 screens built,
│   │                            routing wired end to end, motion pass done.
│   │                            PlainResume.jsx rebuilt this batch into a
│   │                            complete resume document (contact, education,
│   │                            experience, skills, awards, volunteering).
│   │                            **Rebuilt again this batch into "The
│   │                            Dossier"** — a navigable, filterable,
│   │                            on-theme document (index rail, collapsible
│   │                            entries, a live filter) replacing the
│   │                            197-line unstyled single-column dump. See
│   │                            §5 Phase 5 below and src/screens/CLAUDE.md.
│   │                            ChapterSelect.jsx rebuilt this batch from a
│   │                            plain grid into a hero-panel + filmstrip
│   │                            layout, matching a live Telltale episode-
│   │                            select screenshot the user supplied — then
│   │                            **rebuilt again**, this time into the night
│   │                            city (dark palette, TitleBackdrop 'distant'
│   │                            behind it, EpisodeWindow art per episode,
│   │                            tiles that navigate on click and preview on
│   │                            hover). See §5 phase 4 below.
│   ├── styles/                ✅ see src/styles/CLAUDE.md — tokens + textures,
│   │                            fonts wired, grain texture in use, plus new
│   │                            motion.css (title-screen scene keyframes)
│   └── assets/                🚧 see src/assets/CLAUDE.md — fonts/ done (two
│                                  real WOFF2 files, self-hosted); audio/
│                                  now holds 14 real lofi tracks for
│                                  BackgroundMusic.jsx (not in spec — 2
│                                  fetched by this assistant with
│                                  permission, 12 dropped in by the user);
│                                  covers/ and title-art/ still empty,
│                                  screens use placeholder fallbacks
│                                  (silhouette covers, TitleBackdrop.jsx
│                                  layered-silhouette scene)
└── CLAUDE.md                  (this file)
```

## 5. Build phase status (spec §8)

Ten phases, must proceed in order — each independently viewable per spec.

| # | Phase | Status |
|---|---|---|
| 1 | Scaffold (Vite+React+Tailwind+Router, tokens, fonts) | ✅ Vite/Tailwind/tokens/Router all done. Fonts are now self-hosted (see `src/assets/CLAUDE.md`): two real Latin-subset WOFF2 files wired via `@font-face` in `tokens.css` and preloaded in `index.html` — verified the preload hashes match the CSS `url()` hashes in a production build. Phase 1 is complete. |
| 2 | The Panel primitive | ✅ `src/components/Panel.jsx` — irregular clip-path, hard shadow, halftone texture, hover lift, all per spec §3.3. Unmodified this batch (motion wraps around it, per `RevealPanel.jsx`, never inside it). |
| 3 | Opening sequence (3 beats) | ✅ Beat 1 (`PresentsCard.jsx`), Beat 2 (`TitleReveal.jsx`), Beat 3 (`MainMenu.jsx`) all built and sequenced by `src/screens/TitleScreen.jsx`. Skippable at every beat by click/tap/key. Deliberately still plain CSS transitions for the beat swap itself, not `AnimatePresence` — see `src/screens/TitleScreen.jsx`'s doc comment: spec 4.1 calls for a hard cut between beats 1 and 2, and an `AnimatePresence` cross-fade there would flash the page's `--paper` background between beats. **Fixed earlier this batch:** beats 2 and 3 were reading as visually empty (a flat two-tone gradient), which spec 4.1 explicitly rules out. `TitleReveal.jsx`'s name text also picked up the spec 3.4 distressed-edge filter (SVG `feTurbulence`+`feDisplacementMap`) and a restrained ink-splatter accent, previously deferred. **Then rebuilt twice more this batch** on user feedback. Round 2 replaced the tessellated polygon zigzag ("basic blocks... easily seen through") with hand-placed buildings and added the interactive set: mouse-parallax, a figure, click-spawned embers (beat 3 only — beat 2's whole screen is a click-to-skip button, so embers are gated off there via an `interactive` prop rather than risk a handler competing with the skip), and a hidden one-time easter egg (3 window clicks → a shooting star). Round 3 fixed what a live screenshot then exposed: the skyline filled only ~20% of the frame leaving a huge dead gradient above it, the "lit windows" were flat pale rects with no halo (so they read as gray squares, not light), and the buildings were still plain merged blocks. `src/components/TitleBackdrop.jsx` is now **four parallax depth planes** (hills → far → mid → near, lighter-to-darker toward camera with a distance-haze band between far and mid), skyline peaking at **~69% of viewport height** per the requested 70/30 ratio, buildings carrying setbacks/water tanks/antennas/peaked roofs with deliberate overlap, and per-building **window grids** where each lit pane is drawn twice — blurred halo + crisp core — so it actually reads as a light source. Round 4 (this batch) cut the human figure entirely and fixed two real bugs. **The figure is gone** — the walking version read as an amateur GIF, and its rooftop replacement was a ~30px smudge nobody could find, so its interactivity may as well not have existed. In its place, and replacing the undiscoverable 3-click shooting-star egg, is **one easter egg that telegraphs itself**: a single near-layer building renders every window dark except one larger, slowly-breathing lit pane; clicking it reveals a silhouette still at a desk and fires a `lateNightWindow` memory toast (new copy pool in `src/data/toastVariants.js` — no inline strings). **Parallax bug fixed:** pointer tracking was on the backdrop's own div, but `MainMenu.jsx` stacks a full-height `<nav>` over it as a sibling, so the scene moved on beat 2 and was dead on beat 3; tracking (and the ember click listener) now live on `window`, with real controls excluded so menu clicks aren't fireworks. **The menu got content:** an identity block (name + headline, read from `profile.js`) and an orientation line naming EXTRAS as the plain resume — it was five bare verbs over a picture, which served the 90-second rule badly. `MainMenu.jsx` also gained a left-edge scrim, since menu text now sits over rooftops rather than empty sky. `src/styles/motion.css` holds the keyframes. Motion is gated off under reduced motion (OS or Settings), though the easter egg still works there since it's content, not decoration — all verified live. Real commissioned/painted art is still the eventual Phase 10 goal — `TITLE_ART_URL` is still the one-line swap point in both files. **Round 5 (this batch):** the intro was replaying every time `/` was reached from inside the app (e.g. the "← Title" link on `ChapterSelect.jsx`), not just on a fresh load. `TitleScreen.jsx` now gates the 3-beat sequence behind a module-level `hasPlayedIntro` flag that flips true the first time beat 3 (`MainMenu`) is reached; any later in-session mount of `/` opens straight on the menu, while an actual page reload still plays the full intro. See `src/screens/CLAUDE.md`. **Round 6 (this batch):** fixed a live-reported bug — a big `--signal` red box sometimes appearing around NEW GAME on load, no interaction needed. Cause was `MainMenu.jsx` imperatively calling `.focus()` on the first item on mount to seed keyboard roving-focus; browsers' `:focus-visible` heuristic treats a page's very first focus as keyboard-worthy even when script-driven, so the focus ring rendered for mouse users too, inconsistently across browsers (the "sometimes"). That `useEffect` is removed — nothing is focused imperatively on mount now. Keyboard reachability is unaffected (Tab reaches NEW GAME via native tab order, which legitimately triggers `:focus-visible`; arrows take over from there), and the `▸` selection highlight still shows on NEW GAME by default since that's driven by React state, not DOM focus. See `src/components/CLAUDE.md`. **Round 7 (this batch) — a new Beat 0 added, not in the original 3-beat spec:** `src/components/StartPrompt.jsx`, an explicit "click to start" gate (old-school arcade styled: full `--ink` screen, one blinking `--bone` line, "Click, tap, or press any key" hint) shown before `PresentsCard`. This exists to finally close out the sound-on-load saga from Phase 7/mechanic 1's status row below — repeated attempts to catch an *incidental* click early enough in beats 1–2 kept falling short ("I can't hear any sound... UNTIL I click on the website which is poor"), because a browser will not play audio before a real gesture and there usually wasn't one that early. This screen asks for the click up front instead of hoping for one. It's a deliberate, narrow exception to spec §2 rule 1 ("no forced interaction") — justified because it serves that hard technical requirement rather than gating content, it's one click/tap/key, not a sequence of choices, and `/plain` remains a separate, ungated route reachable directly. Unlike beats 1–2, it does NOT auto-skip under reduced motion (reduced motion turns off its blink, not the gate). Verified live end-to-end: `navigator.userActivation.hasBeenActive` is `false` while it shows and flips `true` on a keypress, which (confirmed via a patched `createOscillator`) results in the signature sting's full 5-voice chord actually firing once beat 2 is reached. See `src/components/CLAUDE.md` and `src/screens/CLAUDE.md`. **Round 8 (this batch) — three direct live-feedback fixes, all in `MainMenu.jsx`/`ToastProvider.jsx`, none touching the beat sequence itself:** (1) the EXTRAS item is relabeled **PLAIN RESUME** and the now-redundant orientation line explaining what EXTRAS meant is removed — the label itself says what it is now. (2) The OPTIONS toggles (Motion/Sound/Typewriter) switched from the body font (Inter) to the display font (Alfa Slab One), matching every other item in the same menu — reported live as making the screen "look out of touch," since a plain grotesque all-caps settings row sitting directly under five bold slab-serif menu items read as a bootstrapped form bolted onto an in-universe menu. (3) The title-screen easter egg's `lateNightWindow` toast is now exempt from `ToastProvider.jsx`'s 8-toasts-per-session cap, fixing a live-reported bug where the egg would stop responding to clicks ("run out of clicks") once ordinary toasts (episode opens, panel expands, choices, PDF downloads) had already used up the shared session budget — the egg is a repeatable discoverable secret, not a one-shot beat, so it now always answers a click regardless of what else has toasted that session. See `src/components/CLAUDE.md` for full detail on all three. **Round 9 (this batch) — `StartPrompt.jsx` rebuilt** on direct feedback that beat 0 was "waaay too small." It now leads with an identity block above the CTA — an "An Interactive Resume" eyebrow tag, the name/headline read from `src/data/profile.js` (same source as `MainMenu.jsx`'s identity block), and a `--signal` divider — so the very first screen hooks with who this is before asking for the click, rather than two bare lines in a black void. **A real overflow bug was caught and fixed during verification, not just a size bump:** this project's `tokens.css` overrides Tailwind's `text-xs`–`text-3xl` scale with its own larger values (`text-3xl` = 64px), and the CTA's first draft (`text-3xl sm:text-4xl`) rendered a 64px non-wrapping line 187px past the edge of a 375px mobile viewport, confirmed via `getBoundingClientRect()` — the flex column's `items-center` never constrains child width, so nothing forced a wrap. Fixed by measuring actual rendered text widths at each breakpoint and landing on `text-lg sm:text-xl md:text-2xl xl:text-3xl` with a `max-w-[260px]` mobile cap so it degrades to a safe wrap rather than depending on a pixel-exact fit. Verified live at 375px and 1280px viewports with no horizontal overflow. See `src/components/CLAUDE.md` for full detail. **Round 10 (this batch) — live-reported bug fixed: Up/Down arrow keys didn't reach OPTIONS' toggles once the panel was expanded.** `MainMenu.jsx`'s roving-focus `selected` index only ever ran over the 5 top-level items (NEW GAME/CONTINUE/EPISODES/PLAIN RESUME/OPTIONS) regardless of whether OPTIONS was open, so arrow keys kept cycling those same 5 items instead of "expanding into" the Motion/Sound/Music/Track/Typewriter rows underneath. Fixed by combining the top items with the OPTIONS toggles into one list (`navItems`) whenever the panel is open, so Down from OPTIONS now walks into Motion → Sound → Music → Previous/Next Track → Typewriter → wraps back to NEW GAME, and closing the panel snaps selection cleanly back to OPTIONS rather than leaving a stale index. Verified live via dispatched keydown events (screenshots unavailable in this session's sandboxed Browser pane, same known limitation noted in Phase 6). See `src/components/CLAUDE.md` for full detail. |
| 4 | Static screens (chapter select, one episode view, dummy data) | ✅ All four screens built: `ChapterSelect.jsx`, `EpisodeView.jsx` (all 5 episodes, not just one), `TitleScreen.jsx`, `PlainResume.jsx`. Dummy data from `episodes.js` throughout, no hardcoded resume copy. **`ChapterSelect.jsx` rebuilt this batch:** the user supplied a live screenshot of their own reference Telltale game's episode-select screen (a large "now showing" panel with synopsis + a Start button, and a filmstrip of episode tiles underneath) and asked for that exact structure. The plain 1/2/3-column responsive grid is gone, replaced by a hero `Panel` (currently-selected episode's title/subtitle/`choicePrompt`/visited-status plus a `--signal`-filled "▸ START EPISODE N" `Link`, the only control that navigates) sitting above a horizontally-scrolling filmstrip of `Panel`-as-`button` tiles that update the hero's `selectedId` on click without navigating — mirroring the reference's "browse, then start" two-step exactly. Defaults to `useProgress().lastEpisodeId` (resume-where-you-left-off) or episode 1. Still spec §4.2 compliant: nothing locked, visited `--signal` corner marks preserved, silhouette cover fallback preserved, fully keyboard-reachable (Tab order reaches every tile and the Start link, which updates its `href` live as selection changes), mobile collapses the hero to one column and the Start button to full width while the filmstrip stays a horizontal scroller at every breakpoint with no page-level horizontal overflow. Verified live in-browser (selection swap, navigation, mobile geometry) plus `npm run lint` and `npm run build`. See `src/screens/CLAUDE.md` for full detail. **Rebuilt AGAIN this batch — everything above about this screen's palette, art, interaction model and copy priority is superseded.** Direct user feedback on a live screenshot: "WAYY too basic," and more usefully, "the theme and the vibe of the MENU is extremely different from this screen... it appears as we are changing the whole THEME quite quickly." That was the actual defect. A visitor arrived from a dark parallaxing skyline and landed on a cream document with a red button, so `PageTransition.jsx`'s camera dolly read as the site swapping themes mid-click rather than as movement through one place. **The organizing idea now is that `/chapters` IS the same city, seen from further back, and each episode is a room in it with the light on.** `TitleBackdrop.jsx` renders behind the whole screen in a new `distant` variant (far + mid layers only, no pointer parallax, heavier scrim — also two thirds fewer `<rect>`s, which matters because the dolly has to zoom whichever screen is incoming); `EpisodeWindow.jsx` (new) draws each episode as a window with a silhouette interior, keyed by a new `scene` field on each episode; `Panel.jsx` gained a `tone="night"` surface because an ink border on an ink ground is invisible. **Visited state is the window being LIT** rather than a `--signal` corner triangle — which is also how the screen got back inside spec §3.1's "max 3 uses of `--signal`" budget: it was spending eight (a Start slab, six corner marks, a link glyph), it now spends exactly two, verified by walking every element under `<main>` in the live page and matching computed colors against `rgb(196,50,30)`. **Tiles navigate on click and preview on hover/focus**, retiring the two-step browse-then-start that the reference has because it is a controller UI on a television; with a mouse it was a wasted click on the primary path, which the 90-second rule cannot afford. Three defects reported in the same round are fixed by the new layout rather than patched into the old one: titles no longer truncate mid-word (tiles are 272px at `lg`, labels wrap, verified `scrollWidth <= clientWidth` on all five at 1280px and 375px), the `/plain` escape hatch moved from a footer-styled link at the bottom of the page into the header opposite "← Title" alongside a real `Season 1 · N of 5 viewed` progress line, and the dead space is gone (hero bleeds past the right margin, filmstrip overruns the frame — measured 1424px of content in a 1225px scroller, so the last tile is clipped by the viewport edge on purpose). Also deliberately avoided, per the user's "no signs of Vibe Coded Design" constraint and listed so nobody re-adds them as polish: rounded corners, blurred shadows, a symmetric centred hero, a gradient-masked filmstrip, an icon set. Verified live at 1280px and 375px with no page-level horizontal overflow at either; `npm run lint` clean, `npm run build` clean. **Three live-reported defects fixed this batch, all in this screen.** (1) **The page resized as you hovered across tiles** ("the menu is of different size per chapters and thus, the website changes size per chapter hovering over it"). Every variable-height element in the hero now reserves its maximum height instead of sizing to its own content: the title reserves two lines, the contents line two, the tag row three chip-rows on a 375px column and two from `sm` up, and the meta line two lines on mobile (the trailing "· Viewed" only exists for visited episodes and is exactly what tipped it over on a narrow column). The hero grid also has a `lg:min-h-[360px]` floor. Reserving space is the fix rather than clamping, because truncation is the defect this screen was just rebuilt to remove. The reservations were measured per episode at each width rather than guessed — episode 4’s tags are the longest and set the floor. Verified by cycling the hover through all five episodes at 375px, 640px, 768px and 1280px and recording `document.documentElement.scrollHeight` at each step: one unique value per width (1169 / 1194 / 1162 / 948), where before it moved across a 25px range on mobile and 16px on desktop. (2) **The filmstrip’s native scrollbar is hidden** ("the scroll wheel below the chapters looks bad") via a new `.no-scrollbar` utility in `../index.css` — it read as a stray browser control bolted under the art. It is replaced by better affordances rather than by none: ◂ ▸ stepper buttons sit in the "EPISODES" heading row with an `◂ ▸ to browse` hint, and the row still scrolls by wheel, trackpad and touch. (3) **Left/Right arrow keys now move between episodes**, wrapping at both ends, and scroll the newly selected tile to the centre of the strip. This forced a state simplification that was overdue: selection is now ONE value driven by hover, focus, the steppers and the keyboard alike. The previous build split it into a committed id plus a transient hover preview that reset on mouse-out, which cannot coexist with arrow keys — a keyboard move would be silently discarded the next time the cursor left a tile. `scrollIntoView` is `behavior: 'auto'` under reduced motion and `'smooth'` otherwise, with `block: 'nearest'` so centring a tile horizontally never scrolls the page vertically (verified: `window.scrollY` unchanged). Verified live: five ArrowRight presses walk EDUCATION → … → PROJECTS → wrap, ArrowLeft steps back, both stepper buttons move the selection, `aria-current="true"` follows it, the strip reports zero scrollbar height, and `scrollIntoView` moves the row to its exact maximum offset (199.2 of 199) and back. The smooth-scroll variant could not be observed animating here — this environment’s Browser pane is not composited, so rAF-driven scrolling is frozen, the same known limitation documented against Phase 6 — but the `'auto'` path proves the mechanism and the geometry. **Still open and NOT addressed this batch: `EpisodeWindow.jsx`’s art**, which the user reports as "not that well done." A redesign is on hold pending a reference screenshot of the Batman: A Telltale Series episode menu, which the user has asked this screen to take heavy inspiration from — the previous rebuild of this screen only landed because an actual screenshot was supplied rather than a description, and guessing at a reference is what produced the art being complained about. **Rebuilt AGAIN this batch, against an actual screenshot of the Batman: A Telltale Series episode menu that the user supplied as the reference to take "MAJOR and HEAVY inspiration" from. Everything above about this screen’s layout is superseded.** What was taken from the reference, in order of how much it changes the screen: the selected episode’s key art is **full-bleed** — the background of the entire screen, with no panel, border or shadow around it — and the text sits directly on it, top-left, over a left-to-right scrim that exists only to hold contrast; the hero text order is the reference’s exactly (small "Episode N", title large in the accent color, small all-caps status line, description, then the button); the Start button is a **hollow outlined rectangle**, not a filled slab; the filmstrip sits at the bottom with labels **below** each thumbnail, the selected one bright with a light border and the rest dimmed; unvisited episodes carry a small corner ribbon the way the reference tags unplayed ones with "NEW!"; a season line with circular ◂ ▸ steppers sits above the strip, and a row of secondary actions ("◂ Previous menu", "▤ Plain resume") below it — which is a better home for both than either had before. **What was deliberately NOT taken: the reference’s cyan grade and its light sans-serif.** Those would have reintroduced exactly the theme jump this screen was rebuilt to remove two batches ago. Palette stays `--ink`/`--dusk` dark and `--bone`/`--paper` light, type stays Alfa Slab One, accent stays `--signal`. **`Panel.jsx` and `RevealPanel.jsx` are no longer used on this screen** — a deliberate call, not an oversight, and the one worth pushing back on if you disagree: the comic-panel primitive is the language of the CONTENT layer and belongs on `/chapters/:id` where panels are literally comic panels; hanging paper-panel chrome on full-bleed cinematic art fought the reference on every axis. `RevealPanel` went with it because this layout no longer scrolls at desktop sizes, so a scroll-triggered reveal had nothing to trigger on and its parallax transform was only a source of overflow. Moving the `--signal` spend from a filled Start slab to the episode title dropped the screen to **exactly one** accent use, verified live. **A real bug was caught while measuring this, unrelated to the redesign brief:** the screen used `min-h-full`, and `App.jsx` wraps every screen in `PageTransition.jsx`, which is `display: contents` at rest — an element that generates no box, so a percentage `min-height` has nothing to resolve against and collapses to `auto`. This screen is the first one short enough for that to show: `<main>` measured 737px inside an 812px viewport, leaving a **75px band of the cream `--paper` body background** exposed below the artwork. Fixed with `min-h-screen`, which skips the containing-block chain entirely; other screens have the same latent issue but enough content to hide it. Verified live after the fix at 375×812, 1280×800 and 1280×600: `<main>` fills the viewport exactly with a zero-pixel gap below it, no horizontal overflow at any size, and hovering across all five tiles produces a single unique `(scrollHeight, Start-button top)` pair per width — i.e. the layout-shift defect stays fixed under the new layout. At viewport heights below ~727px the page scrolls rather than cramming, which is the intended fallback. Arrow keys, both steppers, `aria-current`, and the hidden scrollbar were all re-verified after the rebuild. Blur filters on the whole screen dropped to **one** (hero only; tiles render at `detail="low"`, see `../components/CLAUDE.md`’s `EpisodeArt.jsx` row) against ~18 for a naive full-detail approach, which matters because `PageTransition.jsx` has to zoom this screen. `npm run lint` clean, `npm run build` clean at 141.6 KB gzipped. **This batch — `EpisodeArt.jsx` rebuilt a third time**, on the report that the art "isn't meeting the style requirements." The screen's layout is untouched; only the art inside it changed. A live screenshot turned that into five concrete defects, and the biggest one was not that the art was under-drawn: **all five scenes were rendering identically because each scene's distinguishing props were filled `var(--ink)` on an `--ink` background and were therefore invisible** — only episode 2's monitor and episode 4's clock ever read, being the only props filled with the light gradient. The other four: the figure was a symmetric no-neck no-arms snowman; there was no rim light, so a 100%-ink silhouette against a glow read as a hole punched in the frame rather than a person in front of a window; there was no texture at all, which is the actual style miss, since every other surface in this app is deliberately printed and distressed (`Panel.jsx`'s torn clip-path, `TitleReveal.jsx`'s `feTurbulence` lettering, the halftone and grain in `textures.css`) and this was the one rendered in ruler-straight vector, so it read as a diagram; and the camera was identical in all five, the same window rectangle at the same coordinates every episode. Fixes, in order: a four-value **tonal ladder** so scene parts separate from the ground; hand-authored figure paths with neck, sloping shoulders, hanging arms and deliberate asymmetry, plus a fourth pose; a **`Rim`** helper that draws every silhouette twice (a `--bone` copy offset toward the window, ink on top) — chosen over a blur-based glow precisely because it is a plain fill that costs nothing and survives being scaled to a 176px tile; **`feTurbulence`+`feDisplacementMap` contour roughening** plus a **halftone-dot light falloff** in place of a smooth gradient ramp; and a per-scene window rectangle, with episode 5 lit from the LEFT. **Two more defects were caught by measurement rather than by eye**, after this environment's Browser pane stopped compositing mid-batch (the same limitation documented against Phase 6) and verification moved to rasterizing each live `<svg>` into a canvas and reading the pixels back: episodes 1 and 3 still scored only **6.8** mean per-cell luminance difference on a 16×9 grid against 15–25 for every other pair, so episode 1's window became wide-and-high rather than tall and its foreground became horizontal bench slabs rather than episode 3's domed heads (weakest pair now **15.9**, range 15.9–32.2); and tiles were flatly unreadable, with 75% of every tile's pixels between luminance 18 and 25 of 255 — three multiplied dimmings stacking (unvisited light, hero-strength vignette, the filmstrip's own CSS dimming), now fixed by a lighter vignette and more light on unvisited tiles specifically (tile p95 brightness 107–150 → 143–182). The `detail="low"` performance rule survived intact and was re-verified: **1 blur and 1 displacement filter on the entire screen**, both on the hero, zero of either in any tile; 425 SVG nodes across all six scenes, up from 341. `--signal` still spent exactly once, no horizontal overflow, `<main>` still exactly fills the viewport. `npm run lint` clean, `npm run build` clean at 144.8 KB gzipped. **The composition itself has not been seen** — no screenshot was obtainable after the rebuild, so it should be eyeballed in a real browser before this is called done. **This batch — two keyboard-nav fixes, direct user reports.** (1) On `ChapterSelect.jsx`, pressing **Enter** while an episode is only hovered/selected (DOM focus still on `<body>`, so nothing native fires) now opens that episode — the screen's window `keydown` listener gained an Enter branch alongside the existing Left/Right, guarded to defer to native activation when a link or button genuinely has focus. (2) New `src/components/EscapeBack.jsx` — a headless handler mounted once in `App.jsx` that makes **ESC** walk one level up the route hierarchy from anywhere: `/chapters/:id` → `/chapters`, `/chapters` → `/`, `/plain` → `/`, `/` a no-op. It is hierarchical, not `navigate(-1)`, so a deep-link/refresh can't escape off-site and it agrees with the on-screen "◂ Previous menu" links. `MainMenu.jsx` also handles ESC itself to close an open OPTIONS panel before the global handler's `/` no-op applies. All five paths verified live; `npm run lint`/`npm run build` clean at 144.98 KB gzipped. |
| 5 | The `/plain` route | ✅ **Rebuilt this batch into "The Dossier"** — a navigable, filterable, on-theme document, replacing a 197-line unstyled single-column dump (the live complaint: "extremely hard to navigate... we have to scroll in all that mess"). Desktop: a sticky left rail (identity block, a 9-row section index with live/filtered counts, Download/print, a back link to `/`) beside a right column. Below `lg`: identity block, then a Download+back row, then the same index as a sticky horizontal chip bar — see `src/screens/CLAUDE.md` for exactly why it's the *same* `<nav>`/`<a>` elements at both breakpoints, not two. A "10-second band" of four stat tiles (new `profile.highlights[]`) sits above `profile.summary`. Six of the nine sections (Experience, Projects, Competitions, Education, Volunteering, Other work — 30 entries total) render as controlled native `<details>` via the new `src/components/DossierEntry.jsx`; three (Skills, Honors & awards, Certifications) render as plain non-collapsible rows since they're already a fast scan. A case-insensitive filter (`<input type="search">`) matches across every field of every entry, auto-expands matches, dims/zeroes out non-matching rail rows, and shows a "Clear filter" empty state when nothing matches — restoring the visitor's own manually-opened set untouched when the filter is cleared. Expand all / Collapse all, and an `IntersectionObserver` scrollspy (re-created whenever the filter changes which sections exist) drive the rail's active marker. `--signal` spent exactly twice — the active rail row and the Download button — verified live via `getComputedStyle` (see this batch's verification report). Print (`window.print()`, still no static PDF asset) expands every entry first and is backstopped by `beforeprint`/`afterprint` listeners plus a CSS-only fallback, so a bare Ctrl+P gets the same treatment as the button; `src/index.css`'s new `@media print` block forces true black-on-white and hides everything classed `.dossier-chrome`. Two data-layer bugs fixed in the same batch, not styling: `episodes.js`'s `competitions` episode was tagged `plainRole: 'projects'`, mixing five hackathon panels into the "Projects" heading — now its own `plainRole: 'competitions'` section (`PLAIN_SECTIONS` gained a second entry); and the top nav's link to `/chapters` was removed per direct instruction ("no EPISODE BS to figure out the Resume"), leaving exactly one back link. Still linked from every other screen (`TitleScreen`/`MainMenu`'s PLAIN RESUME item, `ChapterSelect`'s "▤ Plain resume", `EpisodeView`'s header link) — none of those call sites changed. `npm run lint` clean (no new warnings), `npm run build` clean at 147.08 KB gzipped JS / 8.93 KB gzipped CSS (was ~144.8 KB / ~7.29 KB). See `src/screens/CLAUDE.md`, `src/data/CLAUDE.md`, `src/components/CLAUDE.md`, and this batch's verification report for full detail and measured numbers. **Three defects found in Opus review after that report, all fixed and re-measured live.** The biggest: **the mobile section index was not sticky at all.** `position: sticky` is clamped to the parent box and the `<nav>` sat inside the `<aside>`, which on mobile is a 336px content-sized column on a 5,479px page — measured at scroll y=1500 the nav was at `top: -1177` and off-screen, so the index (the entire point of the rebuild) vanished after ~340px and never came back. It was missed because rail clicks were only exercised at desktop width, where the nav is `lg:static` inside an aside that sticks as one block. Fixed with `contents lg:flex` on the aside: `display: contents` generates no box, so the nav's sticky containing block becomes the full-height page container — measured pinned at `top: 0` at scroll y=2500 (375px) and y=2000 (768px). That fix then required `scroll-mt-16 lg:scroll-mt-6` on every `<section>` (a genuinely-sticky 53px bar would otherwise cover the heading a rail jump scrolls to — now 11px clearance on mobile, exactly 24px on desktop) and `order-4 lg:order-none` on the content column (`display: contents` promotes the aside's ordered children into the same flex container, whose default `order: 0` would have painted the whole resume above the identity block). Third, unrelated: `profile.highlights[0].value` changed `1 / 313` -> `#1` with the denominator moved into the label, because at 375px it wrapped and rendered that tile 107px tall against 53px for the other three. Re-verified after all of it: signal count exactly 2, no horizontal overflow at 375/768/1280px, Expand all 30 / Collapse all 0, `beforeprint` 30 / `afterprint` 0, filter `graph` -> 9 of 45 with the visitor's manual open-set preserved, `npm run lint` clean and `npm run build` clean at 147.12 KB gzipped. **This batch — two live-reported `/plain` fixes plus a content-density pass.** (1) A headshot now sits atop the identity block, driven by a new `profile.photo` filename in `public/` (resolved against `BASE_URL`, frame hidden if the file is missing); `--signal` budget still exactly 2. **Follow-up this batch:** the real color `public/portrait.jpg` replaced the generated placeholder, the `grayscale` class was dropped (it read as washed-out), and the frame grew from 96/80px to 160/144px (`lg:`) — direct user feedback ("in Black and White and wayy too small"). Verified live at 375px and 1280px: `filter: none`, square renders at 160/144px, no horizontal overflow, `--signal` count still exactly 2. (2) The section index no longer overflows at either breakpoint: the mobile chip bar dropped `no-scrollbar` (a 9-chip ~1,540px scroller with the scrollbar hidden read as clipped) and the desktop vertical rail got `lg:whitespace-normal` so "Competitions & hackathons" wraps to two lines inside the 16rem rail instead of running past its edge and shoving the count off — verified live at 375px and 1440px, no page-level horizontal overflow at either, `--signal` still 2. (3) Six terse `profile.js` `detail` one-liners (NaSCon Event Head, TEDx, FCS Buddy Program, Code for Pakistan, Help 4 Help, Mehra Abadi) and the Cricnalize blurb were fleshed to 1–2 sentences of real role scope — no invented roles/dates/numbers/awards, and the deliberately-vague honesty-sensitive entries left as-is. `npm run build` clean at 147.63 KB gzipped JS. |
| 6 | Motion pass (Framer Motion + `useReducedMotion`) | ✅ Done. `useReducedMotion()` replaced the inline `window.matchMedia` check in `TitleScreen.jsx` and `EpisodeView.jsx` (the two files that had it), combined with `useSettings().motion` throughout. `AnimatePresence` used for the "episode transitions" spec §7 names explicitly (`EpisodeView.jsx`'s title-card dismissal) and for the toast (`ToastProvider.jsx`). `RevealPanel.jsx` (new) gives every panel the "fade + 12px rise, staggered 60ms" scroll reveal from spec 3.5, used by both `EpisodeView.jsx` and `ChapterSelect.jsx`. `DialogueChoice.jsx`'s hover bar now genuinely slides in (`scale-x`) rather than only fading color. Reduced motion (OS or Settings) disables all of the above — verified content stays fully visible, never mid-animation, by never branching to a differently-shaped DOM tree between the two states (see `RevealPanel.jsx`'s doc comment for the specific bug this avoids). **This batch — route-change transition added, not spec-mandated:** direct user request for a "parallax effect... when we click NEW GAME or CONTINUE or any other options that open another menu." New `src/components/PageTransition.jsx` wraps every screen: an opaque `--ink` curtain wipes across via `scaleX` (foreground plane) while the screen's own content only fades, offset by a short delay (background plane) — two layers moving at different rates is what reads as "parallax" rather than a flat cross-fade. Wired into `App.jsx` via `AnimatePresence mode="wait"` around a `useOutlet()`-based `AnimatedOutlet` (the four routes are now nested under one layout route so this works — the more commonly documented `<Routes location={location} key={location.pathname}>` pattern was tried first and, in this project's react-router-dom 7 + framer-motion 13 combination, never actually mounted the new matched screen after a navigation; `useOutlet()` is the standard fallback for that failure mode). Content deliberately animates opacity only, never scale/x/y, because several screens (`TitleScreen.jsx`'s beats, `EpisodeView.jsx`'s title card) depend on their own `fixed inset-0` children sizing to the true viewport, and any non-`none` `transform` on an ancestor — even framer-motion's at-rest identity transform — would make that ancestor the containing block for those descendants instead. Respects the Settings motion toggle and `prefers-reduced-motion` (renders `children` with no wrapper at all when either is off, so the swap is instant). **Verification note:** the animated path could not be visually screenshotted in this session — the sandboxed Browser pane used for testing was never composited ("the Browser pane is not displayed"), which appears to stall `requestAnimationFrame`-driven animations indefinitely (real foreground browser tabs run rAF normally and are not expected to hit this). Verified instead via: (a) the reduced-motion/disabled code path, which bypasses `motion`/`AnimatePresence` entirely and was confirmed end-to-end (NEW GAME, CONTINUE, EPISODES all landed correctly and instantly), proving the routing/`useOutlet` wiring itself is correct; (b) `npm run build` succeeding with no new errors; (c) lifecycle logging confirming `AnimatedOutlet` re-renders exactly once per navigation with the correct new pathname. The animated transition itself should be confirmed visually in a real browser before calling this fully done. **Rebuilt this batch on direct follow-up feedback — the curtain wipe is gone.** The ask: "rather than the sliding animation that we have currently... the side options and everything slowly fades away, a WHOOSH slow sound effect as we move into the center of the screen with the buildings moving away and we enter the specific menu and then we have the same effect in reverse if we come back to the main menu." `PageTransition.jsx` is now a **camera dolly**, not a wipe: going deeper the outgoing screen scales 1 → 1.45 and fades (menu items drift outward and dissolve, `TitleBackdrop.jsx`'s skyline grows past the frame edges — that is "the buildings moving away" when the camera is what's moving) while the incoming screen grows in from 0.86; returning to the menu is the exact inverse (outgoing recedes to 0.84, the menu returns from 1.32). An `--ink` veil behind the moving screen crossfades so the beat between the two is a dip to black rather than a flash of bare `--paper`. Direction comes from **route depth**, not history (`/`=0, `/chapters` and `/plain`=1, `/chapters/:id`=2), so the browser Back button and an in-page "← Title" link read identically. Two structural changes came with it: `AnimatedOutlet` now calls the exported `noteRouteChange()` during render (the outgoing screen's props are frozen by `AnimatePresence`, so only a module-level value set before either child renders can tell both halves which way the camera is going) and resets scroll on navigation; and the wrapper now **switches `display`** — `fixed inset-0 overflow-hidden` (exactly viewport-sized) while animating, `display: contents` (no box at all) at rest. That last part is what resolves the `position: fixed` containing-block constraint this row previously called out as the reason content could only animate opacity: a zoom is a transform, so rather than avoid it, the wrapper is only ever a box while its own box IS the viewport, and generates none at all the rest of the time. `html { scrollbar-gutter: stable }` was added in `src/index.css` because a screen going `fixed` empties the document flow and would otherwise take the scrollbar (and the page width) with it mid-transition. **The verification gap noted above is now root-caused rather than just observed:** this environment's Browser pane is never composited, so `document.timeline.currentTime` is pinned at **0** and every Web Animations API animation is frozen. framer-motion accelerates `opacity` through WAAPI while running `scale` on its own rAF frameloop — so with a temporary `requestAnimationFrame` shim the scale ramp animates correctly (measured: 1.002, 1.006, 1.018, 1.028, 1.05, 1.07, 1.11, 1.14 … 1.45, correctly accelerating) and the opacity half never moves, which is why `AnimatePresence mode="wait"` never completes here. The exiting element's WAAPI keyframes were read out of the DOM and are correct (`opacity: ["1", "0"]`). A real browser tab has a live timeline and runs both halves. Also verified: the whoosh fires on a real NEW GAME click with the correct rising sweep, the route-depth table returns the right direction for all seven transitions tested including the idempotent repeat, and the reduced-motion path still renders no wrapper at all. **Then optimized on a live "the website lags a bit whilst doing this" report** — see `src/components/CLAUDE.md` for the full detail. Short version: the zoomed screen is ~1,450 SVG rects plus three Gaussian-blur filters, and vector content re-rasterizes whenever its drawn scale changes, so the wrapper now gets a `.route-stage` compositor-layer promotion (`will-change` + `contain: paint`) while moving, `html.route-moving` pauses decorative CSS animation for the ~1s of the move so that promoted layer stays a static texture, and the peak zoom dropped 1.45 → 1.3. All three verified applying and releasing correctly across real navigations in both directions. |
| 7 | Mechanics 1, 2, 6 (toast, dialogue choices, choice-stats) | 🚧 Mechanic 1 (toast queue) done: `ToastProvider.jsx` + `MemoryToast.jsx` + `toastVariants.js`, now with the spec 3.5 slide-in/fade motion, plus a new `lateNightWindow` pool for the title-screen easter egg. **Restyled again this batch on a live user screenshot of the actual Telltale in-game notification** — the previous flat "HUD caption" bar (two thin rules, centered caps) was itself already a departure from spec but still wasn't what the reference showed. `MemoryToast.jsx` is now a solid `--ink` banner with a hand-torn right edge (static `clip-path` polygon, not seeded per Panel.jsx), a warning-triangle icon, and left-aligned bold sentence-case `--bone` text; `ToastProvider.jsx` moved the toast from bottom-left to flush top-left and changed the enter/exit motion to drop in from above instead of sliding from the side. See `src/components/CLAUDE.md` for the full rationale. Mechanic 2 (`DialogueChoice.jsx`) done — and now actually renders its prompt, fed by the new `choicePrompt` field on every episode. **`Typewriter.jsx` built this batch** (spec 3.5): 22ms/char, skippable by click or Enter, instant under the typewriter/motion toggles or `prefers-reduced-motion`, full text always present for screen readers, untyped remainder held at `opacity-0` so nothing reflows. Scoped to the dialogue prompt only — never panel bodies, which would put a timer in front of resume content (§2 rule 2). **The OPTIONS "Sound" toggle is now real too** — `src/lib/sound.js` synthesizes short UI cues with the Web Audio API (no shipped audio files); menu/choice roving focus plays `navigate`, activating an item plays `select`, and every memory toast plays `toast`. This is separate from spec §5 mechanic 5's "ambient audio" (a background soundscape, one of four optional atmosphere picks, cut earlier as scope creep) — short interaction feedback is what the toggle was always meant to gate, and shipping a toggle with nothing behind it was the actual bug, not the toggle's existence. **This batch:** menu-item hover now also plays `navigate` (previously hover changed selection silently, keyboard-only tick was deliberate — the user asked for hover to make sound too, see `MainMenu.jsx`'s `hoverIndex` helper), and the `toast` cue was retuned darker/less energetic per direct user feedback — lower pitch, glides down instead of up, held slightly longer (`src/lib/sound.js`). **Also this batch:** a one-time synthesized "signature sting" (`sounds.intro`) plays on first landing on the main menu — the user's "Netflix ta-dum" request, adapted dark/moody (a three-note minor motif settling down rather than a bright fanfare) rather than shipping an audio file. Fired from `src/screens/TitleScreen.jsx`, gated by the existing `hasPlayedIntro` session flag so it plays once, not on every return to `/`. **Retuned louder immediately after** ("that sound is a bit TOO small... make it louder and proper") — gains roughly 2.5x, and the closing note is now a real three-layer chord (sub-bass root, glide-down root, quiet octave-up shimmer) instead of one thin sine tone, since layering rather than raw volume is what makes a synth hit read as "proper." **Then retimed on two more live notes** — "the sound doesn't play on reload" and "play it the moment the second loading screen comes and stop when the menu loads." `intro` now starts on beat 2 (`TitleReveal`) instead of beat 3, and is actively cut off via a new `stop()` function it returns (rather than only ever finishing on its own) the instant the beat moves on to `MainMenu`, whether that's `TitleReveal`'s hold timing out or the visitor skipping it. The "doesn't play on reload" report turned out to be correct browser autoplay-policy behavior, not a bug — beats 1–2 auto-advance with no interaction required, so a visitor who never clicks/taps/presses a key during them never produces the user gesture a browser requires before any `AudioContext` can play audio; skipping a beat (already supported) supplies that gesture. See `src/lib/CLAUDE.md` and `src/screens/CLAUDE.md` for the full mechanism. **Retuned louder a second time** ("EXTREMELY LOW... can't hear it at 100 percent volume without headphones") — roughly another 3x on top of the previous pass, with the simultaneous three-voice chord layer specifically budgeted (not just multiplied blindly) so the peaks sum to ~0.95 rather than clipping past Web Audio's 1.0 ceiling (`src/lib/sound.js`). **A real bug also surfaced under that same report** ("I still can't hear shit until I CLICK"): the beat-2 effect was setting the one-shot `hasPlayedIntro` flag and attempting playback unconditionally, even with zero gesture yet, so the attempt failed silently (correct autoplay behavior) but burned the only shot — a later click could never trigger it again. New `hasHadUserGesture()` export (wraps `navigator.userActivation`) lets `TitleScreen.jsx` check first and, if no gesture has happened yet, wait for the first real `pointerdown`/`keydown` before playing instead of firing blind. **The saga finally closed this batch:** a further live report ("I can't hear any sound... UNTIL I click on the website which is poor") made clear that catching an incidental click was never going to be reliable — there usually wasn't one early enough. Rather than another retiming attempt, this batch adds Phase 3's new Beat 0 (`StartPrompt.jsx`, see that phase row above) — an explicit "click to start" gate before anything else in the sequence, so the required gesture is guaranteed rather than hoped for. Verified live end-to-end this time: the gesture flag is confirmed `false` while the gate shows and `true` immediately after a keypress on it, and the signature sting's full 5-voice chord is confirmed to actually fire once beat 2 is subsequently reached. Mechanic 6 (`ChoiceStats.jsx`) not started. **This batch — a new main-menu "radio," direct user request, not part of mechanic 5 or any other spec mechanic:** `src/components/BackgroundMusic.jsx`, mounted only by `MainMenu.jsx` so it plays only while the menu is on screen (user explicitly scoped this to the menu for now, not the whole site) and stops the instant a visitor navigates away. Tracks come from `src/lib/musicPlaylist.js`, which auto-discovers whatever MP3/OGG files exist in the new (currently empty) `src/assets/audio/` folder via `import.meta.glob` — no manifest to edit, and the component renders nothing at all (no player, no badge) until real tracks are dropped in, same placeholder-graceful-degradation pattern as `TITLE_ART_URL`. The user asked to source the actual audio from a specific YouTube video; that was declined (downloading/ripping YouTube audio isn't something this assistant does — copyright/ToS reasons) in favor of pointing them to royalty-free sources (Pixabay Music, Mixkit) they can license and drop in themselves. Plays as a rotating playlist (auto-advances on `ended`, loops the set; a genuine single track uses `<audio loop>` directly instead) with a ~600ms volume fade-in per track to avoid pops on auto-advance. A "Currently Playing" badge, fixed top-right with a small pulsing three-bar equalizer glyph (new `eq-bar` keyframe, `src/styles/motion.css`, reduced-motion gated), doubles as a real `<button>` that skips to the next track. Gated by a new, independent `music` Settings toggle (`OPTIONS`, defaults `true`, same autoplay rationale as `sound` — see `src/context/CLAUDE.md`) rather than reusing `sound`, since UI blips and a music bed are separate preferences. Autoplay is still gesture-gated by the platform the same way every other cue in this app is — `StartPrompt.jsx`'s Beat 0 already guarantees a real gesture before the menu can render. **Follow-up requests, same batch:** the user asked for the actual audio (initially pointing at a specific YouTube Music playlist URL — declined, same copyright/ToS reasoning as the original ripping request, since embedding even via YouTube's own Player API was judged a worse architectural fit than self-hosting for this app: external dependency, ad risk, fragility, contradicts the "no external requests" design this whole app otherwise follows). Instead: two tracks ("Sweet September" by Arulo, "Sleepy Cat" by Alejandro Magaña) were fetched directly from Mixkit's public CDN with explicit in-chat permission; twelve more were then dropped into `src/assets/audio/` directly by the user mid-session and picked up automatically with zero code changes, proving out the auto-discovery design. Two more direct follow-ups landed after that: (1) the badge relabeled from "Currently Playing" to the originally-requested **"Now Playing"**, now showing song title in the display face plus an artist/source line (`src/lib/musicPlaylist.js` gained artist/album/source metadata and a filename parser supporting both the original `NN-title.mp3` convention and the `Artist - Title.mp3` convention the user's own files use — a real title-casing bug on contractions, e.g. `Can't` → `Can'T`, was caught and fixed in the same pass); (2) playback is now **shuffled, not sequential** — the opening track is randomized on mount and every advance (auto or manual-skip) picks a new random track excluding whichever is currently playing, so it never starts from or repeats the same fixed track. See `src/components/CLAUDE.md`, `src/lib/CLAUDE.md`, and `src/assets/CLAUDE.md` for full detail, including verification notes (live in-browser for the original build, Node unit checks for the shuffle/title-case fixes after the sandboxed Browser pane's compositor became unavailable again mid-batch). **Separately, the sound toggle now defaults ON**, per direct user instruction ("the sound of the website should be on, always when a new user comes... they can turn it off if they want"): `SettingsContext.jsx`'s `DEFAULTS.sound` flipped `false` → `true`. This supersedes the "must default false — never autoplay" language from earlier batches, which described spec mechanic 5's cut ambient-soundscape feature, not these interaction cues; real autoplay is still impossible regardless of this flag, since `AudioContext` still requires a genuine browser gesture, and Beat 0 (`StartPrompt.jsx`) already guarantees one occurs before beat 1 renders. See `src/context/CLAUDE.md` and `src/lib/CLAUDE.md`. **This batch — two direct user reports, both fixed:** (1) the toast read as smaller than the top-left corner it occupied and wanted to sit further from the edge — `ToastProvider.jsx`'s wrapper moved from flush `left-0 top-0` to `left-4 top-4` (`left-8 top-6` at `sm:`) and both it and `MemoryToast.jsx` grew (max-width `sm`→`md`/`md`→`lg`, more padding, larger icon and text). (2) `sounds.intro` felt too short — lengthened from ~0.9s to ~1.5s by extending the held closing chord's duration/delay, not by adding notes, still comfortably inside `TitleReveal.jsx`'s 1.8s hold so it still finishes naturally when the beat isn't skipped. See `src/components/CLAUDE.md` and `src/lib/CLAUDE.md`. **This batch — three more live-reported `MemoryToast.jsx` glitches, all fixed:** text rendering outside the box (root-caused to a notch cut deeper than the padding, not an actual overflow — fixed by capping notch depth and widening padding), the torn "claw" edge existing on the right only (mirrored onto the left), and the top edge still being a flat straight line (now jagged like both sides, plus a `--shadow`→`--ink` gradient background so the top reads as rough/painted, not just geometrically cut). See `src/components/CLAUDE.md` for the full detail. **This batch — the title-screen easter egg's `lateNightWindow` toast is now exempt from the 8-toast session cap**, fixing a live report that the egg would stop responding ("run out of clicks") once ordinary toasts (episode opens, panel expands, choices, PDF downloads) had already used up the shared session budget; every other trigger still respects the cap. `MainMenu.jsx`'s EXTRAS item is also relabeled **PLAIN RESUME** (the now-redundant explanatory line beneath it removed) and its OPTIONS toggles switched from the body font to the display font to match the rest of the menu, per live feedback that the settings row looked out of place. See `src/components/CLAUDE.md` for full detail on all three. **This batch — the sound-loudness saga's real fix, not a fourth gain bump:** a further live report ("if I am not wearing headphones, the intro sound is not hearable at all, nor the menu sound, nor the notification sound — it's wayy too low") made clear that repeatedly raising individual cues' `gain` values had run out of road — every voice connected straight to `audioCtx.destination`, which hard-clips past 1.0, so `intro`'s chord (already tuned to sum to ~0.95) was right at that ceiling with nowhere left to go. `src/lib/sound.js` now routes every voice through a `getMasterInput()` master chain (compressor → 3.2x makeup gain → brickwall limiter → destination) instead of straight to destination, which is what actually buys headroom to get louder without distortion; `navigate`/`select`/`toast` also got a smaller supporting gain bump now that the chain can safely carry it. Verified live by dynamically importing the module in the running dev page and confirming the graph (two compressor nodes, the 3.2 makeup gain, all four cues firing without error). See `src/lib/CLAUDE.md` for the full mechanism. **This batch — a fifth and sixth cue, `whooshIn`/`whooshOut`**, the route-change camera sound for Phase 6's rebuilt dolly transition ("a WHOOSH slow sound effect as we move into the center of the screen"). First cue here that isn't a `blip()`: a whoosh is broadband air, not a pitch, so it's looped white noise (buffer generated once, still zero shipped bytes) through a bandpass sweeping 200 → 2000 Hz going in and 2000 → 200 coming out, with a quiet sine sub gliding the same direction underneath for weight. Split into two names rather than one cue taking an argument so `useSound()` keeps its single-string API. Deliberately restrained in gain — it fires on every navigation, so it has to sit under the motion rather than announce itself, unlike `intro`'s one-time signature exception. Verified live: patched `createBiquadFilter` in the running page and confirmed `whooshIn` sweeps 200 → 2000 and `whooshOut` 2000 → 200, plus a real NEW GAME click firing the rising sweep unprompted. **This batch — `MemoryToast.jsx`'s torn-edge clip-path rebuilt again** on direct live feedback that it was "poorly designed": the previous polygon cut 6–9% of the box's own width/height on every edge in a tight, evenly-repeating sawtooth, which at this component's size read as a flame/thorn outline rather than a torn-paper edge, and was 5–8x heavier than `Panel.jsx`'s own 2–6px-per-corner jitter that every other irregular shape in the app follows. Replaced with one shallow 4–6px bite per edge (fixed px depth, matching Panel.jsx's scale), plus a small `--shadow` icon badge and a hairline inset border for legibility against dark backdrops. **Then rebuilt again the same batch** on a follow-up correction: the ask was never about the border being hard-cut vs. restrained — it was that the border should *fade*, the ink color dissolving into a barren/decayed scene rather than being cut out of it. `clip-path` (of any kind) is gone; the edge is now an SVG `<mask>` — a `radialGradient` in `objectBoundingBox` units (auto-matches the box's own aspect ratio) run through the same `feTurbulence`+`feDisplacementMap` technique `TitleReveal.jsx` uses for its distressed lettering, so the fade comes out ragged/eroded rather than a clean vignette, applied only to the background layer so text/icon stay fully legible. A real bug surfaced mid-build (the gradient's coordinate frame was accidentally sized to the filter's overscanned region instead of the box's true dimensions, so nothing visibly faded) and was caught by rasterizing the live mask to a canvas and sampling pixel alpha rather than trusting the markup alone. See `src/components/CLAUDE.md` for full detail. **This batch — a second `musicPlaylist.js` title-display bug fixed, direct report that some track titles "messed up the view":** the earlier contraction fix (`Can't` → `Can'T`) only ever capitalized a word's first letter and never touched the rest, so a filename that was itself all-caps — `Supapao - HEAVYLIGHT.mp3`, `AERØHEAD - Path Of The Fireflies.mp3` — still rendered shouting (`HEAVYLIGHT`, `AERØHEAD`) in the "Now Playing" badge, inconsistent with every other properly title-cased track. `titleCase()` now lowercases a word first only when the whole word is uppercase, then capitalizes its first character, leaving a partly-capitalized word (e.g. the `Artificial.Music` artist credit) untouched since there's no way to tell a deliberate stylization from a mistake there. Verified live via HMR against the running dev server's actual playlist output. See `src/lib/CLAUDE.md` for the full fix. **This batch — the radio's scope corrected, direct user follow-up:** "scoped to the menu only" turned out to mean narrower than intended once `/plain` existed — `BackgroundMusic.jsx` used to be mounted by `MainMenu.jsx` (start-on-mount, stop-on-unmount), so it stopped on ANY navigation away from the menu, including into `/plain`. The actual ask: keep the radio playing across menu-ish navigation and stop specifically on entering `/chapters` or `/chapters/:id`. `BackgroundMusic.jsx` now mounts once in `src/App.jsx`, outside the routed screens, so it never unmounts on navigation; a new `src/context/MusicContext.jsx` (`useMusic()`, a one-way `started` session flag) replaces "is this component mounted" as the never-play-during-beats-0-2 gate, flipped by `TitleScreen.jsx` the moment its beat sequence reaches MENU, and the component checks `useLocation().pathname.startsWith('/chapters')` internally to pause (not unmount) on those two routes, so playback resumes from the same position rather than restarting. Verified live: track paused mid-song entering `/chapters`, stayed paused at the same position through `/chapters/:id`, resumed and kept advancing on navigating on to `/plain` or back to `/`. See `src/CLAUDE.md`, `src/context/CLAUDE.md`, and `src/components/CLAUDE.md` for full detail. **This batch — Previous/Next Track controls added to OPTIONS**, direct user request ("Add Option so that we can change music from the OPTIONS... going next music and previous music"): the "Now Playing" badge could only skip forward, and only from wherever it happened to be rendered. Track selection (which index is current, plus `next()`/`previous()`) moved out of `BackgroundMusic.jsx`'s local state and into `src/context/MusicContext.jsx` as a `{history, pointer}` shuffle-with-history stack — `next()` replays forward through existing history if `previous()` was used earlier, otherwise rolls a new random track excluding the current one; `previous()` steps back, a no-op at the start of history. This is what lets `MainMenu.jsx`'s OPTIONS panel drive the same track the radio is actually playing, rather than needing its own separate notion of "current track." Two new buttons, `◂ Previous Track` / `Next Track ▸`, sit directly under the Music toggle, rendered only when the playlist has more than one track and with Previous individually disabled at the start of history. Verified live via the accessibility tree (Browser pane compositor unavailable for screenshots, same known limitation noted elsewhere in this table): Next Track swapped the badge from "Herbal Tea" to "Lullaby," Previous Track swapped it back, and `Previous Track` reported `disabled: true` once there was nowhere further back to go. `npm run lint`/`npm run build` both clean, bundle size unchanged. See `src/context/CLAUDE.md` and `src/components/CLAUDE.md` for full detail. **This batch — the shuffle itself was replaced**, on a live report that "when I go next track, sometimes the same track comes back after 2-3 tracks." That was correct behavior for the algorithm, not a stray bug: `next()` rolled `Math.random()` excluding only the current track, and a memoryless pick over 14 tracks repeats inside a four-track window more than 20% of the time — so the fix is a different algorithm rather than a tighter exclusion. `MusicContext.jsx` now draws from a **shuffled bag** (Fisher-Yates permutation of every index, consumed one per `next()`, rebuilt only when empty), which guarantees every track plays once before any plays twice. A bag alone still leaves a seam — a track at the end of one bag can reappear near the start of the next, which is the reported symptom — so `refillBag()` takes the tail of what just played and builds the new bag position by position, skipping anything inside a no-repeat window of half the playlist (7 of 14), with a terminating fallback rather than rejection sampling. Half, not `length - 1`, because forbidding every recent track determines the next bag's order outright and collapses shuffle into a fixed rotation. Verified by extracting the real helpers and driving them over 150,000 simulated plays: smallest gap between two plays of the same track is **8** (was **2**), zero back-to-back repeats, per-track share 7.13–7.16% against an even 7.14%, Previous-then-Next replays history identically, and 1/2/3-track playlists behave sanely. `npm run lint`/`npm run build` clean. See `src/context/CLAUDE.md`. **This batch — the radio's route scope removed entirely, direct user request:** "The New Game Menu and the Episodes menu should have the music from the main menu kept on playing alike it is in the Plain Resume Setting." Both NEW GAME and EPISODES navigate to `/chapters`, which was precisely the route `BackgroundMusic.jsx` paused on, so the music bed cut every time a visitor entered the game surface. This is the third and final widening of the same request — menu-only → menu + `/plain` → site-wide — and it simplifies rather than adds: `useLocation()` and the `inChapters` check are gone from `BackgroundMusic.jsx` and `shouldPlay` is now just `music && started`. Because playback no longer depends on the pathname, navigation doesn't touch the `<audio>` element at all, so a track plays straight through a route change instead of pausing and resuming at the same offset. Nothing on `/chapters` or `/chapters/:id` plays audio of its own, so there is no conflict to duck for, and the OPTIONS `music` toggle remains the way to silence it. The "Now Playing" badge now shows on those screens too, which is fine on both: `ChapterSelect.jsx` keeps its hero text top-LEFT so the fixed top-right badge has clear space, and on `EpisodeView.jsx` it sits above the `z-40` title card (badge is `z-50`), consistent with it being a persistent HUD everywhere else. Verified live against the running dev server: `currentTime` ran 1.81s → 13.86s across a real NEW GAME click on the same track file with `paused: false` throughout (no pause, no restart), and on `/chapters/education` an 11-sample 1s-interval probe showed `currentTime` advancing 13.57 → 23.57, never paused, one unchanged track. `npm run lint` clean (pre-existing warnings in other files only), `npm run build` clean at 144.77 KB gzipped — unchanged, since this removed code rather than adding any. See `src/components/CLAUDE.md`, `src/CLAUDE.md` and `src/context/CLAUDE.md`, whose rows describing the old pause gate are corrected. **This batch — the "Now Playing" badge now auto-collapses**, direct live report that the top-right overlay "comes over the data... as soon as you enter into the menus of the game or the game itself." Now that the radio plays site-wide (previous change), the full ~199px card sat permanently over the top-right corner of every screen — `Floor.jsx`'s header links, `EpisodeView.jsx`'s masthead "Plain resume" link, `Play.jsx`'s record column. `BackgroundMusic.jsx` now pops the card open for `COLLAPSE_MS` (4.5s) on each genuine track change (first play, auto-advance, skip) then shrinks it to a ~31px equaliser-only puck; hover/focus re-expands it (and restarts the timer on leave/blur), and a click on the collapsed puck expands it rather than skipping. Entering a menu doesn't restart playback, so navigation never re-expands it — it only ever opens when the track actually changes. Verified live end-to-end on the running dev server: expanded 199px on track start → 31px puck after 4.5s → 199px on `focus` → 31px again after `blur`, `aria-label` swapping between "Click to skip to the next track" and "Click to show track details." `npm run lint`/`npm run build` clean, bundle size unchanged. |
| 8 | Mechanics 3, 4 (confidence meter, branching moment) | 🚧 Data/logic for both exists (`ProgressContext.jsx` computes confidence; `episodes.js` `build` episode carries the `branch` object) but no UI component renders either yet — `ConfidenceMeter.jsx` not started. |
| 9 | Polish pass (2 of 4 ambient details, then remove one) | 🚧 Two ambient details chosen and built: film grain (`src/styles/textures.css` `.texture-grain-animate`, mounted by `src/components/AmbientLayer.jsx`) and slow panel-background parallax, max 8px (`src/components/RevealPanel.jsx`). Ambient audio and the crosshair cursor deliberately not built, per spec's "pick two." The "then remove one" step of this phase has not been done — that is a content/design judgment call, flagged for Opus, not made unilaterally here. |
| 10 | Content swap (real copy, real title art) | 🚧 **Copy is DONE, art is not.** Every episode title/subtitle, panel, choice label and the mechanic-4 branch carry real content from the user's master resume — no placeholder prose remains in `episodes.js`. Bodies verified inside spec §6's 30–60 word band and headers inside the 2–5 word band, so nothing reflows. **This batch: `episodes.js` reorganized from chronology to category** on direct user feedback that episodes read as "going through my life" rather than as a portfolio — now `education`, `academia` (TA/RA/research), `guild` (leadership & volunteering), `sidequests` (hackathons), `build` (projects), 17 panels total (up from 16, a direct consequence of the regroup, not a target). See `src/data/CLAUDE.md` for the full per-episode breakdown and the `PLAIN_SECTIONS`/`profile.js` follow-on change this required. `src/data/profile.js` carries everything real that did not earn a panel (contact, education, full experience, skills, awards, other projects, volunteering, certifications) and `/plain` was rebuilt around it. Still outstanding: real title art and 5 cover images (`src/assets/title-art/` and `src/assets/covers/` remain empty), plus four facts deliberately left out because the master resume flags them as unverified — listed in `src/data/CLAUDE.md`. **This batch — the copy was re-cut again and the cover placeholder was replaced with real drawn art.** Episode titles are now PLAIN CATEGORY LABELS with the flavor titles removed outright, not demoted: `EDUCATION`, `TEACHING & RESEARCH`, `LEADERSHIP & COMMUNITY`, `COMPETITIONS`, `PROJECTS`, with `subtitle` repurposed into a factual contents line. Direct user instruction ("don't add any HERO type names... just simple things speaking what do they have in them?"), and it fixes a real 90-second-rule failure: five flavor titles, three of them truncating, told a recruiter nothing about what any episode held. Ids were renamed to match, and ZNotes moved from the leadership episode into teaching where someone looking for teaching experience will actually find it. The cover slot is no longer a flat gradient reading "EP 1" — `src/components/EpisodeWindow.jsx` draws a real per-episode scene, so `src/assets/covers/` being empty no longer looks unfinished. Real painted art would still supersede it; it is a genuine stand-in now rather than a hole. `choicePrompt` and the choice labels are the last copy still in the old voice — the user has scheduled that pass for after this screen. **This batch — a complete pass over the whole resume surface against a NEW master resume the user supplied (`~/Downloads/master-resume.md`), and two of the changes are corrections, not polish.** (1) **The research content was wrong.** Both `episodes.js` and `profile.js` described the RA post as generic knowledge-graph construction and link prediction, and claimed a co-authored unpublished image-processing paper. The master resume says it is **CHIP, an HEC-funded project on whether disease incidence in Pakistan tracks weather and natural-disaster patterns** — where finding and cleaning barely-documented national data is the hard part — and that the image processing is a **separate, just-started collaboration on why knee surgeries fail** whose role is explicitly "not yet defined." Episode 2's two research panels were rewritten and renamed (`DISEASE AND WEATHER`, `WHY KNEE SURGERIES FAIL`), and `profile.js` now carries the image-processing work as its own `experience[]` entry scoped as early-stage rather than folded into the RA role. (2) **EduGap and TaxNet no longer read as personally engineered.** The master resume is explicit on both ("keep the resume honest… do NOT claim you personally engineered the harness/RAG/pipeline") — the contribution was architecture and planning input plus the winning pitch. Both panels now name that split in their closing sentence and tag `Architecture`/`Pitch` instead of implementation stacks. That is content, not tone: it should not be rewritten into something punchier that reclaims authorship. Additions: `OFF CAMPUS` on episode 3 (Sundas Foundation, the Mehra Abadi water campaign, Code for Pakistan, the Help 4 Help webinar — community service had no representation on the game surface at all) and `CITYMIND, ONE LIVE GRAPH` on episode 5, promoted out of `profile.js`'s `alsoBuilt[]` now that the master resume confirms a specific role (the CSP layout solver, the dynamic A* router, the risk-to-cost feedback loop, the Tkinter dashboard, integrating all five modules). Two links previously recorded as unfindable are now wired up — CityMind's repo and EduGap's. Panels: 19, was 17; all bodies re-verified at 30–60 words and headers at 2–5, every choice target still resolving. **Subtitles were held at or under 60 characters on purpose** — the length of the longest one `ChapterSelect.jsx`'s hero `min-h` reservations were measured against live, since a longer contents line would wrap past the reservation and reintroduce the resize-on-hover defect that screen was fixed for; episodes 2 and 5 were drafted at 64 and 67 and trimmed back for that reason. `profile.js` also picked up the current standing (fifth semester, Fall 2026 in-progress coursework), per-course TA dates, both ProSports roles, the Governance Crisis report, the Help 4 Help webinar, and feature extraction / link prediction / an `Other` skills group. Still out, still flagged: any marks figure for the C++ Smart City, which the new master resume continues to list as an unresolved 134/134-versus-102 discrepancy. `npm run lint` clean, `npm run build` clean at 142.9 KB gzipped. See `src/data/CLAUDE.md`. **This batch — the voice-ledger project was removed from the site entirely on user request ("I don't want the voice ledger app reference here AT ALL").** Its `episodes.js` `projects` panel was deleted (that episode: 4 → 3 panels; whole-file total 19 → 18), the `projects` subtitle and `build.branch.resolution` were rewritten to drop the ledger, the two `projects` choices were repointed/relabelled for the new panel order, and `profile.js`'s `alsoBuilt[]` landing-page entry was deleted. `src/data/CLAUDE.md`'s private-repo note and one `docs/PortfolioSpec.md` Mechanic 6 mockup label were also cleared. All bodies still 30–60 words, headers 2–5, every choice target resolves. **This batch — the `/plain` Dossier rebuild (Phase 5 above) touched this file's schema, not its copy.** `episodes.js`'s `competitions` episode changed from `plainRole: 'projects'` to its own `plainRole: 'competitions'` (a real bug fix: its five hackathon panels were rendering mixed into the three actual project panels under one "Projects" heading on `/plain` — they're separate sections now), `PLAIN_SECTIONS` gained the matching second entry, and `profile.js` gained a `highlights[]` array for the Dossier's stat band. No panel/episode prose changed; panel total is still 18. **This batch — Promptopia result corrected.** Direct user report: he and Raja Muhammad Ali came *runners-up*, not "no placement." `episodes.js`'s `PROMPTOPIA, NO MEDAL` panel is now `PROMPTOPIA, RUNNERS-UP` (body rewritten, still 30–60 words, teammate named), the `competitions` subtitle is now `Three second places and a national top ten`, and `profile.js`'s `awards[]` gained a Promptopia runners-up line. Panel total unchanged at 18. See `src/data/CLAUDE.md`. |

| 11 | **The game surface** (not in the original spec — see §0.1) | 🚧 **Built and verified this batch; art should be eyeballed by a human.** New `src/game/` layer (engine, shared ink drawing vocabulary, runtime palette bridge, React host) plus `src/data/games.js`, `src/screens/Floor.jsx` (`/floor`) and `src/screens/Play.jsx` (`/play/:id`), wired into `App.jsx` under the same `AnimatedOutlet` layout as every other screen so the camera dolly, scroll reset and ESC treat them normally (`PageTransition.jsx`'s depth table gains `/floor`=1 and `/play/:id`=2; `EscapeBack.jsx` maps `/play/:id`→`/floor` and `/floor`→`/`). `MainMenu.jsx`'s NEW GAME now goes to `/floor`; EPISODES is relabelled **EPISODE EXPLORER** and still goes to `/chapters`. `ProgressContext.jsx` gains a `cleared` set — deliberately separate from `visited`, so reading an episode in the ungated Explorer does not unlock a door and clearing a door does not pretend you read it. **Five minigames, five genuinely different verbs** (timing / triage / management / endurance / puzzle), each tuned by headless simulation rather than by feel — see `src/game/CLAUDE.md` for the numbers. **Bugs found and fixed during integration, worth not repeating:** the overworld walker was drawn at the virtual origin instead of its world position (invisible); door labels were drawn at `FLOOR_Y + 34` inside `drawDoor()` and `drawFloor()`'s ink fill painted over every episode title (labels now draw in their own pass after the floor); the corridor's first window was positioned on top of the stairwell (windows are now derived from the door grid at half-spacing); `grain()` composited `overlay`, which against an `--ink` ground pushed the whole frame to muddy brown (now `soft-light`); the back wall was a flat mid-`--shadow` fill covering ~60% of the frame, flattening every silhouette (now near-black with local light pools); `GameCanvas`'s canvas used `h-full` inside a `flex-1` child with no definite height, so on a 375x812 phone it rendered a 188px scene in a 471px slot with ~700px of dead black below (now `absolute inset-0`); `fortyEight.js` drew its own beat card, making the same paragraph appear three times at once alongside `Play.jsx`'s DOM flash and record column (removed — DOM wins because the virtual box scales to ~0.39 on a 375px screen, which rendered 14px canvas text at ~5px); and `visitEpisode()` returned a fresh object rather than spreading `prev`, which would have wiped all game progress the first time anyone read an episode. **Verification:** `npm run verify:scenes` ALL PASSED across all five scenes under three input modes; a separate headless harness drove `overworld.js`'s `update()` and confirmed walking focuses the stairs then door 1, an unlocked door emits `enter`, a locked door emits `locked` and never navigates, the stairwell emits `stairs`, and returning from a room spawns at that door. All five screens screenshotted live at 1280x800 and `/floor` at 375x812 with no horizontal overflow. **The one thing not verifiable here:** the games cannot be *played* in this environment — the Browser pane does not run `requestAnimationFrame` (measured 0 ticks in 500ms), so motion, difficulty feel and collision behaviour have only been simulated, never played. That needs a human in a real browser. **THIS BATCH — one design change and six live-reported bugs, all fixed and verified.** **THE DESIGN CHANGE, and it was the important one: the resume had gone missing behind the games.** Reported verbatim — "it's like we are MORE indulged into the game that the Resume is BARELY JUST NOT THERE... make it such that the resume isn't just something on the side but rather something we are continuously working upon." That was `src/screens/Play.jsx`'s fault rather than any scene's: the record column started literally empty ("Nothing yet. Play to fill it."), so for the first thirty seconds of every game there was no resume on screen at all, and the brief card explained the mechanic without ever saying what it had to do with the person. Three fixes, all in the shared chrome so all five games got them at once: (1) a new required **`why`** field in `src/data/games.js` states the metaphor in plain words — "The tower you stack IS the transcript", "Each meter is a real society, event or community" — printed on the brief card before the canvas mounts and again on the results card, and the brief card now leads with the episode's own number/title/subtitle and lists every beat label under "What you will uncover"; (2) the record column is **pre-seeded with every beat from before the first frame**, dim under its own label and filling in as the game pays it out, so it reads as a table of contents being completed rather than an empty box that might eventually contain something; (3) the results card prints the **whole** record including the lines a short run never reached, marked as such but printed in full — the game may be lost, the resume may not be locked (§6 rules 1 and 2). `why` doubles as a design test: if a sixth game cannot be given that line, the mechanic is wrong, not the copy. **THE SIX BUGS.** (a) *Wrong toast on a locked door* — `Floor.jsx` fired the `episodeFirstOpen` pool, so failing to get into a barred room answered "THE RECRUITER WILL REMEMBER THAT."; there is now a `doorLocked` pool that names the bar and points at the stairs. (b) *The desk is interactable* and opens `/plain`, at world x 246 (exactly `drawStudy`'s `lampX`), with the default spawn moved 190 → 120 so a fresh arrival is not already inside its prompt — the plain resume now has a place in the world, not just a header link, which is the same argument that put the stairwell inside the corridor. (c) *HANDS UP was two different games* — keyboard auto-targeted the most urgent hand for free (winnable by mashing Space, arrows decorative) while the pointer needed to hit an ~18-unit circle that letterboxes to a few real pixels on a phone; the keyboard fallback is now nearest-in-screen-space and the pointer hit-tests the whole arm within a phone-sized radius. (d) *`/play` scrolled the page mid-game* ("in level 3 the screen goes into scrollable at the end") — the record column's list had no `min-h-0`, so a flex child grew instead of scrolling and once eight beats landed the whole page outgrew the viewport; the playing layout is now exactly one viewport tall on `lg` with the record scrolling inside its own column. (e) *EVENT DAY let a dead meter climb back off the floor* — a collapse is now final and unselectable, and if all four die the run flushes every unrevealed beat before ending. (f) *FORTY-EIGHT HOURS was too hard* — the level was widened AND, after a control run proved the level was no longer the bottleneck, the flight constants were scaled to 0.4× (measured lag tolerance 108ms → 192ms) with the gravity:lift symmetry preserved. **The unreachable first place at hour 48 was not touched and must never be.** **ALSO: `ONE LIVE GRAPH` was rebuilt** after "it's like pointless and can't be understood" — five labelled sockets instead of one vague drop ellipse, and the city now *accumulates* one attributable layer per module wired (districts, route, risk, road weights, feedback loop) instead of sitting there finished from frame 0. **Verification:** `npm run verify:scenes` ALL PASSED across all five scenes and three input modes; `npm run lint` clean (pre-existing warnings only, none in a touched file); `npm run build` clean at **175.10 KB gzipped**, under the 200 KB budget. **Still not verifiable here, unchanged from the previous batch:** the games cannot be *played* in this environment — the Browser pane does not run `requestAnimationFrame` — so every difficulty number above comes from headless simulation and the rebuilt graph screen has never been seen. A human in a real browser is still the missing check. |

**Resolved blocker:** `src/App.jsx` is no longer the default Vite
template. It is now the router root (`<BrowserRouter>` → `ProgressProvider`
→ `SettingsProvider` → `ToastProvider` → `AmbientLayer` + `<Routes>`). The
dead imports (`./App.css`, `./assets/react.svg`, `./assets/vite.svg`,
`./assets/hero.png`, the `/icons.svg` sprite reference) were deleted, not
recreated — `npm run build` and `npm run lint` both pass.

## 6. Non-negotiable rules (spec §2) — do not trade these away for polish

1. No forced interaction — every choice is optional, content reachable by scroll/keyboard alone.
   **How the locked doors on `/floor` coexist with this (see §0.1):** they
   only do so because that route is not the only way in. `/chapters` (the
   Episode Explorer) and `/plain` are both completely ungated and reachable
   from every screen, a locked door says what it wants and points at the
   stairwell that leads to the Explorer, and `Play.jsx`'s results card
   offers the next door outright after two failed runs. **If you remove
   the stairwell, or make the Explorer depend on `cleared`, you have
   broken this rule.** The two progress sets are kept apart in
   `ProgressContext.jsx` for exactly this reason.
2. No timers that gate content. (A minigame's own clock is not a content
   gate: nothing it shows is unavailable in the Explorer or on `/plain`,
   and no minigame can be lost in a way that hides a beat — a run that
   ends early still lands on the results card with everything uncovered.
   That is why the scene contract has no `'lose'` outcome.)
3. A plain-text escape hatch always exists (`/plain` route + persistent PDF download).
   **Fixed earlier:** `/plain` used to render episode panels plus a name
   hardcoded in JSX — no email, no education, no skills anywhere in the data —
   so a recruiter who took the escape hatch could not actually act on it. It
   now builds a real resume from `src/data/profile.js`. The PDF button is
   still `window.print()`; no static PDF asset exists yet.
   **Superseded this batch:** this rule used to be read (by an earlier
   implementation, not by anything the spec actually says) as "no theme
   styling at all" — `/plain` rendered in the browser's plain default
   black-on-white with no tokens, no display face, nothing. That reading
   is gone. Spec §7 requires exactly three things of this route: semantic
   headings, no ARIA gymnastics, and being linked from every screen — none
   of which forbid `--ink`/`--paper`/the display face. `/plain` ("The
   Dossier," rebuilt this batch — see Phase 5 below) now carries the
   site's full design language on the light half of the palette. What
   hasn't changed: the actual PDF a recruiter saves is still genuinely
   plain. The `@media print` block appended to `src/index.css` this batch
   forces true black-on-white regardless of the on-screen theme, so the
   escape hatch's *output* is exactly as unthemed as this rule always
   intended — only the on-screen reading page picked up styling.
4. Load fast — under 2s to interactive on mid-range Android/4G. No video backgrounds, no multi-MB hero images.
5. `prefers-reduced-motion: reduce` disables all transitions/parallax/typewriter — content appears instantly. (Enforced two ways now: `src/index.css`'s global CSS-level media-query fallback, PLUS a `.motion-off`-scoped mirror of the same rule toggled by `src/components/AmbientLayer.jsx` whenever `useSettings().motion` is off, so the in-app toggle disables plain-CSS transitions too, not just the OS setting. Framer Motion components (`RevealPanel.jsx`, `ToastProvider.jsx`, `TitleScreen.jsx`, `EpisodeView.jsx`) each combine `useReducedMotion()` with `useSettings().motion` directly. `Typewriter.jsx` is now built and combines `useReducedMotion()` with both the `typewriter` and `motion` settings, short-circuiting to the complete string by initialising to full length rather than animating fast — so text is never caught mid-reveal.)
6. Mobile is not an afterthought — chapter grid → single column, choice buttons → full-width stacked.

## 7. Accessibility & performance floors (spec §7)

- All interactive elements are real `<button>`/`<a>`, never a div with a click handler.
- Visible focus ring: 2px `--signal` outline, 2px offset — never removed. (Done globally in `src/index.css`.)
- `--signal` on `--paper` must be contrast-checked before using it for text (may only pass at large sizes).
- `/plain` has semantic headings, no ARIA gymnastics, linked from every screen.
- Fonts: Latin-subset WOFF2, preloaded. **Done** — two real files in `src/assets/fonts/`, `@font-face` in `src/styles/tokens.css`, preloaded in `index.html`, verified against a production build that the preload and the CSS `url()` resolve to the same hashed asset. Cover images: WebP, ≤1200px wide, lazy loaded — still pending real art (Phase 10). Textures: inline SVG data URIs only (done in `src/styles/textures.css`). Total JS bundle target: under 200KB gzipped — currently **~144.8KB gzipped** (`npm run build` output, after the Batman-reference chapter-select rebuild and `EpisodeArt.jsx`'s third pass); still comfortably under budget. The five episode scenes cost ~2KB of JS and zero image bytes — inline SVG, same reasoning as the textures and the synthesized sound engine. The sound engine added ~0.5KB — Web Audio synthesis rather than shipped audio files, per the performance floor.

## 8. Prompt template for handing off a build phase

Per spec §9, when assigning a phase to Sonnet/Haiku:

> Build phase [N] from `docs/PortfolioSpec.md`. Follow the design tokens
> in §3 exactly — no substituted colors or typefaces. All content must
> come from `src/data/episodes.js`; no component may hardcode copy.
> Respect every rule in §2. Before writing code, restate which files you
> will create or modify and why. After building, update this root
> `CLAUDE.md` and the affected folder `CLAUDE.md`(s) per §1 above, and
> list anything in the spec you could not implement and why.

## 9. Deployment (not in the original spec)

The repo is pushed to GitHub (`itsabdullahaamir/PortfolioWebsite`, public)
and served live via GitHub Pages at
**https://itsabdullahaamir.github.io/PortfolioWebsite/**, rebuilt
automatically by `.github/workflows/deploy.yml` on every push to `main`.
This exists purely to give outside testers a stable link — it is
infrastructure, not a spec phase, and doesn't affect the Build Phase
Status table in §5.

Three things had to change to make a client-routed (`BrowserRouter`) SPA
work on GitHub Pages, which serves static files with no server-side
rewrites:

1. `vite.config.js` sets `base: '/PortfolioWebsite/'` — required because
   Pages serves a project site (no custom domain configured) from a
   `/<repo-name>/` subpath, not the domain root.
2. `index.html`'s favicon link had to switch from a raw `/favicon.svg` to
   `%BASE_URL%favicon.svg` — Vite only rewrites bundled asset references
   for the new `base`, not literal absolute paths to `public/`.
3. `public/404.html` (new) plus a matching inline decode script in
   `index.html`'s `<head>` implement the standard
   [rafgraph/spa-github-pages](https://github.com/rafgraph/spa-github-pages)
   redirect trick, so a direct load or refresh of `/chapters`, `/plain`,
   etc. resolves instead of 404ing. See `public/CLAUDE.md` for the
   mechanism detail.

If a custom domain is ever added, `base` should revert to `/` and the
404 trick can be removed if the host does its own SPA fallback — flagged
here so a future change doesn't leave stale subpath config behind.
